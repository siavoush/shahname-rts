# Codex — Questions for Design

Code sessions append questions here instead of guessing on lore, feel, naming, or content substance. The design chat (Cowork with Siavoush) answers them. **Do not resolve these unilaterally** — use the `aka`/placeholder until answered.

Format: `### [OPEN|ANSWERED] short title` + context + (answer when resolved).

---

### [OPEN] Spelling: Siavash vs. Siavoush
Scholarly is *Siyāvaš*; Siavoush is Siavoush's own preferred form. Which is the entry `title` (id)? The other → `aka`. Affects `id` stability, so decide before authoring his entry.

### [OPEN] Spelling: Simorgh vs. Simurgh
`00_SHAHNAMEH_RESEARCH.md` uses "Simurgh"; "Simorgh" is closer to سیمرغ. Pick one for the `id` (currently stubbed as `simorgh`).

### [OPEN] Spelling: Haft Khan vs. Haft Khwan
Research doc uses "Khwan"; simplest `id` is `haft-khan`. Confirm.

### [OPEN] Sekandar (Alexander) — register
He straddles legend and history. What `register` (legend | history) when authored?

### [OPEN] farr.md primary text
Needs the canonical Jamshid farr-departure passage fetched from Ganjoor (verbatim, with provenance) for the `## Primary text` section. Verse selection is a design call; fetching is mechanical (Code's verse-fetch tool can pull candidates once the passage is chosen).
