---
title: Architecture — Target Shape and Build State
type: architecture
status: living
version: 0.17.6
owner: engine-architect
summary: Orientation layer — system map, subsystem build state, tick pipeline summary, directory rationale, contract index. Read first in implementation mode after MANIFESTO and CLAUDE.md.
audience: all
read_when: implementation-mode
prerequisites: [MANIFESTO.md, CLAUDE.md]
ssot_for:
  - subsystem build state table (planned/in-progress/built)
  - directory layout with rationale
  - contract index (which doc to read for which domain)
  - plan-vs-reality delta record
  - high-level system map (UI / simulation / event bus / foundation layers)
  - pinned Godot engine version
references: [SIMULATION_CONTRACT.md, STATE_MACHINE_CONTRACT.md, TESTING_CONTRACT.md, RESOURCE_NODE_CONTRACT.md, AI_DIFFICULTY.md, ../02_IMPLEMENTATION_PLAN.md, STUDIO_PROCESS.md]
tags: [orientation, architecture, build-state, directory, system-map]
created: 2026-05-01
last_updated: 2026-05-01
# bumped to v0.17.6 — Phase 2 Session 1 post-live-test: BUG-05 click-tolerance fallback (ai-engineer)
---

# Architecture — Target Shape and Build State

> This document is the **orientation layer** for the project. After `MANIFESTO.md` and `CLAUDE.md`, this is the first thing anyone (agent or human) reads when picking up work — especially after a context boundary (compaction, time off, fresh session). It exists so you don't have to re-read every contract and every commit just to know where you are.
>
> **What lives here:** the target shape, the build state, the data flow, the directory map. **What lives elsewhere:** the *decisions* that shaped each subsystem (in `docs/*_CONTRACT.md`), the *plan* for getting there (in `02_IMPLEMENTATION_PLAN.md`), the *principles* behind everything (in `MANIFESTO.md`).

---

## 1. The System Map

The game is a deterministic real-time simulation with seven phases per tick at 30 Hz. Subsystems are organized into four layers:

```
┌─────────────────────────────────────────────────────────────────┐
│                       UI LAYER (off-tick)                        │
│  Camera • SelectionManager • CommandManager • HUD • Minimap     │
│  DebugOverlayManager (F1-F4) • Farr Gauge • Build Menu          │
└────────────────────┬────────────────────────────────────────────┘
                     │ reads sim state freely
                     │ writes sim state ONLY through queued commands
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                   SIMULATION LAYER (on-tick)                     │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ SimClock (30 Hz, runs the phase pipeline)                │   │
│  │   input → ai → movement → spatial_rebuild → combat       │   │
│  │            → farr → cleanup                              │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                  │
│  Systems (phase coordinators):                                   │
│   - InputSystem             - CombatSystem                       │
│   - AIControllerHost        - FarrSystem                         │
│   - MovementSystem          - CleanupSystem                      │
│   - SpatialIndex (rebuild)                                       │
│                                                                  │
│  Components (extend SimNode, register with phase coordinators):  │
│   - HealthComponent  - MovementComponent  - CombatComponent      │
│   - SelectableComponent  - SpatialAgentComponent                 │
│                                                                  │
│  Entities (composed of components):                              │
│   - Units (Kargar, Piyade, Kamandar, Savar, Asb-savar, Rostam)   │
│   - Buildings (Throne, Khaneh, Mazra'eh, Atashkadeh, ...)        │
│   - ResourceNode → MineNode | Mazra'eh                           │
└────────────────────┬────────────────────────────────────────────┘
                     │ emits typed signals (write-shaped + read-shaped)
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                          EVENT BUS                               │
│   Single autoload. Every signal declared with typed args.        │
│   Telemetry sink subscribes to write-shaped signals.             │
└────────────────────┬────────────────────────────────────────────┘
                     │
       ┌─────────────┴─────────────┐
       ▼                           ▼
┌──────────────┐          ┌──────────────────┐
│ Telemetry    │          │ MatchLogger      │
│ (Phase 0)    │          │ (Phase 6)        │
│ in-memory    │          │ NDJSON to disk   │
│ JSON dump    │          │ per tick events  │
└──────────────┘          └──────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                       FOUNDATION LAYER                           │
│  SimClock • TimeProvider • GameRNG (domain-keyed) • EventBus    │
│  Constants • BalanceData (resource) • GameState                  │
│  SimNode (base class) • StateMachine + State (core)              │
│  IPathScheduler (interface) → NavigationAgent | MockPathScheduler│
└─────────────────────────────────────────────────────────────────┘
```

**Key invariant:** gameplay state mutates only inside `_sim_tick()` calls dispatched by `SimClock`. UI reads freely; UI never writes synchronously in a sink callback (uses queue-then-drain). Per Sim Contract §1.

---

## 2. State of the Build

**Pinned engine version:** Godot **4.6.2 stable** (official build `71f334935`), recorded in `game/project.godot` `application/config/godot_version`. Bumping the MAJOR (e.g., 5.x) requires a new `DECISIONS.md` entry per the 2026-05-01 decision; patch updates do not.

| Subsystem | Target spec | Status | Version | Owner | Notes |
|-----------|-------------|--------|---------|-------|-------|
| **Godot engine** | `DECISIONS.md` 2026-05-01 | ✅ Built | 4.6.2-stable | engine-architect | Installed via Homebrew cask `godot`; binary at `/opt/homebrew/bin/godot`. |
| **Godot project init** | [02_IMPLEMENTATION_PLAN.md Phase 0](../02_IMPLEMENTATION_PLAN.md) | ✅ Built | — | engine-architect | `game/project.godot` exists; main scene boots; placeholder `Main` scene confirms SimClock ticks. |
| **SimClock autoload** | [SIM 1.2.0 §1.2](SIMULATION_CONTRACT.md) | ✅ Built | — | engine-architect | 30Hz fixed-tick driver, accumulator pattern in `_physics_process`, emits `tick_started` / `sim_phase × 7` / `tick_ended`. `is_ticking()` and the `_test_run_tick` / `_test_advance` / `reset` test hooks ship session 1. Unit tests in `tests/unit/test_sim_clock.gd`. |
| **SimNode base class** | [SIM 1.2.0 §1.3](SIMULATION_CONTRACT.md) | ✅ Built | — | engine-architect | `_sim_tick(_dt)` virtual + `_set_sim(prop, value)` with on-tick assert. Self-only mutation discipline documented in source. Unit tests in `tests/unit/test_sim_node.gd`. |
| **EventBus autoload** | [SIM 1.2.0 §7](SIMULATION_CONTRACT.md) | ✅ Built | — | engine-architect | Session 1 declares `tick_started`, `tick_ended`, `sim_phase`. Phase 0 forward-declared `unit_health_zero` and `unit_state_changed` for State Machine death-preempt and telemetry. Phase 0 session 4 added `farr_changed`. **Phase 2 session 1 wave 1A added `unit_died(unit_id, killer_unit_id, cause, position)`** — gameplay-systems wired the producer in HealthComponent and added the signal + forwarder arm + `_SINK_SIGNALS` entry; payload-position lets Phase 5 Yadgar consumers read death position off the listener side without calling back into a freed component. Re-entrancy comment block above the declaration warns listeners against synchronous cross-listener mutation (cb95d09 lesson). `connect_sink` / `disconnect_sink` API shipped; no consumer yet (MatchLogger is Phase 6). Unit tests in `tests/unit/test_event_bus.gd`. |
| **GameRNG autoload** | [SIM 1.2.0 §5](SIMULATION_CONTRACT.md) | 📋 Planned | — | engine-architect | Phase 0 session 2; 4 domains |
| **TimeProvider autoload** | [SIM 1.2.0 §1](SIMULATION_CONTRACT.md) | ✅ Built | — | engine-architect | Wraps `Time.get_ticks_msec()`; `set_mock` / `clear_mock` / `is_mocked` for deterministic tests. Unit tests in `tests/unit/test_time_provider.gd`. |
| **SpatialIndex autoload** | [SIM 1.2.1 §3](SIMULATION_CONTRACT.md) | ✅ Built | — | engine-architect | 8m uniform grid (XZ plane, Y ignored). Three queries: `query_radius`, `query_nearest_n`, `query_radius_team`. Auto-population via `SpatialAgentComponent` (extends `SimNode`); registers on `_ready`, deregisters on `_exit_tree`. Rebuild listens on `EventBus.sim_phase(&"spatial_rebuild", ...)`. Unit tests in `tests/unit/test_spatial_index.gd`. |
| **IPathScheduler** | [SIM 1.2.1 §4](SIMULATION_CONTRACT.md) | ✅ Built | — | engine-architect | Interface (Phase 0 session 3). `PathState` enum, `request_repath` / `poll_path` / `cancel_repath`. `PathSchedulerService` autoload holds the active scheduler. Phase 1 session 1 wave 1: `set_scheduler` / `reset` semantics changed — `reset()` now reverts to a fresh production `NavigationAgentPathScheduler` instance instead of clearing to null (logged §6 v0.9.0). Concrete implementations: `MockPathScheduler` (qa-engineer, Phase 0 session 4) and `NavigationAgentPathScheduler` (engine-architect, Phase 1 session 1 wave 1). |
| **CI lint script** | [SIM 1.2.0 §1.4](SIMULATION_CONTRACT.md) | ✅ Built | — | qa-engineer | `tools/lint_simulation.sh`. All 5 rules (L1-L5) implemented with allowlists for L3 (`rng.gd` when it lands) and L5 (`time_provider.gd`, `sim_clock.gd`). Exits non-zero on any violation. Verified against deliberate violation test files for each rule. |
| **Pre-commit hook** | [TEST 1.4.0](TESTING_CONTRACT.md) | ✅ Built | — | qa-engineer | Canonical hook at `tools/git-hooks/pre-commit`; install via `bash tools/install-hooks.sh`. Runs lint then GUT; blocks commit on either failure. |
| **GUT framework** | [TEST 1.4.0](TESTING_CONTRACT.md) | ✅ Built | 9.4.0 | qa-engineer | Installed at `game/addons/gut`. Headless runner: `game/run_tests.sh` (`godot --headless ... -s addons/gut/gut_cmdln.gd -gconfig=res://.gutconfig.json`). 28 tests across 4 unit-test scripts pass at session 1 close. Pre-commit hook + lint script land session 2. |
| **MatchHarness** | [TEST 1.4.0 §3](TESTING_CONTRACT.md) | ✅ Built | — | qa-engineer | Phase 0 session 4 wave 2. `tests/harness/match_harness.gd` (no class_name — registry-race pattern). `start_match(seed, scenario)` resets all autoloads, injects MockPathScheduler, seeds global RNG (GameRNG TODO), loads BalanceData with validate_hard/validate_soft, sets GameState to PLAYING. `advance_ticks(n)` via SimClock._test_run_tick (shared code path with live driver). `snapshot()` returns flat primitive-only Dict (tick/farr/coin_iran/grain_iran/coin_turan/grain_turan/unit_count_iran/unit_count_turan). `_test_set_farr(value)` direct off-tick write to `FarrSystem._farr_x100` + synthetic farr_changed emit. `set_resources/get_resources/get_unit` helpers. `teardown()` resets all autoloads. Scenarios in `tests/harness/scenarios.gd` (CATALOG dict: empty/starved/rich/kaveh_edge/kaveh_triggered/basic_combat). FarrSystem.reset() added cross-domain (documented in §6). Tests in `tests/unit/test_match_harness.gd` (19) + `tests/integration/test_match_harness.gd` (25). |
| **MockPathScheduler** | [TEST 1.4.0 §3](TESTING_CONTRACT.md) | ✅ Built | — | qa-engineer | Phase 0 session 4 wave 1. `scripts/navigation/mock_path_scheduler.gd` extends `IPathScheduler`. Straight-line two-waypoint paths; READY on `requested_tick + 1`; `cancel_repath` sticky; `fail_next_request()` for FAILED-path tests; public `call_log: Array[Dictionary]`; `get_request_count_for_unit(id)`; `clear_log()`. 15 unit tests in `tests/unit/test_mock_path_scheduler.gd`. class_name registry race: `_mock` field in test uses `Variant` typing per ARCHITECTURE.md §6 v0.4.0 pattern. |
| **Determinism regression test** | [TEST 1.4.0 §6.2](TESTING_CONTRACT.md) | ✅ Built | — | qa-engineer | Phase 0 stub (session 4 wave 2). `tests/integration/test_determinism.gd`. Three tests: empty_match_is_deterministic (same seed → same snapshot after 60 ticks), different_seeds_produce_same_empty_snapshots (Phase 0 no-RNG-consumer case), sequential_harnesses_are_isolated (teardown isolation). Will grow Phase 1+ as units/combat/Farr generators register with the tick pipeline. |
| **BalanceData resource** | [TEST 1.4.0 §1](TESTING_CONTRACT.md) | ✅ Built | — | balance-engineer | Phase 0 session 4 wave 1. `data/balance_data.gd` (`class_name BalanceData extends Resource`). Six sub-resources: `UnitStats`, `BuildingStats`, `FarrConfig`, `CombatMatrix`, `EconomyConfig` (contains `ResourceNodeConfig`), `AIConfig` (12 flat fields for easy/normal/hard). `validate_hard()` / `validate_soft()` for match-start gate. `balance.tres` at `data/balance.tres`. Tests in `tests/unit/test_balance_data.gd`. |
| **Constants autoload** | [02_IMPLEMENTATION_PLAN.md §2](../02_IMPLEMENTATION_PLAN.md) | ✅ Built | — | engine-architect | Phase 0 session 3. Structural keys/enums only — phase StringNames, EventBus signal name keys, team identifiers (`TEAM_IRAN`, `TEAM_TURAN`, `TEAM_NEUTRAL`, `TEAM_ANY`), resource kinds, state ids, command kinds, structural caps. Tunable numbers live in `BalanceData.tres` (session 4). Unit tests in `tests/unit/test_constants.gd`. |
| **GameState autoload** | [02_IMPLEMENTATION_PLAN.md §2](../02_IMPLEMENTATION_PLAN.md) | ✅ Built | — | engine-architect | Phase 0 session 3. Match phase (`lobby`/`playing`/`ended`), `winner_team`, `match_start_tick` (captures `SimClock.tick` on `start_match`), `player_team`. `match_tick()` / `match_time()` give relative offsets. Tests in `tests/unit/test_game_state.gd`. |
| **StateMachine + State** | [STATE 1.0.0](STATE_MACHINE_CONTRACT.md) | ✅ Built | — | engine-architect | Phase 0 session 3 (framework). `core/state_machine/` ships `State`, `StateMachine`, `Command`, `CommandQueue`, `UnitState`, `InterruptLevel`. `CommandPool` autoload pre-allocates Commands. Death-preempt connected to `EventBus.unit_health_zero`; transition history ring buffer (16 entries unit / 64 AI); `transition_to_next()` dispatcher pops `Command` and maps kind→state-id. Concrete unit states (Idle, Moving, etc.) ship Phase 1+. Tests in `tests/unit/test_state_machine.gd`. |
| **CommandPool autoload** | [STATE 1.0.0 §2.5](STATE_MACHINE_CONTRACT.md) | ✅ Built | — | engine-architect | Phase 0 session 3. `rent()` / `return_to_pool()` over a pre-allocated pool. Auto-resets on rent and return. Tests share the pool via `tests/unit/test_state_machine.gd` (CommandPool fixtures). |
| **PathSchedulerService autoload** | [SIM 1.2.1 §4.3](SIMULATION_CONTRACT.md) | ✅ Built | — | engine-architect | Phase 0 session 3 (autoload only). Holds an `IPathScheduler` instance for `MovementComponent` to read; `null` until session 4 wires real/mock impls. Tests in `tests/unit/test_path_scheduler_service.gd`. |
| **Camera Controller** | [02_IMPLEMENTATION_PLAN.md Phase 0](../02_IMPLEMENTATION_PLAN.md) | ✅ Built | — | ui-developer | Phase 0 session 3 wave 2. Fixed isometric, no rotation. WASD pan + edge-pan (50px threshold) + scroll-wheel zoom (clamped). Diagonal input normalized; frame-rate independent. Bounds clamp to `Constants.MAP_SIZE_WORLD` half-extent. Rig at `scenes/camera/camera_rig.tscn` (yaw -45° baked in scene); controller at `scripts/camera/camera_controller.gd`. Tests in `tests/unit/test_camera_controller.gd` (19). NOT yet wired into `main.tscn` — engine-architect's session 4 task. |
| **Terrain plane (256×256)** | [02_IMPLEMENTATION_PLAN.md Phase 0](../02_IMPLEMENTATION_PLAN.md) | ✅ Built | — | world-builder | Phase 0; flat plane with checkerboard StandardMaterial3D placeholder. `Constants.MAP_SIZE_WORLD = 256.0` and `Constants.NAV_AGENT_RADIUS = 0.5` added. NavigationRegion3D with synchronous bake at scene-load, `PARSED_GEOMETRY_STATIC_COLLIDERS`. Scene: `scenes/world/terrain.tscn`. Script: `scripts/world/terrain.gd`. Tests: `tests/unit/test_terrain.gd` (7 tests). |
| **DebugOverlayManager** | [02_IMPLEMENTATION_PLAN.md Phase 0](../02_IMPLEMENTATION_PLAN.md) | ✅ Built | — | ui-developer | Phase 0 session 3 wave 2. Autoload registry: `register_overlay(key, Control)` / `unregister_overlay(key)` / `toggle_overlay(key)` / `handle_function_key(keycode)`. F1-F4 → `Constants.OVERLAY_KEY_F1`..`F4` (pathfinding / Farr log / AI state / attack ranges per Phase 6 / 4 / 6 / 2). `_unhandled_input` dispatches function keys; off-tick reads only per Sim Contract §1.5. Concrete overlays land WITH their owning systems (kickoff doc rule). Tests in `tests/unit/test_debug_overlay_manager.gd` (16). |
| **Translation infrastructure** | [02_IMPLEMENTATION_PLAN.md Phase 0](../02_IMPLEMENTATION_PLAN.md) | ✅ Built | — | ui-developer | Phase 0 session 4 wave 1. `translations/strings.csv` with `keys,en,fa` columns; auto-imported to `strings.en.translation` / `strings.fa.translation`. `project.godot` `[internationalization]` registers the en file with `fallback="en"`. Persian column is intentionally blank for Phase 0 — adding Tier 2 content is a CSV edit + project.godot list addition, no code change. Key conventions: `UI_*` (HUD), `UNIT_*`, `BLDG_*`, `EVENT_*`. Seed keys: `UI_FARR`, `UI_COIN`, `UI_GRAIN`, `UI_POPULATION`, `UI_TIER_VILLAGE`, `UI_TIER_FORTRESS`. All HUD labels read via `tr()`. Tests in `tests/unit/test_resource_hud.gd`. |
| **Telemetry sink** | [SIM 1.2.0 §7](SIMULATION_CONTRACT.md), [TEST 1.4.0 §2](TESTING_CONTRACT.md) | 📋 Planned | — | balance-engineer + engine-architect | Phase 0 (in-memory); Phase 6 MatchLogger |
| **FarrSystem skeleton** | [01_CORE_MECHANICS.md §4](../01_CORE_MECHANICS.md), [SIM 1.2.0 §1.6](SIMULATION_CONTRACT.md) | ✅ Built | — | gameplay-systems | Phase 0 session 4 wave 1. `scripts/autoload/farr_system.gd` extends SimNode via path-string. Fixed-point int storage (Farr × 100; 50.0 = 5000). `apply_farr_change(amount, reason, source_unit)` is the sole mutation chokepoint; asserts on-tick; emits `EventBus.farr_changed`. Generators (Atashkadeh, etc.) and drains (unit kills, etc.) ship Phase 4; Kaveh Event ships Phase 5. Tests in `tests/unit/test_farr_system.gd`. |
| **Farr HUD readout** | [01_CORE_MECHANICS.md §11](../01_CORE_MECHANICS.md) | ✅ Built | — | ui-developer | Phase 0 session 4 wave 1, refactored Phase 1 session 2 wave 1C. `scenes/ui/resource_hud.tscn` (CanvasLayer + MarginContainer + HBoxContainer) + `scripts/ui/resource_hud.gd`. Top-bar layout: `[Coin] [Grain] [Pop] [Spacer EXPAND] [FarrGauge]` — Coin/Grain/Pop labels stay left-anchored; `Spacer` (size_flags_horizontal=EXPAND, mouse_filter=IGNORE) pushes the gauge to the right edge per spec §11. Labels formatted via `tr()` for i18n. Polls in `_process` per Sim Contract §1.5 (UI off-tick reads unrestricted) — Coin/Grain/Pop only; Farr is no longer polled (the gauge subscribes to `EventBus.farr_changed` directly). Defensive reads for Coin/Grain/Pop look for declared property first, then `Object.get_meta(...)` — works during the Phase-0 holding pattern and after gameplay-systems' future ResourceSystem ships. Wired into `main.tscn` as a direct child. Tests in `tests/unit/test_resource_hud.gd` (12). |
| **Farr gauge** | [01_CORE_MECHANICS.md §4.4 / §11](../01_CORE_MECHANICS.md) | ✅ Built | — | ui-developer | Phase 1 session 2 wave 1C. `scenes/ui/farr_gauge.tscn` (Control, 64×64, mouse_filter=IGNORE) + `scripts/ui/farr_gauge.gd`. Custom `_draw` circular arc — placeholder visuals per CLAUDE.md, no asset dependency. `Control._draw` chosen over `TextureProgressBar` (no sweep texture available) and over `Polygon2D` (forces Node2D under the HUD CanvasLayer). Visual fill is `displayed_farr / tier2_threshold` clamped [0,1] (full meter at Farr=40 per kickoff §149); above Tier 2 the GOLD color band carries the overshoot signal, not fill ratio. Color bands per spec §4.4: <15 red, 15-40 dim, 40-70 ivory, ≥70 gold (inclusive lower bound — exactly 15→dim, 40→ivory, 70→gold). Threshold ticks (Tier 2 gold-medium, Kaveh red-thick) painted at angular positions per BalanceData.farr — never hardcoded (CLAUDE.md). `FARR_MAX = 100.0` from Constants. Data wiring is signal-driven via `EventBus.farr_changed` (subscribe in `_ready`, disconnect in `_exit_tree` for clean teardown); initial seed reads `FarrSystem.value_farr` once at _ready. Tween 0.20s `TRANS_QUAD EASE_OUT` per change — no debounce (balance-engineer veto: would silently drop F2-overlay log entries). `color_band` updated synchronously on signal (so observers see the new band immediately) AND per-tween-step (so intermediate band crossings during the tween fire the correct visual). Defensive autoload pattern matches `resource_hud.gd:192-200`. Below-Kaveh red pulsing animation (spec §4.4) DEFERRED to Phase 2 — when implemented, must drive from a `_process` flag (`_is_below_kaveh`) not the tween (tween finishes; pulse must keep animating). Floating reason-text labels (spec §4.4 "+3 Farr (hero rescued worker)") DEFERRED to Phase 2 — same `farr_changed` signal carries the `reason` payload. Threshold-crossing audio cues DEFERRED to Tier 2. Tests in `tests/unit/test_farr_gauge.gd` (29). |
| **Unit base scene + class** | [STATE 1.0.0 §5.1](STATE_MACHINE_CONTRACT.md) | ✅ Built | — | engine-architect | Phase 1 session 1 wave 1. `class_name Unit extends CharacterBody3D` at `scripts/units/unit.gd`; scene template at `scenes/units/unit.tscn`. Composes a placeholder MeshInstance3D (0.5×0.6×0.5 cube), CollisionShape3D, HealthComponent, MovementComponent, SelectableComponent, SpatialAgentComponent. Static `unit_id` counter auto-assigns ids (`reset_id_counter` for match-start). `_apply_balance_data_defaults` pulls `max_hp` and `move_speed` from `BalanceData.units[unit_type]`. `command_queue` constructed in `_init`; `replace_command` / `append_command` are the sanctioned write paths per State Machine Contract §2.5. Legibility helpers (`is_idle`, `is_engaged`, `is_dying`, `is_busy`) defensively handle uninitialized FSM. Concrete unit types (Kargar, Piyade, etc.) and concrete states (Idle, Moving) ship in Phase 1 session 1 wave 2+. Tests in `tests/unit/test_unit.gd` (15 tests). |
| **HealthComponent** | [STATE 1.0.0 §4](STATE_MACHINE_CONTRACT.md), [SIM 1.2.1 §1.6](SIMULATION_CONTRACT.md) | ✅ Built | — | engine-architect (Phase 1) + gameplay-systems (Phase 2 wave 1A) | Phase 1 session 1 wave 1. `class_name HealthComponent` extending SimNode at `scripts/units/components/health_component.gd`. Fixed-point storage (`hp_x100: int` per Sim Contract §1.6). `init_max_hp(float)` boundary-converts BalanceData values. `take_damage(amount, source)` and `heal(amount)` route through `_set_sim` (on-tick assert). Latches a single `EventBus.unit_health_zero(unit_id)` emit at hp=0 (no over-kill double-emit) — this is the trigger for StateMachine death-preempt per State Machine Contract §4.1. `hp` / `max_hp` / `is_alive` accessors. **Phase 2 session 1 wave 1A (gameplay-systems):** added `take_damage_x100(amount_x100: int, source: Node = null, cause: StringName = &"unspecified")` — the fixed-point hot path CombatComponent calls per attack tick (skips the float→fixed-point round-trip the float wrapper does). Both float and fixed-point paths converge in private `_apply_damage_x100(amount_x100, source, cause)` which is the canonical death chokepoint. Captures `last_death_position: Vector3` from the parent Node3D's `global_position` BEFORE any signal fires (Yadgar consumer in Phase 5 reads off the listener-side payload, not a possibly-freed component). Resolves `killer_unit_id` from `source.unit_id` duck-typed (`-1` sentinel when source is null or has no `unit_id`). Emits `EventBus.unit_health_zero(unit_id)` FIRST (FSM death-preempt per Contract §4.2), then `EventBus.unit_died(unit_id, killer_unit_id, cause, position)` (broader telemetry / Farr-drain channel) — order pinned in source-comments. The single `_zero_emitted` latch covers both signals so over-kill ticks emit neither. Tests in `tests/unit/test_health_component.gd` (now 23 tests; 6 added in wave 1A — 2 for `take_damage_x100` arithmetic, 3 for the new `unit_died` payload + `last_death_position` capture, 1 for `unit_health_zero` BEFORE `unit_died` ordering). |
| **MovementComponent** | [SIM 1.2.1 §4.1](SIMULATION_CONTRACT.md) | ✅ Built | — | engine-architect | Phase 1 session 1 wave 1. `class_name MovementComponent` extending SimNode at `scripts/units/components/movement_component.gd`. Wraps the IPathScheduler integration: `request_repath(target)` cancels prior in-flight requests and issues a new one through the injected scheduler. `_sim_tick(dt)` polls for the result; on READY, copies waypoints; on subsequent ticks advances the parent Node3D's `global_position` toward the current waypoint at `move_speed * dt`. `path_state` and `is_moving` are live computed properties. Position writes go through `Node3D.global_position` directly (Sim Contract §4.1 carve-out — global_position is a Godot built-in, not a SimNode field; on-tick invariant still holds via lint rule L1). Defaults `_scheduler` from `PathSchedulerService.scheduler` at `_ready`; tests inject MockPathScheduler. Tests in `tests/unit/test_movement_component.gd` (11 tests). |
| **CombatComponent** | [SIM 1.2.1 §1.6 / §3](SIMULATION_CONTRACT.md), [01_CORE_MECHANICS.md §6](../01_CORE_MECHANICS.md) | ✅ Built | — | gameplay-systems | Phase 2 session 1 wave 1A. `class_name CombatComponent` extending SimNode (path-string base) at `scripts/units/components/combat_component.gd`. Tunables (read from BalanceData by Unit `_apply_balance_data_defaults`): `attack_damage_x100: int` (fixed-point per Sim Contract §1.6 — eliminates float drift across long matches), `attack_speed_per_sec: float` (cooldown denominator), `attack_range: float` (XZ-distance, world units). Internal state: `_target_unit_id: int = -1` (sentinel for no target) and `_attack_cooldown_ticks: int = 0` (decremented per tick; 0 = ready to fire), both written via `_set_sim`. `set_target(unit_id_value: int)` is the public command interface (off-tick safe; resets cooldown to 0 so the first tick after engagement fires immediately — single-tick attack on engagement, matches RTS expectations). `_sim_tick(dt)` order: (1) decrement cooldown if > 0; (2) bail if no target (`-1` sentinel); (3) resolve target via `target_lookup_callable` injection seam (mirrors MovementComponent's `_scheduler` pattern from Sim Contract §4.1) — null/invalid lookup safe-clears `_target_unit_id` to `-1` and returns; (4) XZ-only squared-distance range check (matches `SpatialIndex` projection convention from Sim Contract §3); (5) cooldown gate; (6) fire — calls `target.get_health().take_damage_x100(attack_damage_x100, get_parent())` and resets `_attack_cooldown_ticks` to `roundi(SimClock.SIM_HZ / attack_speed_per_sec)` clamped to ≥ 1 (at 30 Hz: 1.0 atk/s = 30 ticks, 1.5 = 20, 2.0 = 15). Production `target_lookup_callable` walks the scene tree from the parent's root looking for a Node3D whose `unit_id` matches (O(N) per failed-lookup tick — acceptable at session-1 scale ≤15 units; LATER `UnitRegistry` autoload closes to O(1) when N>~100). Tests inject a closure mapping id → fixture Node3D (same shape as MovementComponent's `_scheduler` injection). Wired into `unit.tscn` as a sibling of MovementComponent; Unit `_apply_balance_data_defaults` reads `attack_damage_x100` (TYPE_INT), `attack_speed_per_sec` (TYPE_FLOAT/INT), `attack_range` (TYPE_FLOAT/INT). Kargar's `attack_damage_x100 = 0` makes the attack tick a no-op (workers ship the component but don't attack); the cooldown formula's safe-speed floor of 0.1 prevents misconfigured BalanceData from divide-by-zero. UnitState_Attacking (ai-engineer wave 1B) drives this component's `_sim_tick` directly via the unit's StateMachine.tick path until the dedicated CombatSystem phase coordinator lands (LATER). Tests in `tests/unit/test_combat_component.gd` (covers HP decrement, cooldown rapid-fire block, cooldown formula at 30Hz for 1.0/1.5/2.0 atk/s, range XZ-only with Y-offset target, target=-1 no-op, freed-target safe-clear, fixed-point exact arithmetic). |
| **EventBus.unit_died signal** | [SIM 1.2.0 §7](SIMULATION_CONTRACT.md), [02d_PHASE_2_KICKOFF.md §2 (3)](../02d_PHASE_2_KICKOFF.md) | ✅ Built | — | gameplay-systems | Phase 2 session 1 wave 1A. Typed signal `unit_died(unit_id: int, killer_unit_id: int, cause: StringName, position: Vector3)` declared on `EventBus` autoload. Added to `_SINK_SIGNALS` (telemetry sink contract §7) with the matching `_make_forwarder` arm. Producer: HealthComponent's `_apply_damage_x100` death emit (this same wave). Consumers (planned and incoming): FarrSystem worker-killed-idle drain (gameplay-systems wave 2A this session — listener checks `cause` + dying unit's `is_idle()` to drain -1 Farr per `01_CORE_MECHANICS.md` §4); Yadgar memorial site placement (Phase 5 — reads `position` to mark hero-death tiles); SelectionManager pruning (LATER — currently filters via `is_instance_valid` per emit). Listener-order discipline documented inline in event_bus.gd: handlers MUST NOT mutate other listeners' state-holders synchronously (cb95d09 class of bug); when in doubt, route through `apply_farr_change` or `call_deferred`. |
| **SelectableComponent** | [02b_PHASE_1_KICKOFF.md §2](../02b_PHASE_1_KICKOFF.md) | ✅ Built | — | engine-architect | Phase 1 session 1 wave 1. `class_name SelectableComponent` extending SimNode at `scripts/units/components/selectable_component.gd`. `is_selected: bool`; `select()` / `deselect()` toggle a placeholder MeshInstance3D selection ring (CylinderMesh disk in Iran's gold faction palette). Auto-creates the ring as a sibling under the parent unit if no `ring_path` export is supplied (added via `call_deferred` to avoid the "parent busy setting up children" error during the parent's `_ready`). Subscribes to `EventBus.selection_changed(selected_unit_ids: Array)`; component selects when its `unit_id` appears in the broadcast list, deselects otherwise. Tests in `tests/unit/test_selectable_component.gd` (11 tests). |
| **NavigationAgentPathScheduler** | [SIM 1.2.1 §4.3](SIMULATION_CONTRACT.md) | ✅ Built | — | engine-architect | Phase 1 session 1 wave 1. Production IPathScheduler at `scripts/navigation/navigation_agent_path_scheduler.gd`. Wraps `NavigationServer3D.map_get_path(map_rid, from, to, true)` synchronously — flat-terrain MVP path queries are fast enough that PENDING is skipped entirely (READY or FAILED returned at request time). Resolves the active navigation map from `Engine.get_main_loop().root.world_3d.navigation_map`; tests can override via `set_map_rid_override`. `cancel_repath` flips READY → CANCELLED; FAILED is sticky. `clear_log` resets bookkeeping for harness reuse. Wired as the default in `PathSchedulerService` via the autoload's `_ready`; `reset()` reverts to a fresh production instance (semantics change vs Phase 0 — see §6 v0.9.0). Tests in `tests/unit/test_navigation_agent_path_scheduler.gd` (8 tests; 2 pending in headless runners without a baked navmesh). |
| **UnitState_Idle** | [STATE 1.0.0 §3.4](STATE_MACHINE_CONTRACT.md) | ✅ Built | — | ai-engineer | Phase 1 session 1 wave 2. `class_name UnitState_Idle extends "res://scripts/core/state_machine/unit_state.gd"` at `scripts/units/states/unit_state_idle.gd`. id=`&"idle"`, priority=0, interrupt_level=NONE. enter() caches the parent unit's MeshInstance3D and resets its scale to neutral; _sim_tick writes a deterministic ±5%/1Hz sin-pulse driven off `SimClock.tick * SIM_DT` (replay-safe; renders the "alive but uncommitted" cue per CLAUDE.md placeholder visuals); exit() restores neutral scale. State is otherwise a true no-op — Contract §3.4 specifies that command-queue dispatch flows through `Unit.replace_command` / `append_command` calling `transition_to_next`, not Idle's own polling. Tests in `tests/unit/test_unit_states.gd` (4 dedicated + 1 base-class registration test). |
| **UnitState_Moving** | [STATE 1.0.0 §3.4 / §6.2](STATE_MACHINE_CONTRACT.md) | ✅ Built | — | ai-engineer | Phase 1 session 1 wave 2. `class_name UnitState_Moving extends "res://scripts/core/state_machine/unit_state.gd"` at `scripts/units/states/unit_state_moving.gd`. id=`&"moving"`, priority=10, interrupt_level=COMBAT. enter() reads target Vector3 from `ctx.current_command.payload.target` (populated by `StateMachine.transition_to_next` per the §3.4 worked example) and calls `unit.get_movement().request_repath(target)`; defensive bail to Idle when no current_command or no target. _sim_tick drives `MovementComponent._sim_tick(dt)` (the per-tick driver until the MovementSystem phase coordinator lands — see LATER items §6 v0.10.0); flips `_arrival_pending` latch on first READY observation; transitions via `transition_to_next` when path was loaded and waypoints consumed (`!is_moving` after latch). On FAILED/CANCELLED path resolution, push_warning then transition_to_next. exit() cancels in-flight repath via `_scheduler.cancel_repath(_request_id)` and resets `_request_id` to -1 so MovementComponent doesn't poll a cancelled request. Tests in `tests/unit/test_unit_states.gd` (8 dedicated + 1 integration cycle). |
| **UnitState_Attacking** | [STATE 1.0.0 §3.4 / §3.5](STATE_MACHINE_CONTRACT.md) | ✅ Built | — | ai-engineer | Phase 2 session 1 wave 1B. `class_name UnitState_Attacking extends "res://scripts/core/state_machine/unit_state.gd"` at `scripts/units/states/unit_state_attacking.gd`. id=`&"attacking"`, priority=20 (above Moving's 10 — attack preempts move), interrupt_level=NEVER (damage doesn't interrupt the attack itself; only player commands or death do, per Contract §3.5). enter() reads `target_unit_id: int` from `ctx.current_command.payload.target_unit_id`, resolves via scene-tree walk against `unit_id`, caches refs to target + CombatComponent + MovementComponent. Defensive bails to Idle on missing payload, missing target_unit_id, non-int payload, or unresolved target id (mirrors UnitState_Moving's no-target bail). _sim_tick: queue_freed target → `transition_to_next`; XZ-distance > attack_range → `MovementComponent.request_repath(target.global_position)` (re-issued each tick so a moving target stays tracked — fine at Phase 2's 15-unit cap; LATER throttle if N>50); ≤ attack_range → cancel in-flight repath + `combat.set_target(target.unit_id)` (CombatComponent fires its own attacks on its own _sim_tick per gameplay-systems wave 1A). exit() defensively calls `combat.set_target(-1)` and cancels in-flight repath via `_scheduler.cancel_repath`. Coordinates with gameplay-systems wave 1A's CombatComponent (`attack_range: float`, `set_target(uid: int)`); reads via `has_method` / property guards so it tolerates missing CombatComponent without crashing (push_warning surfaces missing wiring; default attack_range=1.5). Registered by Unit base class in `_ready` alongside Idle / Moving. **Wave 2B (2026-05-04):** click-handler enemy-right-click branch wired in (see Selection system row); the state itself is unchanged. Tests in `tests/unit/test_unit_state_attacking.gd` (11 tests). LATER: a `UnitRegistry` autoload (id → ref dict) closes target-lookup cost from O(N) tree-walk to O(1) when N>~100. |
| **UnitState_AttackMove** | [STATE 1.0.0 §3.4 / §3.5](STATE_MACHINE_CONTRACT.md), [02d_PHASE_2_KICKOFF.md §2 (4)](../02d_PHASE_2_KICKOFF.md) | ✅ Built | — | ai-engineer | Phase 2 session 1 wave 2B (commit `aa429ef`). `class_name UnitState_AttackMove extends "res://scripts/core/state_machine/unit_state.gd"` at `scripts/units/states/unit_state_attack_move.gd`. id=`&"attack_move"`, priority=15 (between Moving's 10 and Attacking's 20), interrupt_level=NEVER (committed engagement movement; same as Attacking). enter() reads `target: Vector3` from `ctx.current_command.payload.target` (same shape as UnitState_Moving) and calls `MovementComponent.request_repath(target)`. _sim_tick drives `_movement._sim_tick(dt)` (same per-tick driver pattern as Moving), latches `_arrival_pending` on first READY observation, transitions on FAILED/CANCELLED via push_warning. Engage-check per tick: `SpatialIndex.query_radius_team(self.position, Constants.ENGAGE_RADIUS, OPPOSING_TEAM)` (with OPPOSING_TEAM = `_opposing_team(self_team)` — Iran↔Turan binary, neutral/future factions get TEAM_ANY). On hit, takes the closest enemy by squared XZ distance, then **enqueues a resume-AttackMove command** via `Unit.append_command` to the BACK of the queue, **rents an Attack command** from `CommandPool` (kind=`COMMAND_ATTACK`, payload={target_unit_id: enemy.unit_id}) and pushes it to the FRONT via `command_queue.push_front` (the sanctioned AI-panic-insertion primitive per State Machine Contract §2.4 / §2.5), then calls `fsm.transition_to_next` — the standard dispatcher pops the front (Attack), stashes payload on `ctx.current_command`, and lands in Attacking. When Attacking exits (target dead → transition_to_next), the queue's head is the resume-AttackMove and standard dispatch re-enters this state. The **resume-after-kill** mechanic is the trickiest piece — it lives entirely in queue FIFO ordering, no special "remember target across states" plumbing required. exit() cancels in-flight repath. Registered by Unit base class in `_ready` alongside Idle / Moving / Attacking. Constants additions: `Constants.COMMAND_ATTACK_MOVE = &"attack_move"`, `Constants.STATE_ATTACK_MOVE = &"attack_move"`. `StateMachine._COMMAND_KIND_TO_STATE_ID` gains `&"attack_move" → &"attack_move"`. Tests in `tests/unit/test_unit_state_attack_move.gd` (11 tests). |
| **AttackMoveHandler (A+click input)** | [02d_PHASE_2_KICKOFF.md §2 (4)](../02d_PHASE_2_KICKOFF.md) | ✅ Built | — | ai-engineer | Phase 2 session 1 wave 2B (commit `aa429ef`). `scripts/input/attack_move_handler.gd` (Node, no class_name). Sibling of ClickHandler in `main.tscn`, placed BEFORE ClickHandler in document order so its `_unhandled_input` fires first and consumes the click via `set_input_as_handled`. State machine: `_attack_move_pending: bool` flips on `KEY_A` press (only if `SelectionManager.selection_size() > 0`); next left-click consumes the input as the attack-move target; right-click or Escape cancels. Public `process_attack_move_hit(hit: Dictionary)` is the test seam (same shape as ClickHandler.process_*_hit). On consumed click: builds an AttackMove command (kind=`Constants.COMMAND_ATTACK_MOVE`, payload={target: hit.position}) and dispatches per selected unit via `Unit.replace_command`. Pending flag clears regardless of dispatch success — A+click is a single-shot. Tests in `tests/unit/test_attack_move_handler.gd` (7 tests). LATER: cursor change while pending (Phase 5 polish), A on enemy unit (currently uses ground position; engage check catches the enemy en route via UnitState_AttackMove's per-tick spatial query). |
| **Kargar (worker) unit type** | [01_CORE_MECHANICS.md §6](../01_CORE_MECHANICS.md) | ✅ Built | — | gameplay-systems | Phase 1 session 1 wave 2. `class_name Kargar` extending `unit.gd` (path-string base to dodge class_name registry race) at `scripts/units/kargar.gd`. Sets `unit_type = &"kargar"` in BOTH `_init` (for headless `Kargar.new()` paths) AND `_ready` BEFORE `super._ready()` — Godot's scene-instantiation order assigns @export defaults from the .tscn AFTER `_init`, clobbering _init's write back to the unit.gd default. The `_ready` override fires before `Unit._apply_balance_data_defaults` reads unit_type to look up `BalanceData.units[&"kargar"]` (max_hp 60, move_speed 3.5). Scene at `scenes/units/kargar.tscn` inherits unit.tscn, overrides script + the MeshInstance3D's mesh (CylinderMesh, radius 0.35, height 0.7 — squat worker silhouette) and material (sandy-brown albedo `Color(0.65, 0.5, 0.3)`). All other unit composition (HealthComponent / MovementComponent / SelectableComponent / SpatialAgentComponent / CollisionShape3D) inherits unchanged from unit.tscn. Tests in `tests/unit/test_kargar.gd` (10 tests). |
| **Match start spawn (5 Kargar)** | [02b_PHASE_1_KICKOFF.md §2 #9](../02b_PHASE_1_KICKOFF.md) | ✅ Built | — | gameplay-systems | Phase 1 session 1 wave 2. `_spawn_starting_kargars()` in `scripts/main.gd` spawns 5 Kargar workers under `Main/World` at known XZ-plane positions (origin + 4 cardinal offsets at distance 3, all Y=0.5). All team Iran (`Constants.TEAM_IRAN`). Calls `Unit.reset_id_counter()` first (via path-string-preloaded `_UnitScript` ref, same registry-race dodge) so unit_ids deterministically run 1..5 across runs. Five rather than the canonical-spec three (01_CORE_MECHANICS.md §2 step 1) for wave-2 ergonomics — gives the SelectionManager more click-targets to test against; downgrades to 3 in Phase 3 when the resource economy ships (the canonical workforce). Tests in `tests/unit/test_match_start_spawn.gd` (6 tests). |
| **GroupMoveController** | [02_IMPLEMENTATION_PLAN.md Phase 1](../02_IMPLEMENTATION_PLAN.md) | ✅ Built | — | ai-engineer | Phase 1 session 2 wave 1B + 2C. `scripts/movement/group_move_controller.gd` (RefCounted, no class_name — registry-race pattern). Single static entry point: `dispatch_group_move(units: Array, target: Vector3) -> void`. Concentric-ring distribution: index 0 at center, indices 1..6 on a ring of radius `Constants.GROUP_MOVE_OFFSET_RADIUS = 2.0` (60° spacing), indices 7..18 on a 2R ring (30° spacing), etc. Radius is 8× the navmesh `cell_size` (0.25) to survive `NavigationServer3D` snap-to-poly without offsets collapsing. Determinism: pure trig over the unit's index, no RNG. Empty Array → no-op; single unit → identity (target verbatim, matches single-click); freed entries skipped via `is_instance_valid`; multi-unit dispatch issues `Constants.COMMAND_MOVE` per unit through `Unit.replace_command(kind, payload)` per State Machine Contract §2.5. **Wave 2C (2026-05-04):** wired into `click_handler.gd::process_right_click_hit` — both single and multi selections now route through `_GroupMove.dispatch_group_move(sel, target)`; the controller's identity fast path keeps single-click behavior bitwise-identical. Tests in `tests/unit/test_group_move_controller.gd` (7) + `tests/unit/test_click_handler.gd` (3 wire-up tests added in wave 2C). |
| **Selection system** | [02_IMPLEMENTATION_PLAN.md Phase 1](../02_IMPLEMENTATION_PLAN.md) | ✅ Built | — | ui-developer | Phase 1 session 1 wave 2. Single-click only this wave. `SelectionManager` autoload at `scripts/autoload/selection_manager.gd` registered after EventBus in project.godot — tracks `_selected_units` Array, exposes `select` / `select_only` / `deselect_all` / `add_to_selection` (Phase 1 session 2 hook) / `is_selected` / `selection_size` / `selected_units` accessor. Single-broadcast contract: every public state-mutation API emits `EventBus.selection_changed(selected_unit_ids: Array)` exactly once. `select` is idempotent (re-selecting same unit is a no-op, no signal). `reset()` clears state without emitting (test/teardown helper). Click input via `scripts/input/click_handler.gd` attached to `ClickHandler` node under `Main` in `main.tscn`. Routing logic exposed as `process_left_click_hit(hit)` / `process_right_click_hit(hit)` for testability; production `_unhandled_input` raycasts via `Camera3D.project_ray_origin/normal` + `direct_space_state.intersect_ray` then routes through the same public seam. Resolves Unit-shaped colliders by walking up the hit's collider ancestor chain (`replace_command` + `command_queue` duck-type). Hits on terrain colliders or empty space deselect; hits on unit-shaped colliders select_only. **Wave 2B (2026-05-04, commit `aa429ef`):** `process_right_click_hit` extended with team-aware unit-hit branch — when the hit collider is a unit AND the unit's team differs from the selected team (read off `sel[0].team`, single-team selection assumption), every selected friendly receives an Attack command (kind=`Constants.COMMAND_ATTACK`, payload={target_unit_id: int}) via `Unit.replace_command`. Same-team unit hits remain a no-op (friendly fire / follow / guard semantics are later phases — documented choice). Internal helper `_read_team(unit)` duck-types the `team` field. Tests in `tests/unit/test_selection_manager.gd` (16) + `tests/unit/test_click_handler.gd` (19 — +4 from wave 2B: enemy hit dispatches Attack, payload contains target_unit_id, multi-selection fan-out, friendly hit no-op). Box-select / Shift+click / Ctrl-groups / double-click select-all-of-type ship Phase 1 session 2. |
| **Box / drag selection** | [02c_PHASE_1_SESSION_2_KICKOFF.md §2 (1)](../02c_PHASE_1_SESSION_2_KICKOFF.md) | ✅ Built | — | ui-developer | Phase 1 session 2 wave 1A. Pure math at `scripts/input/box_select_math.gd` (RefCounted, no class_name — registry-race pattern): `rect_from_corners` (direction-agnostic Rect2 normalization), `is_past_dead_zone` (4px squared-distance threshold for click-vs-drag arbitration), `units_in_rect` (filters projected-position list, skips on_screen=false). Input handler at `scripts/input/box_select_handler.gd` (Node, not autoload — needs scene-bound Camera3D + Viewport): claims left-press first via `_unhandled_input`, on motion past dead-zone activates drag and shows the overlay, on release either box-selects (drag) or forwards the raycast result to `ClickHandler.process_left_click_hit` (click). Captures Shift state at press time (release-time Shift is ignored — typical RTS behavior). Iterates `get_tree().current_scene` for unit-shaped Node3Ds with `team == TEAM_IRAN`; projects each via `Camera3D.is_position_behind` → `unproject_position`; routes hits through `SelectionManager.add_to_selection` (Shift) or `deselect_all` + `add_to_selection` (no Shift). Drag overlay scene at `scenes/ui/drag_overlay.tscn` (CanvasLayer + Control + custom-drawing Rect Control); `mouse_filter = MOUSE_FILTER_IGNORE` enforced both in the .tscn AND defensively at runtime in `_ready` (session-1 regression pattern). Public test seams: `begin_press`, `update_motion`, `end_press`, `current_drag_rect`, `box_select_units(rect, units, project_callable, shift)` lets unit tests inject a projection helper without a real Camera3D. Tests in `tests/unit/test_box_select_math.gd` (16) + `tests/unit/test_box_select_handler.gd` (15). Wired into `main.tscn` as a sibling node after `ClickHandler` so `_unhandled_input` reaches it first. Click-handler core logic untouched per kickoff "do NOT modify" rule. |
| **Control groups (Ctrl+1..9 / 1..9 / double-tap center)** | [02c_PHASE_1_SESSION_2_KICKOFF.md §2 (2)](../02c_PHASE_1_SESSION_2_KICKOFF.md) | ✅ Built | — | ui-developer | Phase 1 session 2 wave 2A. `ControlGroups` autoload at `scripts/input/control_groups.gd`, registered in `project.godot` AFTER `SelectionManager` in `[autoload]` order. Internal state: `_groups: Dictionary[int, Array]` keyed 1..9 (0 explicitly excluded). `Ctrl+N` snapshots `SelectionManager.selected_units` into group N (replacing prior contents; lazy `is_instance_valid` filter on read). `N` alone replaces the selection with the live members of group N (no-op if the group is unbound or empty — recall is one-way restore, never clears prior selection). Double-tap detection uses `SimClock.tick` (NOT wall-clock — L5 lint determinism rule); `DOUBLE_TAP_TICKS = 10` (~333ms at 30Hz). Second `N` within window calls `CameraController.center_on(centroid)` where centroid = mean XZ of live members. CameraController gained a single-line public `center_on(world_pos: Vector3)` method (clamps to bounds, then `_apply_transforms`) — kickoff explicitly authorized this addition. `event.echo` filter suppresses OS auto-repeat (held key would otherwise spam recall every frame). Different keycodes between taps cancel the pending double-tap. Direct keycode reads (`event.keycode in KEY_1..KEY_9`, `event.ctrl_pressed`) — no InputMap actions needed (same pattern CameraController uses for WASD). Lazy-resolved CameraController via duck-typed scene walk (has `center_on` method AND `target_position` field) — no class import. `set_camera_target(stub)` test seam injects a RefCounted stub. `reset()` wipes all groups + double-tap state. Tests in `tests/unit/test_control_groups.gd` (23). |
| **Double-click select-of-type** | [02c_PHASE_1_SESSION_2_KICKOFF.md §2 (3)](../02c_PHASE_1_SESSION_2_KICKOFF.md) | ✅ Built | — | ui-developer | Phase 1 session 2 wave 2A. `DoubleClickSelect` Node at `scripts/input/double_click_select.gd`, instanced under `Main` in `main.tscn` (NOT autoload — needs scene-bound viewport for live Camera3D resolution). Detection strategy is **kickoff option (b)**: subscribes to `EventBus.selection_changed`. When the broadcast payload is exactly one unit, checks whether that same unit was the sole selection on a recent prior emission within `DOUBLE_CLICK_TICKS = 9` ticks (~300ms). If yes, replaces selection with all visible-on-screen units of the same `unit_type`. Multi-select (size > 1) and empty (deselect_all) payloads disarm the tracker — single-target selections are the only ones that arm or fire. Independent of input layer (ClickHandler, BoxSelectHandler, future ones) — observes the unified selection signal, not raw mouse events. Visible-on-screen filter: `Camera3D.is_position_behind` (z<0 reject) → `unproject_position` → viewport-rect comparison (1px tolerance). Public seam `select_visible_of_type(target_unit, candidates, project_callable)` lets unit tests inject a projector closure without a real Camera3D — same pattern as `BoxSelectHandler.box_select_units`. Target unit always part of the result (defensive against 1-frame projection edge case). `set_candidate_provider(Callable)` and `set_projector(Callable)` are the test seams; production walks `get_tree().current_scene` for unit-shaped Iran nodes (`unit_id` + `team` + `unit_type` + `Node3D`) and uses the live Camera3D. Tests in `tests/unit/test_double_click_select.gd` (14). |
| **Selected-unit panel** | [02c_PHASE_1_SESSION_2_KICKOFF.md §2 (6)](../02c_PHASE_1_SESSION_2_KICKOFF.md) | ✅ Built | — | ui-developer | Phase 1 session 2 wave 2B. `scenes/ui/selected_unit_panel.tscn` (CanvasLayer + Root Control anchored bottom-left at 250×120) + `scripts/ui/selected_unit_panel.gd`. Three sub-layouts toggled by `visible_state: StringName` (`&"empty"` / `&"single"` / `&"multi"`): EmptyLayout (centered "No selection" label), SingleLayout (50×50 portrait ColorRect colored by team via `_faction_color`, type Label via `tr("UNIT_<TYPE_UPPER>")`, HP bar bg+fill ColorRect pair where fill width = HP_BAR_WIDTH × hp_ratio, abilities row with 4 placeholder ColorRects), MultiLayout (4-column GridContainer of `Button` icons capped at `_MAX_ICONS = 12`; each icon is a Button with a faction-color child ColorRect swatch and a tooltip showing the type label). Subscribes to `EventBus.selection_changed` in `_ready`, disconnects in `_exit_tree` (same hygiene pattern as `farr_gauge.gd`). HP polled via `_process` against the displayed unit's `get_health().hp / max_hp` — no `unit_health_changed` signal exists (HealthComponent only emits `unit_health_zero` on death, the StateMachine death-preempt trigger), and adding a per-tick signal would emit write-shaped from `_sim_tick` for every damaged unit; polling one displayed unit is O(1) and stays off-tick per Sim Contract §1.5. `mouse_filter` discipline: containers MOUSE_FILTER_PASS (clicks pass through to world); icon Buttons MOUSE_FILTER_STOP (clicks land on the button); icon swatch ColorRects MOUSE_FILTER_IGNORE (button keeps the click). `handle_icon_click(unit_id)` (also bound to each icon Button's `pressed`) walks the live `SelectionManager.selected_units` list and calls `select_only` on the matching unit; defensive no-op if the unit's been freed since render. `refresh_displayed_unit()` is the public refresh seam — intentionally NOT named `apply_*` (lint rule L1 forbids `apply_*` in files with `_process`); falls back to `_render_empty()` when `_displayed_unit` becomes invalid. New i18n keys in `translations/strings.csv`: `UI_PANEL_NO_SELECTION`, `UI_PANEL_HP`, `UI_PANEL_ABILITIES`, `UNIT_KARGAR`. Wired into `main.tscn` as a sibling of `ResourceHUD`. Tests in `tests/unit/test_selected_unit_panel.gd` (12). |
| **Floating health bars** | [02d_PHASE_2_KICKOFF.md §2 (8)](../02d_PHASE_2_KICKOFF.md) | ✅ Built | — | ui-developer | Phase 2 session 1 wave 2C. `scenes/ui/health_bars_overlay.tscn` (Control, fullscreen anchor, mouse_filter=IGNORE) + `scripts/ui/health_bars_overlay.gd`. Single overlay walks every Iran/Turan unit each `_process` (kickoff option **(b)**, picked over per-unit Sprite3D-Viewport for scale-friendliness — one _draw call regardless of unit count), projects through the live Camera3D, and draws a horizontal bar per damaged on-screen unit. Color bands per kickoff §2 (8): HP > 70% → green, 30% ≤ x ≤ 70% → yellow, < 30% → red (inclusive at the yellow band's bounds — exactly 70% / 30% are yellow, avoids combat-flicker). Width by unit-size class: kargar 32 px, piyade / turan_piyade 48 px, default fallback medium. Hidden when HP is full and when off-screen. `compute_bar_entries(units, project_unit_callable)` is the public test seam. mouse_filter == MOUSE_FILTER_IGNORE in BOTH the .tscn AND `_ready` (Pitfall #1). Tests in `tests/unit/test_health_bars_overlay.gd` (20). |
| **F4 attack-range overlay** | [02d_PHASE_2_KICKOFF.md §2 (9)](../02d_PHASE_2_KICKOFF.md) | ✅ Built | — | ui-developer | Phase 2 session 1 wave 2C. `scenes/ui/overlays/attack_range_overlay.tscn` + `scripts/ui/overlays/attack_range_overlay.gd`. Control + screen-projected polyline (NOT Node3D + cylinder — `DebugOverlayManager.register_overlay` is statically typed against Control). `_CIRCLE_SAMPLES = 48`, gold semi-transparent, lifted Y=0.05 above ground. Subscribes to `EventBus.selection_changed` (Pitfall #4 audit point — handler is read-only). Registers under `Constants.OVERLAY_KEY_F4`; F4 toggles `Control.visible`. Defensive: skips units without CombatComponent, units with attack_range == 0 (Kargars), freed units. Tests in `tests/unit/test_attack_range_overlay.gd` (14). |
| **Session 2 integration tests (wave 3)** | [02c_PHASE_1_SESSION_2_KICKOFF.md §3](../02c_PHASE_1_SESSION_2_KICKOFF.md) | ✅ Built | — | qa-engineer | Phase 1 session 2 wave 3. 37 new integration tests across 5 files. `tests/integration/test_session_2_box_select.gd` (7 tests — drag covers all, covers none, Shift-additive, dead-zone arbitration, current_drag_rect state, no-press no-op). `tests/integration/test_session_2_control_groups.gd` (6 tests — bind/recall, freed-unit filter, unbound-group no-op, double-tap center, cross-key no-center, stale-tap no-center). `tests/integration/test_session_2_group_move.gd` (6 tests — distinct targets for 5 units, targets within ring radius, arrive at distinct positions via mock, single-unit identity, empty-array no-op, freed-unit skip). `tests/integration/test_session_2_farr_gauge.gd` (10 tests — signal updates target_farr, color bands at all thresholds, delta accumulates, seeded from FarrSystem at ready, off-tick signal no crash, successive signals update correctly, large negative clamps, signal before in-tree no crash). `tests/integration/test_session_2_panel.gd` (9 tests — empty state, single layout, 5-icon multi, icon narrows to single, unit death clears panel, freed icon is safe no-op, deselect-all returns empty, partial HP bar, MOUSE_FILTER_STOP regression guard). `tests/integration/test_session_2_smoke.gd` (2 tests — main.tscn spot-check for SelectedUnitPanel + DoubleClickSelect node coexistence; cross-feature round-trip: box-select 5 → group-move → bind/deselect/recall → Farr change → narrow single-select → panel STATE_SINGLE). Total test count: 498 → 535 (+37). All pass headless. 3 pre-existing PENDING unchanged (FarrSystem fallback path, navmesh-not-ready ×2). |
| **CombatSystem** | [02_IMPLEMENTATION_PLAN.md Phase 2](../02_IMPLEMENTATION_PLAN.md) | 📋 Planned | — | gameplay-systems | Phase 2 |
| **ResourceNode + MineNode + Mazra'eh** | [RNC 1.1.1](RESOURCE_NODE_CONTRACT.md) | 📋 Planned | — | world-builder + gameplay-systems | Phase 3 |
| **ResourceSystem** | [01_CORE_MECHANICS.md §3](../01_CORE_MECHANICS.md) | 📋 Planned | — | gameplay-systems | Phase 3 |
| **DummyAIController** | [02_IMPLEMENTATION_PLAN.md Phase 3](../02_IMPLEMENTATION_PLAN.md) | 📋 Planned | — | ai-engineer | Phase 3 — 100-line stub for solo testing |
| **Building system** | [01_CORE_MECHANICS.md §5](../01_CORE_MECHANICS.md) | 📋 Planned | — | gameplay-systems | Phase 3 |
| **Fog-of-war data layer** | [02_IMPLEMENTATION_PLAN.md Phase 3](../02_IMPLEMENTATION_PLAN.md) | 📋 Planned | — | world-builder | Phase 3 — boolean grid only |
| **TechSystem (Tier 1→2)** | [01_CORE_MECHANICS.md §8](../01_CORE_MECHANICS.md) | 📋 Planned | — | gameplay-systems | Phase 4 |
| **FarrSystem (full)** | [01_CORE_MECHANICS.md §4](../01_CORE_MECHANICS.md) | 📋 Planned | — | gameplay-systems | Phase 4 — fixed-point per SIM 1.2.0 §1.6 |
| **ProductionSystem** | [01_CORE_MECHANICS.md §7](../01_CORE_MECHANICS.md) | 📋 Planned | — | gameplay-systems | Phase 4 |
| **Rostam hero unit** | [01_CORE_MECHANICS.md §7](../01_CORE_MECHANICS.md) | 📋 Planned | — | gameplay-systems | Phase 5 |
| **KavehEventSystem** | [01_CORE_MECHANICS.md §9](../01_CORE_MECHANICS.md) | 📋 Planned | — | gameplay-systems | Phase 5 — seeded RNG |
| **TuranAIController (full)** | [01_CORE_MECHANICS.md §12](../01_CORE_MECHANICS.md) | 📋 Planned | — | ai-engineer | Phase 6 |
| **AI difficulty config** | [AI 1.1.0](AI_DIFFICULTY.md) | 📋 Planned | 1.1.0 | balance-engineer | Phase 6 wiring |
| **AI-vs-AI sim harness** | [TEST 1.4.0 §4](TESTING_CONTRACT.md) | 📋 Planned | — | qa-engineer + balance-engineer | Phase 6 |
| **MatchLogger** | [TEST 1.4.0 §2](TESTING_CONTRACT.md) | 📋 Planned | — | qa-engineer | Phase 6 |
| **Khorasan map** | [02_IMPLEMENTATION_PLAN.md Phase 7](../02_IMPLEMENTATION_PLAN.md) | 📋 Planned | — | world-builder | Phase 7 |
| **Fog-of-war rendering** | [02_IMPLEMENTATION_PLAN.md Phase 7](../02_IMPLEMENTATION_PLAN.md) | 📋 Planned | — | world-builder | Phase 7 |
| **Minimap** | [01_CORE_MECHANICS.md §11](../01_CORE_MECHANICS.md) | 📋 Planned | — | ui-developer | Phase 7 |

**Status legend:** 📋 Planned (target spec exists, no implementation) — 🟡 In progress — ✅ Built (passes tests, wired into game) — 🔄 Refactored (built and reworked since)

---

## 3. The Tick Pipeline

30 Hz fixed tick, seven phases per tick, in this order:

`input → ai → movement → spatial_rebuild → combat → farr → cleanup`

Full canonical specification — what each phase does, why it's in this order, the registration shape for phase coordinators, the `advance_ticks` test path — lives in [`SIMULATION_CONTRACT.md`](SIMULATION_CONTRACT.md) §1.2 and §2. Read that for any work that touches the pipeline.

---

## 4. Directory Map (with "why")

```
game/
├── project.godot              # Godot project config
├── scenes/                    # All .tscn scene files
│   ├── main.tscn              # Top-level scene (menu → match)
│   ├── match.tscn             # The match scene (the actual game)
│   ├── ui/                    # HUD, menus, overlays
│   ├── units/                 # Unit scene templates (one per type)
│   ├── buildings/             # Building scene templates
│   ├── maps/                  # Map scenes (Khorasan, Phase 7)
│   ├── camera/                # Camera rig
│   └── ai/                    # AI controller scenes
│
├── scripts/
│   ├── autoload/              # Singletons. Single source of truth for each.
│   │                          # SimClock, EventBus, GameState, GameRNG,
│   │                          # SpatialIndex, FarrSystem, TimeProvider,
│   │                          # Telemetry, Constants. Loaded at boot.
│   ├── core/                  # Base classes — SimNode, StateMachine, State,
│   │                          # UnitState. The shape every component conforms to.
│   ├── managers/              # GameManager, SelectionManager, CommandManager.
│   │                          # Coordinate cross-system flow without owning gameplay state.
│   ├── systems/               # Phase coordinators (MovementSystem, CombatSystem,
│   │                          # FarrSystem, ResourceSystem, TechSystem,
│   │                          # ProductionSystem, KavehEventSystem). Each owns its phase.
│   ├── units/                 # Unit scripts + components/ + states/.
│   │                          # Per-unit behavior, all extending SimNode/UnitState.
│   ├── buildings/             # Building scripts (Throne, Khaneh, Mazra'eh, ...)
│   ├── ai/                    # AI opponent scripts. DummyAIController in Phase 3,
│   │                          # full TuranController in Phase 6.
│   ├── ui/                    # All UI scripts. UI never writes sim state directly;
│   │                          # uses queue-then-drain per Sim Contract §1.5.
│   ├── camera/                # CameraController. Fixed isometric, no rotation.
│   ├── input/                 # Input handling, selection, hotkeys, command system.
│   ├── world/                 # Terrain, fog of war (data layer), resource nodes
│   │                          # base classes. Map metadata.
│   ├── navigation/            # GroupMoveController, path scheduler integration.
│   │                          # Wraps NavigationServer3D behind IPathScheduler.
│   └── constants.gd           # Structural constants and keys (rarely changes).
│                              # Tunable numbers live in BalanceData, not here.
│
├── data/
│   ├── balance.tres           # BalanceData Resource — every tunable number.
│   │                          # Owned by balance-engineer; edited freely.
│   └── telemetry/             # Match logs (NDJSON). Gitignored.
│
├── assets/                    # Placeholder shapes, fonts, terrain texture.
│                              # No real art until MVP loop is fun (per CLAUDE.md).
├── shaders/                   # Fog-of-war shader (Phase 7), selection highlight.
├── translations/              # Godot CSV translation files (en, fa).
├── docs/                      # Contracts and architecture documentation (this file).
└── tests/
    ├── unit/                  # GUT unit tests. State machines, math, FarrSystem.
    ├── integration/           # MatchHarness-driven tests. Full subsystem flows.
    ├── simulation/            # AI-vs-AI headless match runs (Phase 6).
    └── balance/               # Balance analysis scripts. Owned by balance-engineer.
```

---

## 5. Contract Index — what to read for each domain

| If you're working on... | Read first |
|-------------------------|------------|
| Anything that mutates gameplay state | [`SIMULATION_CONTRACT.md`](SIMULATION_CONTRACT.md) §1 (the rule) and §1.3 (SimNode) |
| State machines, command queues, interrupts | [`STATE_MACHINE_CONTRACT.md`](STATE_MACHINE_CONTRACT.md) |
| Tests, telemetry, BalanceData, mocks | [`TESTING_CONTRACT.md`](TESTING_CONTRACT.md) |
| Resource gathering, mines, farms, dropoff | [`RESOURCE_NODE_CONTRACT.md`](RESOURCE_NODE_CONTRACT.md) |
| AI difficulty values | [`AI_DIFFICULTY.md`](AI_DIFFICULTY.md) |
| Process — how syncs work, retro practice | [`STUDIO_PROCESS.md`](STUDIO_PROCESS.md) |
| Why we do anything | [`MANIFESTO.md`](../MANIFESTO.md) |
| What to build (the spec) | [`01_CORE_MECHANICS.md`](../01_CORE_MECHANICS.md) |
| The phase plan | [`02_IMPLEMENTATION_PLAN.md`](../02_IMPLEMENTATION_PLAN.md) |

---

## 6. Plan-vs-Reality Delta

*This section captures honest gaps between the target spec and what's actually been built. Expected to grow as implementation proceeds. Manifesto Principle 1 (Truth-Seeking) lives here most concretely.*

**At v0.1.0 (initial):** nothing built yet. The contracts describe target shape; this document indexes them. As subsystems land, this section records:
- Spec said X; we built Y; reason: Z
- Subsystem A took Phase N+1 (slipped one phase), reason: ...
- Contract V was bumped from 1.x.0 to 1.y.0 during implementation; key change: ...

### v0.2.0 — Phase 0 Session 1 (2026-05-01)

- **EventBus.connect_sink — implementation diverged from the contract sketch.** Sim Contract §7 originally sketched `func connect_sink(callable): for sig in _SINK_SIGNALS: get(sig).connect(func(...args): callable.call(sig, args))`. GDScript does **not** support varargs lambdas (`func(...args)` is not valid syntax), so the actual implementation registers one hand-rolled per-signal forwarder Callable per `(sink, signal)` pair, dispatching to `sink.call(signal_name, args_array)`. The public API (`connect_sink(sink: Callable)` / `disconnect_sink(sink: Callable)` taking a sink that receives `(StringName, Array)`) is unchanged from the contract; only the internal forwarder construction differs. Adding a new signal to `_SINK_SIGNALS` now requires adding a `match` arm in `EventBus._make_forwarder` for that signal's exact arity. This is captured in the source comments. **Resolved: SIM_CONTRACT 1.2.1 (2026-05-01)** patched §7 to replace the invalid sketch with the actual per-signal forwarder pattern.

- **SimClock test hooks added in addition to the contract surface.** `_test_run_tick()`, `_test_advance(delta)`, and `reset()` are present on the autoload to let GUT (and the future MatchHarness) drive the clock manually without running `_physics_process`. They share the exact `_run_tick()` body the live driver uses, satisfying Sim Contract §6.1's "no divergence between live and headless paths." The contract didn't originally enumerate these but their existence is implied by §6.1. **Resolved: SIM_CONTRACT 1.2.1 (2026-05-01)** patched §6.1 to enumerate the three test hooks as part of the contract surface for `MatchHarness` integration.

- **SimNode `_set_sim` off-tick assertion is exercised manually, not in GUT.** GDScript `assert()` halts the script in debug builds and compiles out in release; GUT cannot trap a fired assert in-process. The on-tick happy path is covered by `test_sim_node.gd`; the off-tick path is covered by the lint rule (Sim Contract §1.4) at the call sites that would trigger it, plus the runtime crash-with-stack-trace when a developer breaches the contract in debug. This matches contract intent — the assert is a tripwire, not something to trap. **Resolved: SIM_CONTRACT 1.2.1 (2026-05-01)** patched §1.3 with an explicit "enforcement-via-crash-in-debug, not enforcement-via-test" clarification so future devs don't try to GUT-trap the assert.

- **What did not ship in session 1 (deferred to session 2+ per `02a_PHASE_0_KICKOFF.md` §2):** `GameRNG`, `SpatialIndex`, `Constants` autoload, `BalanceData.tres`, `GameState`, `IPathScheduler` + `MockPathScheduler`, `MatchHarness`, `FarrSystem` skeleton, `DebugOverlayManager`, camera controller, terrain plane, translation infrastructure, HUD readouts, lint script, pre-commit hook. None of these are blocked; all sit on top of the foundations shipped here.

### v0.6.0 — Phase 0 Session 3 wave 2: Camera + DebugOverlayManager (2026-05-01)

- **Camera controller methods renamed `apply_pan` / `apply_zoom` → `pan_by` / `zoom_by`.** The `apply_*` prefix collides with lint rule L1 (Sim Contract §1.4): "gameplay mutation called from `_process`." The camera mutates camera-side state (target_position, zoom_distance) from `_process`, which is correct for UI/off-tick code per Sim Contract §1.5 — the lint just can't tell camera-side state from sim-side state. Renaming preserves both the lint's intent and the camera's intent. The contract surface is otherwise untouched. No contract change needed; the convention "`apply_*` means sim-side mutator" is an implicit lint-driven naming rule that this session made explicit by avoiding it for non-sim code.

- **Camera reads `Constants.MAP_SIZE_WORLD` defensively.** Camera and terrain ship in parallel; the camera lands first if its branch merges first. The controller does `Constants.get(&"MAP_SIZE_WORLD")` and falls back to its `@export var map_size: float = 256.0` if the constant isn't there yet. World-builder's session 3 wave 2 added the constant, so production now reads it cleanly; the fallback stays as a belt-and-braces guard. Not a contract gap — both 02_IMPLEMENTATION_PLAN.md Phase 0 and the kickoff doc explicitly note camera bounds and `MAP_SIZE_WORLD` co-evolve.

- **Camera rig is `Node3D`, not `Camera3D`.** The script extends `Node3D` and the rig scene contains a `Camera3D` *child*. This is the standard RTS-camera pattern: rig is the pivot, child is the lens. Code never rotates either — the rig has yaw -45° baked in `camera_rig.tscn` and the child has pitch -55° baked there. `_apply_transforms()` sets only `global_position` (rig-side pan) and the child camera's local Z (zoom distance). The "no rotation API" contract is enforced by the test `test_controller_does_not_expose_rotation_api`, which asserts the absence of `rotate_yaw`, `rotate_pitch`, `orbit`, `set_yaw`, `set_pitch` methods and `yaw` / `pitch` properties — a regression tripwire for any future drift toward rotation.

- **`DebugOverlayManager._unhandled_input` registered with `process_mode = ALWAYS`.** F1-F4 must work even when the game is paused (debug-overlay viability while inspecting state requires it). `process_mode = ALWAYS` is the Godot 4 way to keep input flowing through pause. No contract surface change.

- **DebugOverlayManager dispatch is keycode-based, not InputMap-based.** Phase 0 doesn't yet have an InputMap configured for F1-F4, and the lint/test gate already covers the dispatch correctness via `handle_function_key(keycode)`. When InputMap arrives (Phase 1+ for selection / commands), F1-F4 can move to `Input.is_action_just_pressed("debug_overlay_1")` etc. without touching the registry shape — only the dispatch path. Documented here so the Phase 1+ migration is a search-and-replace, not a redesign.

- **What did not ship in session 3 wave 2 (intentionally out of scope per kickoff):** Concrete debug overlays themselves (F1 pathfinding viz, F2 Farr log, F3 AI state, F4 attack ranges) — they ship with their owning systems in later phases per CLAUDE.md and `02a_PHASE_0_KICKOFF.md`. Selection system / HUD / Farr gauge / minimap / build menu / hero portrait / translation infrastructure — all deferred to Phase 1+ per the implementation plan. Camera not yet wired into `main.tscn` — engine-architect's session 4 task.

### v0.8.0 — Phase 0 Session 4 (2026-05-01)

- **`BalanceData.constants_version` is a manual stub, not auto-derived.** Testing Contract §2.3 specifies that `constants_version` becomes a content hash so balance log analysis can correlate sim outcomes to specific tuning states. Phase 0 ships with `constants_version = "2026-05-01-phase-0-init"` as a hand-written string. The `test_constants_version_is_set` test only checks non-empty, so it passes any value. **Recommended Phase 6 wiring (per balance-engineer):** compute the hash in the `MatchLogger` consumer at match-start log time via `FileAccess.get_file_as_string("res://data/balance.tres").hash()` and write it into the `match_start` NDJSON event — not in `BalanceData` itself. Keeps `BalanceData` clean and locates the attribution logic where it's consumed. Manual stamp is functional for Phase 0-5 (one balance state, no ambiguity); becomes load-bearing in Phase 6 when batch sims need to disambiguate outcomes across tuning states.

### v0.4.0 — Phase 0 Session 3 (2026-04-30)

- **`class_name State` removed; behavior preserved.** State Machine Contract §2.2 specifies `class_name State extends RefCounted`. In Godot 4.6.2 the global class_name registry is populated *after* GUT-collected test scripts parse — when an inner test class extends `state.gd` by path-string and that script declares `class_name State`, the resolver fails with "Could not resolve class 'State'". Removing the `class_name State` declaration eliminates the resolution race entirely; production code refers to the script via `preload("res://scripts/core/state_machine/state.gd")` or by path-string. Behavior is identical; only the global symbol-table registration is dropped. The contract surface (`State` as a *type*, `enter`/`_sim_tick`/`exit` as methods, `id`/`priority`/`interrupt_level` as fields) is preserved exactly. Same workaround applied transitively to several inner type annotations on `StateMachine` (`current: Variant`, `register(state: Object)`, `next_cmd = ...`) and on `CommandQueue` (`push(cmd: Object)`, `peek() -> Object`, `pop() -> Object`). `class_name UnitState`, `class_name Command`, `class_name CommandQueue`, `class_name StateMachine`, `class_name InterruptLevel`, `class_name SpatialAgentComponent`, and `class_name IPathScheduler` are retained — they don't trigger the same race because nothing extends them inline in test scripts. **No contract change required**: the State Machine Contract describes behavior; the class_name registration was an implementation detail.

- **`SpatialAgentComponent` references duck-typed in `SpatialIndex`.** `spatial_index.gd` is an autoload; autoloads parse before the class_name registry is fully populated for `class_name`-decorated component scripts. A typed `agent as SpatialAgentComponent` cast inside the index would fail at script-reload time. Resolution: `SpatialIndex` reads `agent.team` via `agent.get(&"team")` and calls `world_position()` via `agent.has_method(&"world_position")` + `call(...)`. Type safety is preserved by behavior — the only callers are `SpatialAgentComponent` instances. Same pattern in `CommandPool` (uses a `preload`'d Script ref to `command.gd` for `_CommandClass.new()`).

- **`SpatialIndex.query_nearest_n` does not auto-exclude the source.** Sim Contract §3.3 says "query_nearest_n excludes the source if the source is a registered agent (caller may pass its own node and not get itself)." The session-3 implementation does NOT apply that exclusion — there's no canonical "the source" parameter on the API, and inferring it from the point alone is unreliable (multiple agents can sit at the same XZ). Concrete behavior: callers who need self-exclusion filter the result, or pass a sentinel point offset from the source. A follow-up phase will pin down the exact API when the first concrete consumer (selection raycast or AoE caller) lands. **This is a documented short-term gap**, not a contract violation — Sim Contract §3 will be amended in 1.2.2 if the consumer needs the auto-exclude semantics.

- **EventBus gained `unit_health_zero(int)` and `unit_state_changed(int, StringName, StringName, int)`.** State Machine Contract §4.1 requires the first; §5.3 / §7.3 require the second. Both added to `_SINK_SIGNALS` with their forwarder match arms in `_make_forwarder`. `MatchLogger` (Phase 6) will pick them up with no further changes. Ship-blocked Phase 1+ producers (`HealthComponent`, `Unit`) — Phase 0 just declares them so the framework wires them ahead of consumers.

- **GameState `start_match` is idempotent on re-entry.** A second `start_match()` call while `match_phase == PLAYING` no-ops (with `push_warning`). Same shape on `end_match` — only valid in `PLAYING`. This avoids subtle determinism bugs where re-starting mid-match silently overwrites `match_start_tick`, breaking match-relative time reads.

- **What did not ship in session 3 (deferred to session 4+ per kickoff doc):** `MockPathScheduler`, `MatchHarness`, `BalanceData.tres`, `FarrSystem` skeleton, `DebugOverlayManager`, camera, terrain plane, translations, HUD readouts, concrete unit states. `GameRNG` is also still pending (kickoff lists it for "later sessions").

### v0.3.0 — Phase 0 Session 2 (2026-04-30)

- **L5 allowlist discrepancy between Sim Contract §1.4 and kickoff doc.** The Sim Contract §1.4 table lists `sim_clock.gd` as the L5 allowlist file. The kickoff doc (`02a_PHASE_0_KICKOFF.md`) and `time_provider.gd` source comments both name `time_provider.gd` as the correct allowlist. `time_provider.gd` is the file that actually calls `Time.get_ticks_msec()` — `sim_clock.gd` does not. Resolution: both files are allowlisted in `tools/lint_simulation.sh`. `sim_clock.gd` is defensively allowlisted in case a future clock implementation needs wall-clock drift correction. Contract §1.4 table has a minor error in the listed filename; no behavior change to the rule.

- **L3 comment-line false positive.** The bare-RNG pattern (`randi()` etc.) can match GDScript comment lines that happen to reference the function name in prose. Added a post-scan filter that strips matches where the code portion starts with `#` (a GDScript comment marker). The filter uses `rg -v ':[0-9]+:\s*#'` to drop comment lines from the match set. No impact on real violation detection.

- **`rg` shell function in Claude Code session.** During development, ripgrep is intercepted by a Claude Code shell function, causing `command -v rg` to succeed but the subprocess invocation to fail. The lint script runs correctly in a clean `bash --noprofile --norc` environment (which is what pre-commit hooks and CI use). Not a deployment issue; documented here for transparency.

- **What did not ship in session 2 (deferred per kickoff doc):** `MatchHarness`, `MockPathScheduler`, determinism regression test stub — all blocked on `IPathScheduler` interface which is engine-architect's session 3 deliverable. `GameRNG`, `SpatialIndex`, `Constants`, `GameState`, `StateMachine`, `DebugOverlayManager`, camera, terrain plane, translations, HUD readouts — all session 3+ per the original scope split.

---

### v0.5.0 — Phase 0 Session 3 wave 2 (2026-04-30) — world-builder

- **`PARSED_GEOMETRY_STATIC_COLLIDERS` chosen over `PARSED_GEOMETRY_MESH_INSTANCES`.** The kickoff doc and session spec are silent on which NavMesh geometry source to use. `MESH_INSTANCES` was the first choice because the MeshInstance3D (PlaneMesh) directly represents the visual ground. However, Godot 4's NavigationServer reports a significant performance warning when baking from mesh instances in headless/test contexts: it reads GPU vertex data back to CPU, which blocks rendering. `STATIC_COLLIDERS` uses the StaticBody3D + BoxShape3D (256×0.1×256) as the walkable source instead — all-CPU, no GPU readback. The baked result is functionally identical (flat 256×256 walkable surface) and the headless test suite runs cleanly. This choice aligns with production best practice for flat terrain: author a collision shape, bake from that. If terrain ever gains non-flat geometry (Phase 7 height maps), the geometry source may revisit `MESH_INSTANCES` or use a dedicated NavigationMesh resource baked in-editor. Implementation choice per CLAUDE.md "Escalation" rule #1 — no gameplay effect.

- **NavigationMesh pre-bake in `_ready` via `bake_navigation_mesh(false)`.** The spec says "synchronous bake at scene-load." In Godot 4, `bake_navigation_mesh(false)` is the synchronous path (false = on calling thread, not deferred to a background thread). The bake completes before `_ready` returns, so `get_navigation_map().is_valid()` is immediately true — verified by `test_navigation_region_has_valid_map_after_scene_load`. In editor workflows, the NavigationMesh resource would normally be pre-baked and saved to disk (avoiding runtime bake cost entirely). Phase 7 Khorasan map work should bake in-editor and serialize the result to avoid the startup bake. Noted here for Phase 7 world-builder.

- **CheckerBoard as `StandardMaterial3D` albedo_color, not a procedural shader.** The kickoff spec says "optional checkerboard reference texture (procedural via shader is fine, or a simple imported PNG)." A simple flat `StandardMaterial3D` with a sandy-ochre albedo was chosen over a procedural shader or PNG import — it's the smallest footprint for a placeholder (zero assets, zero shader compilation). The camera operator gets visual ground feedback from the flat color + the 256-unit extent cues. A real checkerboard (with a spatial shader or UV tiling) would require either an asset file or a ShaderMaterial sub-resource, neither of which adds functional value at Phase 0. If more granular visual scale reference is needed before Phase 7 art, a `CheckerTexture2D` can be assigned to `albedo_texture` in a one-line tscn edit. Implementation choice per CLAUDE.md "Escalation" rule #1.

- **`MAP_SIZE_WORLD` and `NAV_AGENT_RADIUS` added to `Constants` autoload.** The kickoff specified adding these if missing. Both were absent from the session-3 wave-1 Constants output. Added under a new `# === MAP CONFIGURATION ===` section with rationale comments. `MAP_SIZE_WORLD = 256.0` is the convergence-locked map size. `NAV_AGENT_RADIUS = 0.5` is the navmesh agent clearance. Both are structural (code shape would change if they moved), confirming their home in Constants vs BalanceData.

---

### v0.7.0 — Phase 0 Session 4 wave 1 (2026-05-01) — ui-developer

- **HUD reads via "declared property OR `Object.get_meta`" two-source pattern.** The kickoff doc says: "Coin: `GameState.player_resources.coin` ... if `GameState.player_resources` doesn't exist yet, defensively fall back to display 0." `GameState` is gameplay-systems' file (and they did NOT add `player_resources` / `player_pop` / `player_pop_cap` in session-4 wave-1 — only FarrSystem). The HUD's `_read_field_or_meta(node, field)` helper tries `node.get(field)` (the long-term shape — gameplay-systems' future ResourceSystem will declare a typed property) then falls back to `node.get_meta(field)` (the seam Phase-0 tests use to inject values, since GDScript `Object.set` is silently a no-op for undeclared properties). The runtime cost is one extra `has_meta` per read; the value is unchanged either way. When ResourceSystem ships in Phase 3 with `player_resources: Dictionary[StringName, int]` declared on GameState, the `node.get` path catches first and the meta path is dead code that can be removed in a follow-up. Documenting it here so the cleanup is intentional.

- **Real `FarrSystem.value_farr` is a getter-only computed property; tests mutate `_farr_x100` directly.** gameplay-systems shipped FarrSystem in this same wave with fixed-point integer storage per Sim Contract §1.6 — `_farr_x100: int` is the backing store, `value_farr: float` is computed via `get`. The HUD reads `value_farr` via `Object.get(&"value_farr")` which works fine for getter-only properties. But test code that wants to set the Farr value to a specific number can't write `farr.value_farr = 47.0` (no setter) and can't go through `apply_farr_change` (asserts `SimClock.is_ticking()`). Resolution in `test_resource_hud.gd::_inject_or_mutate_farr_system`: detect whether the live FarrSystem has the production `_farr_x100` field, write that integer-backed store directly with `roundi(target * 100.0)`, restore in `after_each`. For tests injecting a stand-in (no real autoload registered), write `value_farr` directly on a plain `Node`. Either path resolves through the same HUD read code.

- **HUD wired into `main.tscn` first.** The kickoff doc warned: "if other wave-1 agents (especially gameplay-systems) need to also touch `scenes/main.tscn` to wire FarrSystem-related UI, coordinate via DM or land your wiring last." gameplay-systems' FarrSystem ships as an autoload — no scene wiring — so `main.tscn` was clear for the HUD's instance. Existing `StatusLabel` was offset down 40px (top: 16 → 56) so it doesn't overlap the new HUD's top-left `MarginContainer`. The status label remains for SimClock.tick visibility through Phase 1.

- **What did not ship in session 4 wave 1 (intentionally out of scope per kickoff):** Circular Farr gauge with color thresholds + floating change numbers (Phase 1+ per `01_CORE_MECHANICS.md` §4.4 and §11). F2 Farr-change-log overlay (Phase 4 with the full FarrSystem). Persian translation content (Tier 2 — infrastructure ships now, content later). HUD styling, fonts, art (Phase 1+). Selection system, build menu, minimap, hero portrait, tier indicator (Phase 1+). The kickoff doc explicitly listed each of these as out of scope.

---

### v0.9.0 — Phase 1 Session 1 wave 1 (2026-05-01) — engine-architect

- **`PathSchedulerService.reset()` semantics changed.** Phase 0 had `reset()` clear `scheduler` to `null`; Phase 1 reverts to a fresh production `NavigationAgentPathScheduler` instance. Rationale: Phase 1 wires production-by-default at autoload `_ready`, so "reset to pristine" naturally means "reset to production." Tests that wanted the null state for negative-path coverage now write `set_scheduler(null)` directly. `MatchHarness.teardown()` inherits the new behavior — its assertion in `tests/unit/test_match_harness.gd::test_teardown_resets_path_scheduler_to_production_default` was updated to match. No contract change in `SIMULATION_CONTRACT.md` — §4.3 only required *some* default scheduler; the reset semantics were always implementation detail.

- **Unit extends CharacterBody3D, not SimNode directly.** State Machine Contract §5.1 sketches `class_name Unit extends SimNode`. CharacterBody3D is a Node3D subclass needed for: (1) per-unit collision (already required by the unit.tscn template's CollisionShape3D); (2) future formation-collision in Phase 1 session 2's GroupMoveController; (3) the Sim Contract §4.1 `Node3D.global_position` carve-out is meaningless if Unit isn't a Node3D. The SimNode discipline is preserved by *composition*: HealthComponent, MovementComponent, SelectableComponent, SpatialAgentComponent each extend SimNode and own their own state. Unit is composition glue. The `_set_sim` chokepoint applies where state actually lives, not on the Unit. This matches Sim Contract §1.3's "self-only mutation rule" — components own their state, Unit doesn't reach in.

- **SelectableComponent ring is added via `call_deferred`.** When the unit.tscn template instantiates, the parent CharacterBody3D's `_ready` runs after each child's own `_ready` chain. SelectableComponent's `_ready` tries to add a sibling MeshInstance3D ring to the parent — but Godot 4 forbids tree mutation while a parent is "busy setting up children" (the error fires regardless of which child triggers it). Using `parent.add_child.call_deferred(ring)` defers the addition to the next frame, after the parent has finished its setup. Tests that inspect `_ring.get_parent()` immediately after spawn must `await get_tree().process_frame` first; documented in the test file. No contract change.

- **`EventBus.selection_changed(selected_unit_ids: Array)`.** Declared on EventBus this wave. NOT added to `_SINK_SIGNALS` because it's read-shaped (UI driven, not gameplay-state mutation). The L2 lint allowlist (`tools/lint_simulation.sh`) already includes this signal name as exempt from the no-emit-from-`_process` rule, anticipating UI emission. Constants already had `Constants.SIGNAL_SELECTION_CHANGED` declared from Phase 0 session 3 forward-reference work.

- **`HealthComponent.take_damage` and `heal` ignore non-positive amounts silently.** Negative `take_damage` does NOT route through `heal` (no double-meaning); negative `heal` does NOT route through `take_damage`. Each method has one intent. Callers pass the right method for the right intent. This avoids bugs where a buff that "heals -5 hp on expiry" silently becomes damage with no audit trail. Documented in source comments.

- **`NavigationAgentPathScheduler` skips PENDING entirely.** Sim Contract §4.2 sketches PathState as `PENDING → READY | FAILED | CANCELLED`. The synchronous `NavigationServer3D.map_get_path` call resolves at request time, so the production scheduler's request_repath returns READY (or FAILED if no path exists) immediately — `poll_path` never returns PENDING for a valid request_id from this scheduler. This is consistent with the contract's "result lands on `requested_tick + 1` *or later*" — "or later" includes "at the requested tick itself." Concrete consumers (MovementComponent) handle both paths uniformly: poll, branch on state, proceed.

- **What did not ship in wave 1** (intentionally out of scope per `02b_PHASE_1_KICKOFF.md` and the wave-1 task brief): Concrete `Kargar` unit type (gameplay-systems wave 2). Spawning workers in main.gd (gameplay-systems wave 2). `UnitState_Idle` and `UnitState_Moving` (ai-engineer wave 2). SelectionManager + click-to-select raycast (ui-developer wave 2). Right-click-to-move command building (ui-developer + ai-engineer wave 2). Full integration test of click-and-move flow (qa-engineer wave 3). Box-select / control groups / multi-select (Phase 1 session 2). Combat / attack-move (Phase 2). Multiple unit types (Phase 1 session 2 onward). Building system (Phase 3). The 8 deliverable boxes from the kickoff doc are: Unit base scene+class ✅, HealthComponent ✅, MovementComponent ✅, SelectableComponent ✅, NavigationAgentPathScheduler ✅, EventBus.selection_changed ✅, ARCHITECTURE.md update ✅, BUILD_LOG.md entry ✅.

---

### v0.10.0 — Phase 1 Session 1 wave 2 (2026-05-01) — ui-developer (selection)

- **`SelectionManager` autoload `select` is idempotent (no signal re-emission); `deselect_all` always emits even when the set was empty.** Phase 1 session 1 wave 2 kickoff didn't pin down the signal-emit semantics for the corner cases. Choices made: (1) `select(unit)` on an already-selected unit is a no-op — both no list mutation AND no signal emission. Rationale: rapid click-flurries on the same unit should not flood `selection_changed` consumers (HUD, future control-group manager, F3 overlay, telemetry). Idempotency at the *signal* layer is what consumers actually want — they re-render only on real state changes. (2) `deselect_all()` always emits, even when the selection was already empty. Rationale: defensive consumer-rerender on the "nothing selected" state. The set-state check is too cheap to be worth optimizing, and consumers that missed an earlier emission for any reason re-sync. Documented in source. Tests pin both rules (`test_select_is_idempotent`, `test_deselect_all_on_empty_set_still_emits`).

- **`SelectionManager.reset()` does NOT emit a final empty broadcast.** Mirrors the `SimClock.reset` / `GameState.reset` / `FarrSystem.reset` / `SpatialIndex.reset` pattern: reset is the test-fixture "wipe state without side effects" path. Production code that wants the empty broadcast calls `deselect_all()`. Test counts that subscribe to `selection_changed` would otherwise observe phantom emissions from `before_each`/`after_each` and have to compensate. Documented in source contract for the method. Test `test_reset_clears_selection_without_emitting` asserts the no-emit rule.

- **`select_only` preserves the target's ring instead of flickering through deselect→select.** When the target unit is already in the selection set (e.g., user clicks an already-selected single unit), the implementation marks `preserved = true` and skips both the `deselect` call on the existing entry and the redundant `select` on the same target. Without this, the target's SelectableComponent would flash off-then-on between calls. Less important for the placeholder ring than it will be for the eventual animated selection cue, but cheap to get right now.

- **`SelectionManager._selected_units` typed as untyped Array, not `Array[Unit]`.** Same project-wide class_name registry race documented in §6 v0.4.0 / v0.9.0. The autoload parses before the global class_name registry populates, so a typed `Array[Unit]` annotation is brittle. Plain `Array` plus `is_instance_valid()` filtering at every read point is the established pattern. The public `selected_units: Array` accessor returns a fresh copy per read (cheap; selections are tiny) so external mutators can't poison the internal store.

- **`ClickHandler` exposes `process_left_click_hit(hit)` / `process_right_click_hit(hit)` as the testable seam.** The kickoff doc described `_unhandled_input` driving the raycast then routing — but raycasting requires a real `Camera3D` + populated physics world, which neither GUT unit tests nor the headless test runner can easily provide. Refactor: the production path stays `_unhandled_input → _handle_left_click(screen_pos) → _raycast_from_screen(screen_pos) → process_left_click_hit(hit_dict)`. Tests inject synthetic hit dictionaries directly into `process_*_click_hit()` and verify the routing decision (selected_only vs. deselect_all, replace_command vs no-op). The end-to-end raycast wiring is exercised by the scene-level smoke test (qa-engineer wave 3 integration test). This is the same shape `CameraController` uses (`pan_by` / `zoom_by` / `clamp_to_bounds` are pure-ish methods; `_process` and `_unhandled_input` are thin shells). No contract change; just the standard "expose the inner function" testability pattern.

- **Right-click on a unit (the future attack-move target) is a no-op in wave 2, NOT a fallback to "move to that unit's position."** When the player's right-click raycast hits a Unit-shaped collider, the click handler returns early instead of generating a Move command targeting the unit's position. Phase 2 will route this through the future `attack_move` command (target a unit, not a position). Treating it as a position-move now would generate a misleading user model ("right-click on enemy = move to enemy" rather than "right-click on enemy = attack enemy"). Tests pin the no-op (`test_right_click_on_unit_is_noop_in_wave2`).

- **`ClickHandler._is_unit_shaped` duck-types the Unit detection** (`replace_command` method + `command_queue` field). Avoids the typed `is Unit` cast for the same class_name registry reason; concrete Unit subclasses (Kargar, gameplay-systems wave 2) inherit both, so the check carries forward without modification. The resolver walks up the hit's collider ancestor chain so future scene structures that nest collision under a child node (e.g., a Hero unit with multiple sub-meshes) still resolve correctly. Tested by `test_resolve_unit_walks_up_collider_ancestor_chain`.

- **`SelectionManager` registered as autoload AFTER EventBus, BEFORE the position where ai-engineer's wave-2 states would slot in.** The autoload order in `project.godot` is: TimeProvider → EventBus → Constants → SimClock → GameState → SpatialIndex → PathSchedulerService → CommandPool → DebugOverlayManager → FarrSystem → **SelectionManager**. The kickoff said "after EventBus" which we read as "anywhere after EventBus, before any consumer." SelectionManager has no autoload-time dependencies beyond EventBus (which it emits on) and uses lazy registration — no `_ready` boot sequence beyond the implicit Node.

- **What did not ship in wave 2** (intentionally out of scope per `02b_PHASE_1_KICKOFF.md` and the wave-2 brief): Box / drag selection — Phase 1 session 2. Shift+click add-to-selection wiring — `add_to_selection` API exists but no input is wired (Phase 1 session 2). Ctrl+1-9 control groups — Phase 1 session 2. Double-click select-all-of-type — Phase 1 session 2. Selected unit panel (bottom-left detail view) — Phase 1 session 2. Attack-move (A + click) — Phase 2. Hover info / unit tooltips — Phase 2+. Cursor changes per context (default / select / move / attack) — later. The 7 deliverable boxes from the wave-2 brief: SelectionManager autoload ✅, click-to-select input ✅, right-click-to-move command building ✅, selection ring visual feedback (delegated to engine-architect's wave-1 SelectableComponent) ✅, 10-15 tests ✅ (29 actually shipped: 16 SelectionManager + 13 ClickHandler), ARCHITECTURE.md update ✅ (this entry), BUILD_LOG.md wave-2 entry ✅.

---

### v0.12.0 — Phase 1 Session 1 wave 2 (2026-05-01) — gameplay-systems (Kargar + spawn)

- **Kargar uses `extends "res://scripts/units/unit.gd"` (path-string) plus `class_name Kargar`.** Same project-wide class_name registry race documented in §6 v0.4.0 / v0.9.0. Initial attempt was `class_name Kargar extends Unit` — the parser hit "Could not find base class 'Unit'" because GUT's test_collector loads scripts in an order where the Unit class_name isn't yet registered when kargar.gd parses. Switching to the path-string base resolves the parse-time error; the `class_name Kargar` declaration after the doc comment block still registers the type for runtime `is Kargar` checks at call sites where the registry has settled. Established convention from the components in `scripts/units/components/` — the Kargar follows the same shape.

- **Kargar sets `unit_type = &"kargar"` in BOTH `_init` AND `_ready` (before `super._ready()`).** Discovered through TDD: the bare-construction test (`Kargar.new()`) passed but the scene-instantiation test failed with `unit_type = StringName("")`. Root cause: Godot's scene-instantiation order is (1) script `_init`, (2) engine assigns @export defaults from the `.tscn`, (3) script `_ready`. Step 2 overwrites `unit_type` (declared as `@export var unit_type: StringName = &""` on the parent unit.gd) with the empty default from the inherited `unit.tscn`, clobbering whatever `_init` wrote. Setting `unit_type = UNIT_TYPE_KARGAR` again at the top of `_ready` (before `super._ready()` reads it via `_apply_balance_data_defaults`) makes the override stick for scene-loaded instances. The `_init` write is retained for the headless `Kargar.new()` path (no scene → step 2 skipped). Documented in source.

- **Spawning lives in `main.gd`, not a new `MatchSetup` script.** The kickoff doc gave discretion ("main.gd or a new `MatchSetup` script attached to a node"). Choice: keep it in `main.gd` for now — the spawn logic is ~30 lines (5 hardcoded positions + a loop), well under the threshold where extracting helps. Single source-of-truth for "where do units start at scene load" stays at `main.gd::_spawn_starting_kargars()`. If/when spawn logic grows past ~50 lines (multiple unit types, randomized positions, AI starting army, etc. — Phase 3+), extract to `scripts/match_setup.gd` attached as a child of Main in main.tscn. Per CLAUDE.md "Escalation" rule #1: implementation choice, prefer simplest option, document briefly.

- **Five starting Kargars vs. the canonical three.** 01_CORE_MECHANICS.md §2 step 1 specifies "each player starts with one Throne building, three workers, and the hero." Wave 2 ships 5 — purely for click-target ergonomics during ui-developer's SelectionManager testing (more units = more visible click events when stress-testing the selection ring + right-click-move flow). The number lives in `_KARGAR_SPAWN_POSITIONS` (a const Array[Vector3] in main.gd), NOT in BalanceData or Constants — it's a UX-of-testing knob, not a balance value, and it disappears when the canonical 3-worker start lands with the resource economy in Phase 3. Per CLAUDE.md "Escalation" rule #1 — implementation choice with no gameplay effect (no economy yet, the workers can't gather), no QUESTIONS_FOR_DESIGN.md entry needed. Will be reconsidered in Phase 3 when the economy makes the count load-bearing.

- **`main.gd` calls `Unit.reset_id_counter()` via path-string-preloaded `_UnitScript` ref.** Same registry-race reason kargar.gd uses path-string extends. main.gd is the root scene script — it's parsed at scene-boot, the same window where `Unit` may not yet be in the global class_name registry. Reflective `_UnitScript.call(&"reset_id_counter")` works regardless. The reset is needed so unit_ids deterministically run 1..5 across runs (replay-diff cleanliness, snapshot test stability) — `MatchHarness.start_match` already calls this internally, but the live boot path doesn't go through MatchHarness, so the call is duplicated here. When a future `MatchSetup` script lands it'll absorb this responsibility.

- **kargar.tscn material is `Color(0.65, 0.5, 0.3)` (sandy brown), distinct from unit.tscn's `Color(0.3, 0.5, 0.7)` (blue-grey).** Color choice driven by the Shahnameh-rooted "worker of the soil" framing (01_CORE_MECHANICS.md §6 + the recurring "people of the land" theme in the source material). At placeholder fidelity the only requirement is "visually distinct from other unit silhouettes"; the brown does that against the future blue-grey infantry / red/orange Turan units / glowing Rostam. Test `test_kargar_material_is_brown_not_blue_grey` asserts the channel-by-channel signature (red high, blue low) without pinning exact values, so future tweaks can tune within the brown family without test churn. Per CLAUDE.md placeholder policy — colored shapes only, no real art until MVP loop is fun.

- **What did not ship in wave 2** (intentionally out of scope per the kickoff prompt): Other unit types (Piyade, Kamandar, Savar, Asb-savar, Rostam) — Phase 1 session 2 onward. Production buildings, costs spent on spawn — Phase 3 (resource economy). Combat behavior, attack range, damage — Phase 2. Worker gathering / construction / repair — Phase 3 (resource node interactions). Dying state visuals (death animation, fade out) — Phase 2 with combat. UnitState_Idle / UnitState_Moving — ai-engineer wave 2 (separately shipped this same wave). The 4 deliverable boxes from the wave-2 brief: Kargar class+scene ✅, 5-Kargar spawn in main.gd ✅, ARCHITECTURE.md §2 update (Kargar + Match start spawn rows) ✅, BUILD_LOG.md wave-2 entry ✅.

---

### v0.13.0 — Phase 1 Session 1 wave 2 (2026-05-01) — ai-engineer (Idle / Moving states)

- **`UnitState_Idle` and `UnitState_Moving` registered by the `Unit` base class, not by concrete subclasses.** State Machine Contract §3.1 says "Every State instance for a unit is allocated at spawn (in the unit's `_ready`)." The kickoff doc proposed concrete `Kargar`-style subclasses register their own states; the base class registers Idle and Moving (the universally-useful pair) so every concrete unit type ships with a working FSM out of the box. Subclasses that want extra states (Gathering for workers, Attacking for combatants, etc.) call `fsm.register(...)` BEFORE chaining to `super._ready()`. The base's `_ready` then idempotently registers Idle/Moving (only if not already present) and calls `init(&"idle")` only if `current` is still null. Rationale: this minimizes the boilerplate concrete subclasses repeat (gameplay-systems' `Kargar` doesn't have to know about Idle/Moving) and matches engine-architect's wave-1 retro note "If [Unit] does [register stub states], your job is just to make the actual UnitState subclasses exist and be findable."

- **`UnitState_Idle` / `UnitState_Moving` extend `unit_state.gd` by path-string, NOT by `class_name UnitState`.** Same project-wide class_name registry race (§6 v0.4.0, v0.9.0). Initial `extends UnitState` triggered the cascade where unit.gd's `class_name Unit` failed to register, surfacing as `Invalid call. Nonexistent function 'reset_id_counter (via call)' in base 'GDScript'` and `Invalid assignment of property or key 'unit_type' on a base object of type 'CharacterBody3D'` in test fixtures. Path-string base + `class_name UnitState_Idle` / `class_name UnitState_Moving` retained for typed external references. Unit also references the state scripts via path-string preload (`const _UnitStateIdleScript: Script = preload(...)`) instead of by class_name to dodge the same race when unit.gd is loaded in test contexts.

- **`StateMachine.transition_to_next` stashes the dispatched Command's kind/payload on `ctx.current_command` before returning the Command to the pool.** State Machine Contract §3.4 explicitly leaves this open: "the popped command is returned, so concrete states must read their target from elsewhere (e.g., ctx.current_command — to be defined when concrete states ship)." Wave 2 ships the definition: `Unit.current_command: Dictionary = {}` with shape `{ "kind": StringName, "payload": Dictionary }`. The `StateMachine.transition_to_next` writes a defensive `payload.duplicate()` copy (so pool re-rent doesn't race), then returns the Command to the pool, then transitions. `_clear_current_command()` runs on the empty-queue → Idle path so a stale payload never leaks into Idle's tick. UnitState_Moving's `enter` reads `ctx.current_command.payload.target` — the StateMachine framework remains the single dispatcher; states read off the stashed dictionary.

- **`UnitState_Moving._sim_tick` drives `MovementComponent._sim_tick(dt)` directly.** Until the MovementSystem phase coordinator lands (a LATER item per wave-1 retro: "MovementSystem phase coordinator wiring"), Moving IS the per-tick driver for the MovementComponent. When the coordinator ships, it iterates registered MovementComponents during the `movement` phase and calls their `_sim_tick`; Moving's `_sim_tick` drops the explicit drive-call and just polls `path_state` / `is_moving`. This is a one-line refactor and is documented in both the source code (`unit_state_moving.gd` block comment above `_sim_tick`) and in the BUILD_LOG wave-2 retro under LATER items.

- **Arrival detection uses an `_arrival_pending` latch flipped on first READY observation, NOT on first `is_moving == true` observation.** Initial design used `is_moving == true` as the latch trigger, but in single-tick-arrival cases (huge `move_speed`, short distance, e.g. the test scenario `move_speed=100, target=(1,0,0)`) the MovementComponent ingests waypoints, advances position, and sets `_waypoint_index = waypoints.size()` all within a single `_sim_tick` call — so Moving never observes `is_moving == true`. Switched to `path_state == READY` as the latch trigger. The latch closes the "have a path" race correctly: Moving's `enter()` issues `request_repath` but the scheduler is non-blocking, so on the entry tick `is_moving` is false because no waypoints exist yet. Without the latch we'd transition back to Idle on the entry tick, never having moved. The READY-as-latch trigger handles both the single-tick and multi-tick arrival cases uniformly. Tests `test_moving_transitions_to_idle_on_arrival` and `test_full_idle_moving_idle_cycle` pin both shapes.

- **`UnitState_Moving.exit` cancels the in-flight repath request even though `MovementComponent.request_repath` would also cancel it on a subsequent call.** Contract §3.5 specifies states must own their own teardown. The next state may not be another Moving (could be Attacking, Constructing, or Idle), and those states won't re-issue a repath that would shadow ours. Explicit cancel in exit is cheap and matches the contract's intent. Implementation: `_movement._scheduler.cancel_repath(_movement._request_id)` then `_movement._request_id = -1` so MovementComponent's next `_sim_tick` doesn't poll the cancelled request.

- **`UnitState_Idle._sim_tick` writes `MeshInstance3D.scale` directly inside the sim tick.** Sim Contract §1.5 allows visual side-effects in `_process` (UI off-tick), but States are RefCounted and have no `_process`. The pulse runs from `_sim_tick` because that's the only hook a State has, and `MeshInstance3D.scale` is render-only — not a SimNode field, doesn't trigger the on-tick assert (we don't route through `_set_sim`), doesn't feed into combat math, pathfinding, or any sim-state read. This is the same carve-out MovementComponent uses for `Node3D.global_position` (Sim Contract §4.1). The pulse is driven by `SimClock.tick * SIM_DT` — deterministic, replay-safe, and removes a wall-clock divergence vector from headless determinism tests even though we don't strictly need determinism for visual decoration.

- **`UnitState_Moving.interrupt_level = COMBAT`, NOT `NEVER`.** Contract §2.1 spec: NONE = damage doesn't interrupt, COMBAT = damage interrupts, NEVER = damage cannot interrupt. "Non-combat movement" sits in the COMBAT bucket per the contract's own examples. Phase 2's combat system can flip this to NONE for "casual" movement (e.g., hero traveling between bases) if combat balance demands it; one-line edit. Player `replace_command` always wins regardless of `interrupt_level` (Contract §3.5).

- **What did not ship in wave 2** (intentionally out of scope per kickoff): Concrete additional states (Attacking, Gathering, Constructing, Casting, Dying) — Phase 2+ when their owning systems exist. GroupMoveController / formation movement — Phase 1 session 2 (planned row in §2 unchanged). MovementSystem phase coordinator — LATER item, see below. F3 state-machine debug overlay — concrete overlays land WITH their owning systems per kickoff doc rule; visual ID readout for selected unit lands when the selected-unit panel ships (Phase 1 session 2). The 6 deliverable boxes from the wave-2 brief: UnitState_Idle ✅, UnitState_Moving ✅, Wire Idle/Moving in Unit base ✅, 12-18 tests ✅ (13 shipped: 4 Idle + 8 Moving + 1 base-class registration test), ARCHITECTURE.md update ✅ (this entry), BUILD_LOG.md wave-2 entry ✅.

- **LATER items surfaced in wave 2:**
  1. **MovementSystem phase coordinator.** Currently Moving's `_sim_tick` drives `MovementComponent._sim_tick`. When 50+ units move simultaneously this becomes "every Moving state's _sim_tick calls a MovementComponent's _sim_tick" — fine for MVP but a phase coordinator that subscribes to `EventBus.sim_phase(&"movement", ...)` and iterates registered MovementComponents in one batch is the long-term shape (cache-friendlier, removes the per-state-instance drive call). Estimated 1 small wave; not blocking.
  2. **Per-unit current_command lifetime.** `Unit.current_command` is a Dictionary written by StateMachine and read by states. It outlives the dispatched Command (which is returned to the pool). When Phase 2's Attacking state lands, it'll need `current_command.payload.target_unit: Node` — a reference to a Node, which can become invalid mid-state. Either Attacking handles the validity check itself (preferred — same pattern as `is_instance_valid` checks elsewhere) or the dispatcher converts Node refs to unit_ids. Not a wave-2 problem; flagged for Phase 2 ai-engineer.

---

### v0.13.1 — Phase 1 Session 1 live-game fixes (2026-05-01) — lead

User-visible Definition of Done (kickoff §73, items 1–8) was confirmed green by the lead in the editor: 5 kargars spawn on the terrain plane; left-click selects (gold ring); right-click on terrain commands a move; the kargar walks the path; arrival returns to Idle pulse; click on empty terrain deselects. Three bugs blocked items 4–5 in the live game even though all 371 unit tests passed. Fixed in commit `c583d48`.

- **Unit FSM was never ticked in the live game.** `UnitState_Moving._sim_tick` polls the path scheduler and steps the position, but nothing in the live scene called `fsm.tick()` — tests called it directly, and the live game was waiting on the "MovementSystem phase coordinator" LATER item from v0.13.0. Fix: each `Unit` now subscribes to `EventBus.sim_phase` in `_ready` and calls `fsm.tick(SimClock.SIM_DT)` when `phase == &"movement"`. Symmetric `_exit_tree` disconnect. This is the same pattern `SpatialIndex` uses for `&"spatial_rebuild"`. **This is the single most important behavior to lock in via integration test (qa-engineer wave 3) — every unit-level test passed while the live game was silently broken.** When the proper MovementSystem coordinator ships, this is a 3-line removal: drop `_on_sim_phase`, drop the connect/disconnect, register the unit with the coordinator instead. See LATER item below.

- **Edge-pan moved opposite to camera-look.** Two issues stacked in `camera_controller.gd`. (a) `pan_by` did not rotate the screen-axis vector through the rig's basis; with the camera_rig.tscn yaw of +45°, screen-up did not follow camera-forward. Fixed by `world_pan = global_transform.basis * local_pan` (`is_inside_tree()`-guarded so headless test fixtures stay identity). (b) `compute_edge_pan_axis` used the opposite Y-sign convention from WASD: mouse-near-top produced `ax.y = -1` while pressing W produced `ax.y = +1`. The two paths cancelled into world directions opposite each other. Fixed by flipping edge-pan signs so mouse-near-top → `ax.y = +1` (matches WASD W). The original wave-1 tests asserted only the sign of `ax.y`, not the resulting world direction, so the bug slipped through. Tests updated; new comment documents the convention.

- **HUD labels swallowed clicks.** `Label` and `MarginContainer` default `mouse_filter` is `MOUSE_FILTER_STOP`, which silently absorbs mouse events that fall in the Control's rect. The `StatusLabel` (y=56–88), the HUD `MarginContainer` (top 48px), the `HBox`, and the four resource Labels were all eating clicks in their rects. Fix: `mouse_filter = 2` (IGNORE) on each. Defensive but correct — these Controls are decorative readouts, not interactive.

- **`DEBUG_LOG_CLICKS` flag added** in `click_handler.gd`. Default ON. Prints every left/right press, what the raycast hit, and what command (if any) was issued. This was the diagnostic that made bug #1 visible. Left ON for now so the next interactive testing session has the same visibility. Will be flipped off (or gated by `DebugOverlayManager`) once the path is trusted.

- **LATER items surfaced in v0.13.1:**
  1. **MovementSystem phase coordinator** — promoted in priority. The transitional `Unit._on_sim_phase` works for the 5-unit Phase 1 case; at MVP scale (50–100 units) the per-unit signal-handler dispatch is fine, but the coordinator's deterministic `unit_id`-sorted iteration is the long-term shape. The Sim Contract §2 example for `MovementSystem` is the target signature. Estimated 1 small wave; not blocking.
  2. **Visual smoke test** — Phase 0 retro added a §9 rule about scene-level smoke tests; this set of bugs is the canonical "headless tests passed; live game broken" case the rule was written to catch. qa-engineer wave 3 is implementing the integration tests now. The lasting fix is a CI-runnable test that loads `main.tscn`, spawns a unit, drives ticks via `SimClock._test_advance`, and asserts the unit's `global_position` advances toward a commanded target. If that test had existed at the start of session 1, bug #1 would have been caught the moment Idle/Moving + Kargar shipped.

---

### v0.14.0 — Phase 1 Session 2 wave 1B (2026-05-03) — ai-engineer (GroupMoveController skeleton)

- **`GroupMoveController` lives in `scripts/movement/`, not `scripts/ai/`.** The kickoff brief authorized either location. `scripts/movement/` is a new directory; the rationale is that the controller is a pure dispatcher for the player's right-click — it touches no AI controller, no perception, no targeting heuristics. Keeping it next to (the future) `scripts/navigation/` mirrors how the directory map in §4 already groups path-related code, and leaves `scripts/ai/` exclusively for opponent-AI work (`DummyAIController`, `TuranController`). If a future AI-side use of group-move dispatch lands (e.g., AI panic-retreat for a squad), it imports the controller — same primitive, no relocation needed.

- **No `class_name` on the controller.** Same registry-race pattern as `MatchHarness` and the path-string `extends` pattern in `farr_system.gd` (§6 v0.4.0). Static methods on a preloaded script ref work identically with or without `class_name`; callers use `const _GMC := preload("..."); _GMC.dispatch_group_move(...)`. The kickoff brief explicitly authorized this choice; pre-empting the race rather than fighting it.

- **Offset radius 2.0 chosen for nav-snap survival.** `Constants.GROUP_MOVE_OFFSET_RADIUS = 2.0`, sized at 8× the navmesh `cell_size` (0.25 baked in `terrain.tscn`). The kickoff brief flagged this as the load-bearing live-game-broken-surface item — adjacent ring slots of 2.0 chord-length cannot collapse to the same nav-poly under `NavigationServer3D.map_get_path`'s snap-to-poly behavior. Headless tests use `MockPathScheduler` which returns straight-line targets without snapping, so a smaller radius would pass tests but fail in the live game. Documented in the constant's source comment and the controller header. (The constant itself was committed by balance-engineer in `42a2f9b` — wave 1B is a consumer.)

- **Concentric-ring algorithm (1 + 6 + 12 + 18…).** Index 0 sits at the click center; indices 1..6 fan out to a single ring of radius R at 60° spacing; indices 7..18 fall on a 2R ring at 30° spacing; etc. Phase 1's worker cap is 5, so all five units fit on ring 1 (one center + four-of-six ring slots actually used). The algorithm scales — a future 50-unit selection would distribute across 4 rings without any extra logic — but the simplest expression that meets the wave-1 requirement was the priority. Determinism is guaranteed by index-based trig (`cos(i × 60°)`, `sin(i × 60°)`); no `randf()`, no time, no scene state.

- **Single-unit fast path returns `target` verbatim.** Important behavioral detail: a 1-unit "group move" must be bitwise-identical to the existing single-click move. Skipping the offset math for `live.size() == 1` preserves that exactly — no float drift from a `cos(0) = 0.99999…` round-trip — and means the wave-2 click-handler wiring can route both single and multi selections through the controller without breaking the session-1 single-click test suite.

- **What did not ship in wave 1B (deferred to wave 2C):** Click-handler wiring. `click_handler.gd::process_right_click_hit` still calls `unit.replace_command(&"move", {target})` directly per unit. Wave 2C's job is to switch the multi-selection branch to `GroupMoveController.dispatch_group_move(selected, target)` while keeping the single-unit branch untouched (the controller's identity case makes this a 2-line change). The `📋 Planned` row in §2 is now `🟡 In progress`; flips to `✅ Built` when wave 2C lands.

- **What did not ship in wave 1B (out of scope per kickoff):** Facing/rotation (Phase 2). Formation-type selection (line, wedge, column — Phase 2). Reservation-based pathing where units claim individual destination tiles (Phase 3+ when buildings + spatial reservations exist). Off-navmesh target validation — kickoff brief explicitly said "each unit's `request_repath` will FAIL; that's the unit's problem, not yours." `UnitState_Moving`'s FAILED-path branch already handles this.

- **Live-game-broken-surface answers (Experiment 01):** see commit `9d54d79` body — three answers covering snap survival, formation feel/visual, and the lead's interactive smoke test.

- **LATER items surfaced in v0.14.0:**
  1. **Wave 2C wiring** — `click_handler.gd::process_right_click_hit` 2-line swap.
  2. **Stress-test at higher unit counts** — Phase 2/3 army-scale selections.
  3. **Formation visualization for the F1 pathfinding overlay** — Phase 6 per CLAUDE.md.

---

### v0.14.1 — Phase 1 Session 2 wave 1A (2026-05-01) — ui-developer (Box / drag selection)

- **Two-file split: pure math vs. input handler.** `box_select_math.gd` (RefCounted, no class_name) holds `rect_from_corners`, `is_past_dead_zone`, `units_in_rect`. `box_select_handler.gd` (Node) holds the input-event flow, the projection sweep, and the SelectionManager wiring. The headless GUT runner can't reliably stand up a real `Camera3D + Viewport + scene tree` for `unproject_position`; splitting the math out lets the rectangle-vs-projected-point logic be unit-tested without the engine layer (16 math tests cover all four drag directions, dead-zone thresholding, on-screen filter, malformed-entry guards). The handler tests inject a `project_unit: Callable` into `box_select_units(rect, units, project_callable, shift)` so input flow + selection wiring is exercised without a Camera3D either (15 handler tests). The remaining live-game-broken-surface (real `Camera3D.unproject_position` against the live scene; mouse_filter behavior under real input) lands in the lead's interactive smoke test.

- **Coordination with `click_handler.gd` is one-direction (we forward; it never feeds us).** The kickoff said "do NOT modify selection_manager.gd or click_handler.gd core logic." `BoxSelectHandler` claims left-press first via `_unhandled_input` tree-order (sibling **after** `ClickHandler` in `main.tscn`, so `_unhandled_input` reaches it first under Godot's reverse-tree-order delivery). Always calls `set_input_as_handled()` on press AND release, so `ClickHandler` never sees the left button. On non-drag release (the click case), we re-raycast and forward the result to `ClickHandler.process_left_click_hit(hit)` — its existing public seam, which qa-engineer already test-covered (`test_click_handler.gd`). Right-click events still reach `ClickHandler` directly (we only intercept left-button presses). This preserves the "click_handler.gd untouched" contract: not one line edited; we depend on its public surface only.

- **Press-time Shift state, not release-time.** The handler captures `mb.shift_pressed` at press time and uses that value for the release-time selection mode. Real-world case: player hits Shift, presses, releases Shift mid-drag, then releases the mouse — the selection mode stays additive. This is the standard RTS behavior and matches StarCraft 2 / AoE2 muscle memory. Tested explicitly (`test_shift_state_uses_press_time_value`).

- **Drag overlay is a CanvasLayer with `mouse_filter = MOUSE_FILTER_IGNORE` belt-and-braces.** The `.tscn` sets `mouse_filter = 2` on the Rect Control. The `_ready` of `drag_overlay.gd` AND `drag_overlay_rect.gd` re-asserts `mouse_filter = Control.MOUSE_FILTER_IGNORE` at runtime as a defensive guard against future scene drift. Session 1's exact regression pattern (HUD labels at default `MOUSE_FILTER_STOP` swallowing clicks) is what we're inoculating against. The overlay CanvasLayer is `layer = 10` so it draws above the 3D viewport; the child Control is anchored full-rect (anchor_right/anchor_bottom = 1.0) so screen-space coords work without per-frame conversion.

- **Empty-rect drag deselects (no Shift) / preserves (Shift).** Drag on empty space with no Shift clears the selection — the documented "drag onto nothing = deselect" RTS convention. With Shift, prior selection is preserved (additive mode never destroys what's already there). Both paths covered by tests (`test_box_select_on_empty_rect_deselects_all_no_shift` and `test_box_select_on_empty_rect_with_shift_preserves_selection`).

- **Linear unit-iteration sweep, not SpatialIndex.** The handler walks `get_tree().current_scene` looking for unit-shaped Node3Ds (duck-typed: `unit_id` field, `team` field, `Node3D` instance). At Phase 1's worker cap of 5, the linear walk costs nothing; SpatialIndex would add an autoload dependency for no measurable benefit. The kickoff brief explicitly authorized either ("iterate the Units in the scene OR use SpatialIndex"). When the unit count grows past ~50 (Phase 2+), revisit. Filtered to `team == TEAM_IRAN` so the player only box-selects their own units; future enemy-target box-select for attack-move would relax this filter.

- **Live-game-broken-surface answers (Experiment 01 — refined):**
  1. *What state/behavior must work at runtime that no unit test exercises?* The drag overlay's `mouse_filter = MOUSE_FILTER_IGNORE` (set in .tscn AND re-asserted in `_ready` of both `drag_overlay.gd` and `drag_overlay_rect.gd`). Real `Camera3D.unproject_position` per visible Iran unit on every release event (handler iterates `get_tree().current_scene` for unit-shaped Node3Ds with `team == TEAM_IRAN`). Coordination with `click_handler.gd`: BoxSelectHandler always claims the press; on non-drag release it re-raycasts and forwards via `ClickHandler.process_left_click_hit` — the only one-direction integration path that doesn't violate the "don't modify click_handler.gd" rule.
  2. *What can a headless test not detect that the lead would notice in the editor?* Visual: rectangle anchoring/transparency/stroke (1.0 alpha gold stroke @ 0.85, fill @ 0.20). Behavioral: drag-from-HUD-into-world (HUD's labels were set to MOUSE_FILTER_IGNORE in session 1, so the press hits us — but if a future interactive HUD lands, its `_gui_input` should claim it before we see it). Drag in all 4 corner-directions (math tested; visual rect anchoring needs eyes). Quick-click with 1–3px jitter mistaken for drag (the 4px squared-distance threshold is the line; lead may want 6 or 8 if it feels twitchy on trackpad).
  3. *What's the minimum interactive smoke test that catches it?* Lead drags TL→BR across the 5 kargars: all 5 selected, gold rings appear. Lead drags BR→TL: same result (the math is direction-agnostic, but the visual rectangle should still anchor at the top-left of the dragged area). Lead Shift-drags a partial subset while 2 are already selected: only the new units are added; existing stay. Quick-click on one kargar with no drag: that one selects (single-click path through `ClickHandler.process_left_click_hit` still works). Click on empty terrain: deselect all. Drag onto empty space (no units inside the rect, no Shift): deselect all. Drag onto empty space with Shift: prior selection preserved.

- **LATER items surfaced in v0.14.1:**
  1. **`SelectionManager.select_many(units)` to collapse the broadcast.** The current `_commit_selection` calls `deselect_all()` then `add_to_selection(u)` for each hit, which emits one `selection_changed` per unit. At Phase 1's scale this is invisible; at Phase 2's scale (large mixed-army selections) it's worth a single-broadcast API. Out of scope for wave 1A per the kickoff's "do NOT modify selection_manager.gd" rule. Flag for a follow-up wave when `SelectionManager` next gets touched.
  2. **Formation-shape selection feedback.** When the player drags a rect, the units inside light up immediately on release; there's no "drag-preview" highlight (units the rect would catch are NOT visually flagged until release). Standard RTS UX has a soft hover-style highlight while the rect is active. Phase 2+ when the visual polish budget opens up.
  3. **Empty-rect drag interaction with right-click cancellation.** Right-click during an active drag currently doesn't cancel the drag (the drag completes on left-release per the captured shift state). RTS convention is "right-click during marquee = cancel." Tracked but not blocking — wait for lead feel-test feedback before changing.

---

### v0.14.2 — Phase 1 Session 2 wave 2C (2026-05-04) — ai-engineer (GroupMoveController wire-up)

- **The 2-line behavioral swap landed.** `click_handler.gd::process_right_click_hit` previously walked the selected-units array and called `u.call(&"replace_command", Constants.COMMAND_MOVE, payload)` once per unit with the same target. Wave 2C replaces that loop with `_GroupMove.dispatch_group_move(sel, target)` (preloaded at file scope as `const _GroupMove := preload("res://scripts/movement/group_move_controller.gd")`). Single-unit selections still get the click point verbatim (the controller's identity fast path); multi-unit selections fan out across the deterministic ring in `Constants.GROUP_MOVE_OFFSET_RADIUS`. The `kickoff §137` "do NOT modify click_handler.gd core logic" rule was the wave-1 contract; wave 2C is the explicitly authorized exception per the wave-2C brief, and the change is contained to the multi-selection branch only — the raycast, no-selection short-circuit, hit-on-unit short-circuit, and DEBUG_LOG_CLICKS instrumentation are untouched.

- **Why both branches go through the controller (not "single-unit keeps the old loop").** The kickoff explicitly authorized this — the controller's `live.size() == 1` fast path returns `target` verbatim with no offset math, so a single-click move is bitwise-identical to the pre-wire-up behavior. Routing both through one dispatcher means future changes to the move-dispatch path (e.g., adding a queueing mode, a logging hook, an Attack-Move path) edit one place, not two. The session-1 single-click test suite still passes because the observable behavior didn't change: 1 selected unit → 1 `replace_command` call with `{&"target": click_point}`.

- **`Unit.has_method(&"replace_command")` defensive check dropped.** The previous loop did `if u.has_method(&"replace_command"): u.call(...)`. The controller calls `live[i].replace_command(...)` directly without a method-existence check — it relies on `is_instance_valid()` filtering only. This is safe because `SelectionManager` only stores Unit-shaped objects (its `select` API itself uses the `command_queue` + `replace_command` duck-type via `_is_unit_shaped`), and `is_instance_valid()` covers freed entries. The defensive check was load-bearing for a hypothetical future where someone shoves a non-Unit into the selection — that's a contract violation, not a runtime case worth a per-call check. If this assumption ever flips, `SelectionManager` is the right place to enforce it (one gate, not every dispatch site).

- **Three new tests in `test_click_handler.gd` (+3, total 472 → was 469 in HEAD).** `test_right_click_multi_selection_distributes_targets` (TDD-red on the unwired baseline; verifies that 3 selected units produce ≥2 pairs of distinct targets — the "no piling" guarantee). `test_right_click_multi_selection_targets_within_radius` (each dispatched target lies within `GROUP_MOVE_OFFSET_RADIUS` of the click on the XZ plane; Y is preserved verbatim). `test_right_click_single_selection_target_unchanged` (regression guard for the session-1 single-click suite — the controller's identity path keeps single-selection bitwise-identical). The existing `test_right_click_pushes_command_to_every_selected_unit` test still passes (the controller dispatches one `replace_command` per live unit; observable end state matches).

- **Live-game-broken-surface answers (Experiment 01 — refined):**
  1. *What state/behavior must work at runtime that no unit test exercises?* The wire-up is exercised by every right-click in the live game — that part headless tests cover via `process_right_click_hit(hit)` synthetic dispatch. What they cannot reach: the chain `box_select_handler.gd → SelectionManager.add_to_selection (×N) → click_handler.gd._unhandled_input(MOUSE_BUTTON_RIGHT) → raycast → GroupMoveController.dispatch_group_move → unit.replace_command → StateMachine.transition_to_next → UnitState_Moving.enter() → MovementComponent.request_repath`. `NavigationAgentPathScheduler` (production scheduler) snaps each offset target to the nearest navmesh poly; `MockPathScheduler` does not. R = 2.0 (8× the 0.25 navmesh `cell_size`) keeps adjacent ring slots distinct against snap-to-poly per wave 1B's analysis. With box-select shipping in wave 1A, this wave-2C wiring is the **first time multi-unit movement actually exercises the production navmesh path with real offset targets** — until now there was no UI path to put 2+ kargars in the selection.
  2. *What can a headless test not detect that the lead would notice in the editor?* The visible spread of 5 kargars arriving at distinct positions vs. piling up — a feel question that passes tests both ways. The mid-move redirect behavior (right-click while units are still moving): does the second dispatch cleanly cancel the first repath and re-issue with new ring offsets, or does it stutter? Whether a quick double-right-click on nearby points feels like "go there, then adjust" or jitters. Whether the formation rotation looks right (it shouldn't — facing/rotation is Phase 2; if the lead sees rotation here, that's a regression in `UnitState_Moving`).
  3. *What's the minimum interactive smoke test that catches it?* Lead box-selects all 5 kargars (now possible with wave 1A's marquee), right-clicks a far point: all 5 walk, no piling, ring is visibly distributed. Lead right-clicks again mid-motion: clean redirect, all 5 still distributed at the new target. Lead right-clicks near a navmesh edge: the off-navmesh-ring-slots fail individually via `request_repath` FAILED, drop back to Idle (per `UnitState_Moving`'s FAILED branch); other slots still walk. This is the interactive test wave-1B flagged as "testable from the keyboard after wave 2C wiring" — wave 2C is what makes it real.

- **LATER items surfaced in v0.14.2:**
  1. **Shift-queue formation moves.** The controller has a sister primitive `append_command` (per State Machine Contract §2.5) that would drive Shift-queue waypoint moves. Right-click currently hardcodes `replace_command` (right-click semantics, not Shift-queue). When the keybinding wave lands Shift+right-click, the click handler can branch on `mb.shift_pressed` and dispatch through a `dispatch_group_move_append` variant. Out of scope for 2C per the kickoff.
  2. **Right-click on enemy unit / friendly unit.** Currently no-op (Phase 2's attack-move and follow/guard land here). The controller's `dispatch_group_move` is target-agnostic — it dispatches `&"move"` only. Attack-move will need a parallel `dispatch_group_attack_move` that dispatches `&"attack_move"` (or a kind argument on the existing dispatcher). Phase 2.
  3. **Stress-test mid-move-redirect at higher unit counts.** With 5 kargars the mid-move-redirect is fine; at 50+ units (Phase 2/3 army-scale selections) the per-tick MovementComponent repath cancel + reissue cost may be measurable. Profile when army-scale selections actually exist; not blocking now.

---

### v0.14.3 — Phase 1 Session 2 wave 2B (2026-05-04) — ui-developer (Selected-unit panel)

- **No `unit_health_changed` signal exists; HP polled in `_process` against the displayed unit.** The kickoff brief said "subscribe to `EventBus.unit_health_changed` (verify exact signal name)" — verification revealed the signal does not exist. The only HealthComponent emission is `EventBus.unit_health_zero(unit_id)`, fired once at hp=0 as the StateMachine death-preempt trigger (per State Machine Contract §4.2). Adding a per-tick `unit_health_changed` signal would emit write-shaped EventBus signals from `_sim_tick` for every damaged unit on every tick — wasteful when 99% of the time no UI consumer cares, and a write-shaped signal in tick state needs L2-allowlist plumbing. Polling one displayed unit each `_process` frame is O(1), stays off-tick (Sim Contract §1.5: UI reads sim state freely off-tick), and matches the resource_hud.gd pattern for Coin/Grain/Pop reads. When CombatSystem ships in Phase 2 and damage numbers / floating-text feedback need on-change precision, that's the right time to add a targeted signal. Until then: poll.

- **`refresh_displayed_unit()` not `apply_*` — lint rule L1.** `tools/lint_simulation.sh` rule L1 forbids any `apply_*` method in a file that defines `_process` (the convention "`apply_*` means sim-side mutator," documented in §6 v0.6.0). Selected-unit panel runs `_process` for HP polling, so the public refresh seam is `refresh_displayed_unit()`. Same precedent the camera controller set with `pan_by` / `zoom_by`. The naming is the lint signal that this is UI-side state, not sim-side.

- **Three sub-layouts toggled by `visible_state` tag, not by parent visibility alone.** The panel exposes `visible_state: StringName` (`&"empty"` / `&"single"` / `&"multi"`) so tests assert on the stable identifier rather than walking nested visibility properties. Visual tweaks (collapsing EmptyLayout into a styled root background, adding a `&"multi_overflow"` state for >12 selections) become refactor-safe — the test contract is the tag, not the node tree.

- **Multi-select icon Buttons use a child ColorRect swatch, not a theme override.** Godot's `Button` node has no built-in tint property; the standard placeholder approach is a child `ColorRect` filling the button's rect. The button itself is `MOUSE_FILTER_STOP` (catches the click), the swatch is `MOUSE_FILTER_IGNORE` (lets the click reach the button). Tooltip carries the unit's translated type name. Each Button's `pressed` signal is `.bind(unit_id)` at creation time, so the click resolves to the correct unit even if the icon list reshuffles between render and click — defensive `is_instance_valid` walk through `SelectionManager.selected_units` further filters freed units at click time. `_MAX_ICONS = 12` (3 rows of 4) caps panel growth; overflow rendering is a Phase 2 polish concern.

- **Twelve new tests in `test_selected_unit_panel.gd` (+12).** Scene loads, root is CanvasLayer; mouse_filter discipline (no non-button container is MOUSE_FILTER_STOP — recursive walk finds offenders); empty / single / multi states; HP bar reflects health; type label uses `tr()`; multi-selection icon click narrows via `select_only`; freed-unit icon click is safe no-op; freed displayed unit transitions panel out of `&"single"` state; translation keys (UI_PANEL_NO_SELECTION, UI_PANEL_HP, UI_PANEL_ABILITIES, UNIT_KARGAR) all resolve to English under en locale. Headless GUT: all 12 pass alongside the existing 472 (post wave-2A/2B/2C aggregation; 484 total at this commit before the parallel double-click and control-groups waves land their own tests).

- **Live-game-broken-surface answers (Experiment 01 — refined):**
  1. *What state/behavior must work at runtime that no unit test exercises?* The signal-driven re-render cycle when selection changes mid-frame (e.g., box-select releases → SelectionManager broadcasts → panel rebuilds icon grid → player clicks an icon → SelectionManager broadcasts again → panel rebuilds to single-state). Headless tests dispatch each step in isolation; the live game stacks them within a single frame's input + render cycle. Icon-button signal connections survive the rebuild because we `queue_free` old buttons (deferred — the click that triggered the narrow can complete before the buttons are gone). The `_process` HP poll on a freed unit (race between "unit dies" and "panel re-renders to empty") — the `is_instance_valid` guard in `refresh_displayed_unit` falls back to `_render_empty()` defensively. None of these races are deterministic; only live runtime exercises the timing.
  2. *What can a headless test not detect that the lead would notice in the editor?* Visual layout — does the 250×120 panel feel right at 1280×720? Does it overlap with anything (control-group HUD bar, Phase 2+)? Does the bottom-left anchor survive at 1920×1080 / 2560×1440 without scaling weirdly? Does the placeholder grey-rect aesthetic clash visually with the FarrGauge's gold-ivory palette? Multi-select icon grid layout — at 12 icons (3 rows of 4), is the panel still legible? Are the icon swatches large enough to distinguish faction colors? Does clicking the icon feel responsive (sub-50ms perceived) or laggy? The HP bar fill ColorRect — does the proportional width update look smooth as HP drops, or does it pop discretely (we resize each `_process` so it should be smooth, but the ColorRect's anchor preset may snap at certain ratios)? None of this is observable from `visible_state` / `hp_ratio` reads.
  3. *What's the minimum interactive smoke test that catches it?* Lead boots, sees nothing in the panel ("No selection" centered). Lead clicks a kargar: panel shows portrait (sandy-brown), "Kargar" label, full HP bar, 4 grey ability rects. Lead box-selects all 5: panel shows 5 sandy-brown icon swatches in a row (still under 12 cap). Lead clicks the 3rd icon: selection narrows to that one kargar; panel transitions back to single-layout for that specific unit. Lead clicks empty terrain: panel returns to "No selection" centered. Lead's 30-second loop. If the panel covers up the FarrGauge, ResourceHUD, or the kargars themselves at 1280×720, that's the LATER candidate.

- **LATER items surfaced in v0.14.3:**
  1. **HP-change targeted signal when CombatSystem ships (Phase 2).** Polling is fine for one displayed unit at MVP; when the panel needs to render damage flashes or floating numbers ("-12 HP"), a `unit_health_changed(unit_id, hp_after, max_hp, source_unit_id)` signal becomes load-bearing. Add to EventBus alongside `unit_died` and `damage_dealt`; emit from `HealthComponent.take_damage` (already on-tick). The poll path stays as the fallback for non-displayed units' HP renders.
  2. **Multi-select overflow rendering.** Currently caps at 12 icons silently. Beyond 12, render the first 12 plus a "+N more" tag — Phase 2 polish; not load-bearing while Phase 1 caps at 5 kargars.
  3. **Subgroup management beyond icon-narrows-to-one.** Modern RTS panels let players ctrl-click an icon to remove that one unit, or shift-click to select-only-that-type within the multi-selection. Out of scope per kickoff §2 (6); pencil in for Phase 2 selection polish if lead-test feedback flags it.
  4. **Real portraits + real ability buttons.** Placeholder rects per CLAUDE.md until the design chat green-lights art. When portraits land, the `_portrait` ColorRect becomes a `TextureRect` reading from a `unit_portrait_path` field on UnitStats; ability rects become `Button`s wired to the unit's `ability_set` (Phase 2 with Rostam's hero abilities).
  5. **Font-size i18n.** The panel's labels use `ThemeDB.fallback_font` at default sizes. When the Persian column lands at Tier 2, the Latin → Persian script change may need a different font face and a different size to feel right — that's a theme override on the panel root, not a code change. Documented but no work needed pre-fa.

---

### v0.14.5 — Phase 1 Session 2 wave 3 (2026-05-01) — qa-engineer (Integration tests: session-2 flows)

- **37 new integration tests across 5 files; test count 498 → 535.** Coverage: box/drag selection (public test seams, no real Camera3D), control group bind/recall/center round-trip (stub CameraController injected via `set_camera_target`), group-move pile-prevention (5-unit dispatch → ≥4 distinct XZ positions within ε=0.5), Farr gauge listener (target_farr + color_band on `farr_changed` signal, off-tick per Sim Contract §1.5), selected-unit panel content (empty/single/multi states, death, icon click, MOUSE_FILTER_STOP regression guard), and a cross-feature smoke test. 3 pre-existing PENDING unchanged (FarrSystem fallback, navmesh ×2).

- **`add_child_autofree` BEFORE `global_position` assignment is load-bearing.** `Node3D.global_transform` asserts `is_inside_tree()`. Setting `global_position` before adding the node to the scene tree silently returns `Transform3D()` and prints "Condition '!is_inside_tree()' is true." — the unit appears at origin regardless of the value written. All `_spawn_kargar` helpers follow the pattern: `add_child_autofree(u)` first, then `u.global_position = pos`. This is the correct order for all Node3D instantiation in tests.

- **Smoke test step-5 (narrow single-select) uses the unit's actual post-movement screen position.** Units move to formation positions around (20,0,20) in step 2; their exact positions depend on ring offsets. Hardcoding screen coordinates based on original spawn positions selected 0 units. Fix: call `_project_unit(800, 600).call(target_unit)["screen_pos"]` against the first live unit's CURRENT `global_position`, then build a tight 1.2×1.2 Rect2 centered there. The 1.2×1.2 size guarantees exactly 1 unit at the 10:1 projection scale since ring slots are ≥2.0 world units apart (≥20 screen pixels).

- **Farr gauge signal tests are off-tick per Sim Contract §1.5.** `EventBus.farr_changed` is emitted directly in tests without advancing `SimClock`. `gauge.target_farr` and `gauge.color_band` are read immediately after the emit — both update synchronously in the gauge's signal handler. No `await get_tree().process_frame` needed (and none used) — adding an `await` would allow deferred UI updates that mask synchronous-update bugs.

- **HP partial health written via `hp_x100` backing field, not `hp`.** `HealthComponent.hp` is a read-only getter computed from `hp_x100`; `hc.set(&"hp", value)` silently does nothing in GDScript. Tests write `hc.set(&"hp_x100", max_x100 / 2)` directly. The `max_hp_x100` field is also read via `hc.get(&"max_hp_x100")` for safety (avoids hardcoding the kargar's max_hp value in the test itself).

- **MOUSE_FILTER_STOP regression guard in panel tests.** `test_panel_root_does_not_swallow_clicks_via_mouse_filter` recursively walks all `Control` nodes under the panel and asserts that none have `MOUSE_FILTER_STOP` unless they are a `Button`. This locks in the session-1 fix (click-eating HUD bug) so it cannot regress if panel layout changes. The Button carve-out is correct — icon Buttons legitimately stop clicks.

- **main.tscn spot-check confirms DoubleClickSelect + SelectedUnitPanel coexistence.** Both wave-2A and wave-2B nodes were committed separately. Loading `main.tscn` as a `PackedScene` and calling `get_node_or_null` + `has_method` duck-typing confirms neither clobbered the other. This test is the Experiment 01 live-game-broken-surface answer: had it existed in session 1, it would have caught the cross-agent stomp scenario.

- **Live-game-broken-surface answers (Experiment 01 — wave 3):**
  1. *What state/behavior must work at runtime that no integration test exercises?* Real `Camera3D.unproject_position` path — all tests inject a linear projection callable. Actual NavMesh snap behavior in group-move — tests use MockPathScheduler which returns straight-line paths; navmesh snap-to-poly may collapse ring offsets in live play (the production-scheduler integration test is marked PENDING when no baked navmesh exists). EventBus signal delivery order — tests emit `farr_changed` directly; in live play SimClock drives `apply_farr_change` inside a tick, which may interleave with other `sim_phase` subscribers.
  2. *What can a headless test not detect?* Panel rendering at different viewport sizes (all tests assume 800×600 projection). Tween animation of the Farr gauge (tests read `target_farr` and `color_band`; the tween's intermediate steps are UI-only and headless-invisible). Whether the smoke test's 120-tick budget is tight enough at slower machines (tick rate is deterministic but total wall time varies).
  3. *Minimum interactive smoke test:* Lead boxes 5 kargars → right-click move → watch them spread without piling → bind Ctrl+1 → click empty space → press 1 → same 5 reselected → panel shows STATE_MULTI with 5 icons → click one icon → panel shows STATE_SINGLE. Farr gauge changes color when Farr drops below 15. Both SelectedUnitPanel and DoubleClickSelect are present and functional.

- **LATER items surfaced in v0.14.5:**
  1. **Production-scheduler pile-prevention test.** The group-move integration tests use MockPathScheduler. The PENDING test `test_5_units_move_to_distinct_positions_via_production_scheduler` (in `test_session_2_group_move.gd`) should be enabled once a baked navmesh is available in the test scene. This is the last gap in the live-game-broken-surface coverage for group move.
  2. **Farr gauge tween intermediate color-band test.** Currently tests read `color_band` after the signal; the tween may display the wrong band during animation if the `color_band` update in `_process` step is missed. A tween-step spy (or faster tween duration in tests) could cover this. Defer to Phase 2 visual polish.
  3. **Selected-unit panel HP polling frequency.** Panel polls `get_health().hp` every `_process` frame. With 5 units selected and switching between single-display, this is O(1) per frame — fine at Phase 1. At Phase 2+ when there are 50+ units and combat damage is continuous, the HP delta on the displayed unit will be imperceptible. Defer until combat ships.

---

### v0.15.0 — Phase 2 Session 1 wave 1C (2026-05-01) — balance-engineer (BalanceData combat fields)

- **Three new `UnitStats` fields for Phase 2 combat (`attack_damage_x100`, `attack_speed_per_sec`).** `attack_range` was already present from Phase 0. The two new fields follow the Sim Contract §1.6 fixed-point convention: `attack_damage_x100` stores damage × 100 as `int` to eliminate float drift at 30 Hz tick rates. `attack_speed_per_sec` is a `float` since it drives a cooldown denominator, not a tick counter. The legacy `damage: float` and `attack_speed_ticks: int` fields are retained — existing kargar/piyade/kamandar entries populated them and other agents' code may read them. Phase 2 `CombatComponent` reads the new fields only.

- **Kargar `attack_damage_x100 = 0` is the "cannot attack" sentinel.** `attack_range` set to `0.0` (was `1.0` — was an oversight in Phase 0; 1.0 implied it could enter melee range). `CombatComponent` will skip the attack tick when `attack_damage_x100 == 0`.

- **`turan_piyade` entry added to balance.tres.** Mirrors Iran `piyade` exactly — `max_hp=100`, `attack_damage_x100=1000`, `attack_speed_per_sec=1.0`, `attack_range=1.5`. RPS effectiveness differentiating Turan vs Iran units ships in Phase 2 session 2. A TODO comment in the sub-resource marks the gap.

- **`Constants.ENGAGE_RADIUS = 4.0` added under new `# === COMBAT ===` section.** Used by `UnitState_AttackMove` to decide when to break from move and engage. Sized at ~2.7× max melee range (1.5). Tunable without code change when Kamandar (ranged, range 8.0) ships.

- **`validate_hard()` extended for new fields.** Three new hard invariants: `attack_damage_x100 >= 0`, `attack_speed_per_sec > 0` (divide-by-zero guard in cooldown calc), `attack_range >= 0`. `validate_soft()` warns on suspiciously high values (>10000 damage, >100 speed, >50 range).

- **LATER items surfaced in v0.15.0:**
  1. **Kamandar combat fields unpopulated.** Kamandar in balance.tres uses GDScript defaults (`attack_damage_x100 = 0`, `attack_speed_per_sec = 1.0`). It will appear as a non-attacking unit until Phase 2 session 2 populates its combat fields alongside the full RPS matrix.
  2. **`piyade.max_hp` changed from 120.0 → 100.0.** Phase 0 value was 120; session-1 kickoff brief specifies 100 (1.7× Kargar's 60). The prior 120 value was also a "starting point to be tuned" per §0. Any test asserting `max_hp == 120` should be updated; currently `test_balance_data.gd` asserts 100.

---

### v0.16.0 — Phase 2 Session 1 wave 1A (2026-05-01) — gameplay-systems (CombatComponent + HealthComponent death-emit expansion)

- **`CombatComponent` ships at `scripts/units/components/combat_component.gd`.** Path-string SimNode base (matches HealthComponent / MovementComponent / FarrSystem registry-race pattern). Holds `attack_damage_x100: int`, `attack_speed_per_sec: float`, `attack_range: float` (read from BalanceData by Unit `_apply_balance_data_defaults`); internal `_target_unit_id: int = -1` and `_attack_cooldown_ticks: int = 0` (both written via `_set_sim`). `set_target(uid)` is the public command interface; `_sim_tick` runs cooldown decrement → target resolution → XZ-range check → fire (calls `target.get_health().take_damage_x100`). Production target lookup walks the scene tree O(N) per missed lookup; tests inject a closure (mirrors MovementComponent's `_scheduler` pattern from Sim Contract §4.1). Wired into `unit.tscn` as a sibling of MovementComponent; Unit `_apply_balance_data_defaults` reads the three combat fields with defensive type checks.

- **`HealthComponent` extended with the fixed-point hot path + cause-aware unit_died emit.** New `take_damage_x100(amount_x100: int, source: Node = null, cause: StringName = &"unspecified")` is the integer-only chokepoint CombatComponent calls per attack tick — no float→fixed-point round-trip. Both float `take_damage` and fixed-point `take_damage_x100` converge in private `_apply_damage_x100(amount_x100, source, cause)`. New field `last_death_position: Vector3` captured from parent Node3D's `global_position` BEFORE either signal fires; Phase 5 Yadgar building reads off the listener-side `unit_died` payload (`position` field) so it never calls back into a possibly-freed component. `killer_unit_id` resolved duck-typed from `source.unit_id` (`-1` sentinel when source is null or has no `unit_id`).

- **Death-emit listener-order discipline (cb95d09 lesson).** `EventBus.unit_health_zero(unit_id)` fires FIRST (StateMachine death-preempt per State Machine Contract §4.2), then `EventBus.unit_died(unit_id, killer_unit_id, cause, position)` (broader telemetry / Farr-drain channel). The `_zero_emitted` latch is set via `_set_sim` BEFORE either emit so a re-entrant listener that synchronously damages the same component can't recurse through the death path. Single latch covers both signals — over-kill ticks emit neither.

- **`EventBus.unit_died` signal added** with `(unit_id: int, killer_unit_id: int, cause: StringName, position: Vector3)` payload. Forwarder arm added to `_make_forwarder` for the four-arg signature; `_SINK_SIGNALS` extended so MatchLogger (Phase 6) sinks it automatically. Comment block above the declaration warns listeners against synchronous cross-listener mutation.

- **Why fixed-point damage instead of pure float?** Per Sim Contract §1.6, gameplay state that accumulates over a match (Farr, hp) stores as fixed-point int to eliminate IEEE-754 platform divergence over thousands of ticks. CombatComponent's `attack_damage_x100` matches `HealthComponent.hp_x100` so the subtraction is `hp_x100 -= attack_damage_x100` with zero rounding. The float `take_damage` wrapper still exists for legacy callers (Farr drain pathways, tests) — both wrappers funnel through `_apply_damage_x100` so the death-emit discipline is identical regardless of entry point.

- **`set_target` resets cooldown to 0 on engagement.** First tick after `set_target(uid)` fires immediately (single-tick attack on engagement, matches RTS convention). The shipped tests verify `_attack_cooldown_ticks == 30` after the swing at 1.0 atk/s on 30 Hz, and `== 15` at 2.0 atk/s — pinning the cooldown formula `roundi(SimClock.SIM_HZ / attack_speed_per_sec)` directly.

- **CombatComponent target lookup is `target_lookup_callable` injection — production wires a tree-walk fallback.** Production callable walks `get_tree().root` looking for a Node3D with matching `unit_id` (O(N) per missed lookup, acceptable at session-1 scale ≤15 units). Tests assign a closure over fixture Node3Ds — same injection seam shape as MovementComponent's `_scheduler`. The `is_instance_valid` defense in `_resolve_target` is what makes "freed target safe-clear" work: if the unit was queue_freed mid-engagement, the next `_sim_tick` lookup sees null, `_target_unit_id` is reset to -1 via `_set_sim`, no fire, no crash.

- **Forward-resolved task #139's `has_method(&"take_damage_x100")` guard.** When CombatComponent shipped earlier in the wave, `take_damage_x100` didn't yet exist on HealthComponent — the implementation guarded with `has_method` so it would no-op gracefully. With wave 1A complete, the chokepoint is real and the guard is now always-true; can be removed in a future cleanup pass without behavior change.

- **LATER items surfaced in v0.16.0:**
  1. **`UnitRegistry` autoload (id → ref dict).** Production target lookup is O(N) tree-walk per missed lookup. Acceptable at session-1's 15-unit cap; needs replacement when N>~100 (mid-Phase 3 economy + Phase 6 AI). Closes target lookup to O(1).
  2. **CombatSystem phase coordinator.** Same shape as the future MovementSystem coordinator (§6 v0.10.0). Currently UnitState_Attacking drives CombatComponent's `_sim_tick` directly via Unit's StateMachine tick path. When the coordinator lands, it iterates registered CombatComponents during the `combat` phase and calls their `_sim_tick`; UnitState_Attacking drops the explicit drive call. One-line refactor.
  3. **Cleanup of CombatComponent's `has_method(&"take_damage_x100")` guard.** Now always-true; can be removed without behavior change in a future cleanup pass.
  4. **Float `take_damage` legacy path could be deprecated.** All Phase 2+ damage flows through `take_damage_x100`; the float wrapper exists only for pre-Phase-2 callers and tests. If those migrate, the float path can shrink to a thin convenience that round-trips to fixed-point — or be removed entirely.

---

### v0.16.1 — Phase 2 Session 1 wave 1B (2026-05-01) — ai-engineer (UnitState_Attacking)

- **`UnitState_Attacking` is the third concrete UnitState — same path-string base, same registry-race dodge.** Ships at `scripts/units/states/unit_state_attacking.gd` extending the path-string `unit_state.gd` base alongside Idle and Moving. id=`&"attacking"`, priority=20 (above Moving's 10 — when both are valid same tick, Attacking takes precedence; relevant for the future attack-move state's engage transition), interrupt_level=NEVER. Per State Machine Contract §3.5, `INTERRUPT_NEVER` blocks damage-driven preemption only — player `replace_command` and death always win. Damage during Attacking flows through `EventBus.unit_health_zero` → `StateMachine._on_unit_health_zero` → force-transition to Dying (Contract §4); the NEVER level concerns `interrupt_level`-based interrupts, not death. Unit base class `_ready` registers Attacking idempotently after Idle and Moving (gated by `not fsm._states.has(&"attacking")`), matching the wave-2 default-pair pattern.

- **Target lookup is a scene-tree walk, not a SpatialIndex query.** `_find_unit_by_id` recursively walks `get_tree().current_scene` (with a `tree.root` fallback for tests where current_scene is null) matching `unit_id == target_id`. SpatialIndex isn't appropriate here — it returns agents within a radius, not by id. Phase 2's 15-unit cap (5 Kargar + 5 Iran Piyade + 5 Turan Piyade) makes the linear walk free; at 100+ units (Phase 2 session 2 + multi-unit-roster expansion) this becomes O(N) per attack-state entry. **LATER**: a `UnitRegistry` autoload (id → ref Dictionary), populated by `Unit._ready` and depopulated by `Unit._exit_tree`, closes target lookup to O(1). This LATER item is now duplicated across wave 1A (CombatComponent's per-fire target lookup) and wave 1B (Attacking's per-engagement target lookup); both consumers benefit from the same primitive.

- **CombatComponent surface is read defensively.** `enter()` calls `ctx.has_method(&"get_combat")` then `ctx.get_combat()`; `_sim_tick` reads `_combat.attack_range` via `&"attack_range" in _combat` and calls `_combat.set_target(uid)` via `_combat.has_method(&"set_target")`. This wave's commit landed bundled with wave 1A in `81cf42a`, but the defensive guards remain useful: a future unit type that has no CombatComponent (e.g., a pure-utility worker that never engages) still gets sensible Attacking-state behavior — out-of-range targets drive `MovementComponent.request_repath`, in-range targets `push_warning` and no-op on the combat side. Default `attack_range=1.5` (matches the kickoff §2 deliverable 5 Piyade default) keeps the no-CombatComponent path producing "walk into melee then stop" behavior for live-test exploration.

- **Out-of-range movement strategy: option (b) — drive `request_repath` directly each tick.** The kickoff §2 deliverable 2 brief flagged two options: (a) re-issue a Move command via the queue (would compete with future commands) or (b) drive `MovementComponent.request_repath(target.global_position)` directly without going through the command queue, and let the unit walk while the state stays in Attacking. We chose (b) — cleaner, the state owns movement directly, and the queue stays clean for Shift-queued follow-up commands. The per-tick re-request cost (the scheduler cancels the prior in-flight request internally on each new request, so it's safe but not free) is fine for Phase 2's 15-unit cap. **LATER**: throttle to every-N-ticks OR introduce a stale-distance threshold (only re-issue when the target has moved more than X units since the last request) when N>50.

- **`exit()` cleanup matches `UnitState_Moving.exit()`'s shape.** Two defensive cleanups: `combat.set_target(-1)` so CombatComponent doesn't keep firing at the prior target after the state ends (deliberate symmetry with `enter()` setting the target); cancel any in-flight repath via `_scheduler.cancel_repath(_request_id)` followed by `_request_id = -1` so MovementComponent doesn't poll a cancelled request. Per Contract §3.5 the typical exit cause is `replace_command` from the player or death-preempt — both demand a clean teardown.

- **No signal emissions from inside the state.** Per the kickoff §2 deliverable 2 brief's Pitfall #4 (`cb95d09` re-entrant signal recursion lesson): combat-side signals (`unit_died`, `damage_dealt` when it ships) are emitted by `HealthComponent` / `CombatComponent`, not by the state. Reading `SelectionManager` / scene tree mid-tick is fine (read-shaped); emitting any `EventBus.X_changed` from a state's tick would expose the same `cb95d09` class of bug if a listener mutated the same state-holder. Verified by inspection — no `EventBus.<signal>.emit(...)` calls in `unit_state_attacking.gd`.

- **Two-tick-per-test pattern (drain pending → enter, then _sim_tick).** StateMachine.tick runs `current._sim_tick(dt, ctx)` first, THEN drains `_pending_id`. So `transition_to(&"attacking")` followed by one `tick()` call lands the FSM in Attacking (Idle's _sim_tick is empty; the swap calls Attacking's `enter()`). A SECOND tick is required for `Attacking._sim_tick` to actually run. Tests that exercise `_sim_tick` behavior (request_repath, set_target, repath cancellation) call `_tick_attacker()` twice; tests that exercise enter() behavior alone call it once. Contrast with Moving's tests where `enter()` itself issues request_repath, so one tick suffices — Attacking's `enter()` only caches refs, all behavior lives in `_sim_tick`. Documented as a test idiom for future combat-state additions.

- **Test stub (`_StubCombat extends Node`) mirrors the production CombatComponent surface.** `attack_range: float`, `set_target(unit_id: int)`, plus a test-only `last_set_target: int` field for assertion. The fixture `_spawn_unit_with_stub_combat` instantiates `unit.tscn`, removes any existing `CombatComponent` child (defensive against gameplay-systems' wave 1A wiring landing first — which it has), attaches the stub as a child named `CombatComponent` so `get_node_or_null(^"CombatComponent")` resolves to the stub, and writes `_combat_component = stub` to override the @onready cache. Both wave-1A's tests (using the real CombatComponent) and this wave's tests (using the stub) coexist; the stub lives only inside this test file.

- **Live-game-broken-surface answers (Experiment 01 — refined):**
  1. *State/behavior that must work at runtime that no unit test exercises:* The Moving → Attacking → Moving redirection when a target moves out of range mid-combat. Tests fix the target's position; live game has the target retreating or other units shoving it (Phase 3 collision-push). The `_sim_tick` re-issues `request_repath` each tick to the target's CURRENT position — a per-tick cost measurable in the lead's smoke test if 5 attackers chase 5 retreating targets across the map. The scene-tree walk for target lookup runs only on `enter()`, not per tick — that's the savings; the unit-by-id lookup happens once per attack engagement.
  2. *What headless tests cannot detect that the lead would notice in editor:* Whether the unit "snaps" to face the target (no rotation logic — Phase 5 polish; if rotation appears, that's a regression elsewhere). Whether interrupting an attack via `replace_command` (player reissues a Move command mid-fight) feels responsive — `INTERRUPT_NEVER` blocks damage interrupts only, not player commands; this should feel instant. Whether attack-on-death (target dies mid-tick) jitters or transitions cleanly — the `is_instance_valid` check on the cached target ref guards against the freed-mid-tick case, but the lead may notice a frame of "swing into nothing" before the state ends.
  3. *Minimum interactive smoke test (post-wave-2B click_handler wiring + post-wave-1A CombatComponent):* Lead selects an Iran Piyade, right-clicks a Turan Piyade across the map → unit walks (Attacking → request_repath out of range), reaches melee → unit attacks (Attacking → set_target in range → CombatComponent fires per its own _sim_tick), HP bar decrements, target dies → unit transitions to Idle. Lead right-clicks a different enemy mid-attack → state transitions cleanly to a new Attacking with the new target_unit_id. Lead gives a Move command mid-attack → Attacking exits cleanly (`set_target(-1)`, cancel repath), unit walks to new target.

- **LATER items surfaced in v0.16.1 (deduplicated against v0.16.0):**
  1. **Per-tick repath throttle / stale-distance threshold.** Out-of-range Attacking re-issues `request_repath` every tick. Cheap at 15 units; profile when N>50.
  2. **Click-handler enemy-right-click branch wiring (wave 2B).** This wave shipped the state in isolation; the input-side `_handle_right_click` enemy-detection branch lands in wave 2B. Until wave 2B, Attacking is reachable only via direct `transition_to` calls (e.g., from a future `UnitState_AttackMove`'s engage transition) and from headless tests.
  3. **Animation hooks (Phase 5 polish).** No animation triggering in `enter()` / `exit()` — placeholder visuals per CLAUDE.md. When real animations land, NEVER-level interrupt semantics need to play nicely with animation cancellation: a player `replace_command` mid-swing must cancel the swing animation cleanly, not just the gameplay state.
  4. **Verification-loop process bug.** This wave triggered Deviation 01 (logged in `docs/PROCESS_EXPERIMENTS.md`). The agent confused "this work appears in the tree alongside other work I didn't make" for "someone else committed my work, I'm done" — and stood down without staging. The mitigation lives in the Phase 2 session 2 kickoff brief discipline: agents are responsible for committing their own work before stand-down; TaskList is the authority on what's been completed, not `git status`.

---

### v0.16.2 — Phase 2 Session 1 wave 2C (2026-05-03) — ui-developer (Floating health bars + F4 attack-range overlay)

- **HealthBarsOverlay — kickoff option (b), single Control overlay over per-unit Sprite3D-Viewport.** One `_draw` call regardless of unit count. `_process` walks every Iran/Turan unit, projects through the live Camera3D, computes color-band tag + width, queue_redraws. `_draw` paints horizontal bars at the projected positions with `_BAR_HEIGHT_OFFSET = 30 px` above each unit. Color bands per kickoff §2 (8): > 70% green, 30%-70% yellow, < 30% red — boundary policy is **inclusive at the yellow band's bounds** (exactly 70% / 30% are yellow). Width by `unit_type`: kargar 32 px, piyade / turan_piyade 48 px, default fallback medium. Hidden when HP is full.

- **AttackRangeOverlay — Control + projected circle, NOT Node3D + cylinder.** `DebugOverlayManager.register_overlay(key, overlay: Control)` is statically typed against `Control`, and `toggle_overlay` does `_overlays[key] as Control` (returns null for a Node3D — F4 toggle would silently no-op). Per-frame screen projection of N circle samples produces visually identical results under any camera move. **LATER candidate**: widen `DebugOverlayManager.register_overlay(key, overlay: CanvasItem)` once a second concrete overlay needs the same affordance. Pitfall #5 candidate.

- **mouse_filter == MOUSE_FILTER_IGNORE both in .tscn AND `_ready` (Pitfall #1 belt-and-braces).** Both overlays are fullscreen Controls. Third instance of the Pitfall #1 hardening pattern.

- **AttackRangeOverlay subscribes to `EventBus.selection_changed` — Pitfall #4 audit point passed.** The handler walks `SelectionManager.selected_units` (read accessor) and stashes entries on `self`. Does NOT call any SelectionManager mutator from inside the handler. Joins `selected_unit_panel.gd` and `double_click_select.gd` (defers via `call_deferred`) as the third concrete `selection_changed` consumer to ship cleanly with the cb95d09 lesson.

- **`compute_bar_entries(units, project_unit_callable)` is the public test seam.** Same shape as `box_select_handler.box_select_units`. The attack-range overlay's `handle_selection_changed(payload)` is its analogous test seam.

- **`_gather_candidate_units` is permissive (BOTH teams).** Iran AND Turan units get HP bars — combat-feel signal demands it. Phase 4 fog of war will tighten this.

- **F4 toggle path: `DebugOverlayManager.handle_function_key(KEY_F4)` → `toggle_overlay(OVERLAY_KEY_F4)` → flip `Control.visible`.** Overlay registers in `_ready`, unregisters in `_exit_tree`. F4 boots HIDDEN.

- **Live-game-broken-surface answers (Experiment 01 — refined):**

  *HealthBarsOverlay (deliverable 8):*
  1. *State that must work at runtime that no unit test exercises:* The Camera3D unproject_position projection against the live camera. `is_position_behind` filter handles units behind the camera.
  2. *What headless tests cannot detect:* Bar width readability at default zoom. Vertical offset `_BAR_HEIGHT_OFFSET = 30 px` tuned for Piyade cubes; Phase 5 cavalry/heroes may need per-unit-type offset. Color-band feel.
  3. *Minimum interactive smoke test:* Lead's Piyade attacks a Turan Piyade. Bar fades green → yellow → red. Disappears when target dies.

  *AttackRangeOverlay (deliverable 9):*
  1. *State that must work at runtime that no unit test exercises:* The F4 toggle path through `DebugOverlayManager._unhandled_input`. Pitfall #4 — handler is read-only; verified by inspection.
  2. *What headless tests cannot detect:* Whether the circle's color reads as "debug viz." Whether the circle is drawn AT the unit's feet (Y=0.05 above ground). Polygon smoothness at extreme zoom.
  3. *Minimum interactive smoke test:* Lead selects 5 Iran Piyade, hits F4 → 5 gold circles, radius 1.5. Lead hits F4 again → circles disappear.

- **Tests:** `tests/unit/test_health_bars_overlay.gd` (20) and `tests/unit/test_attack_range_overlay.gd` (14). +34 total. All pass headless.

- **LATER items surfaced in v0.16.2:**
  1. **Widen DebugOverlayManager.register_overlay parameter type** (Control → CanvasItem or Node). Pitfall #5 candidate.
  2. **Tween bar fade on damage** (Phase 5 polish).
  3. **Spatial-query gathering for HP bars** at high unit counts (Phase 4+, N>50).
  4. **Per-unit-type bar offset** for cavalry / heroes (Phase 5).

---

### v0.17.0 — Phase 2 Session 1 wave 2A (2026-05-04) — gameplay-systems (Piyade + TuranPiyade + first Farr drain)

Code shipped under commit `aa429ef` (cross-agent contamination during a parallel-wave window — ui-developer's `git commit` swept up the working-tree state which included this wave's already-completed gameplay-systems files; the wave-2A code is intact under that SHA, attribution is the only thing scrambled). This §6 entry retros the wave-2A scope.

- **Piyade is the first non-Kargar concrete Unit subclass.** Same dual `_init`+`_ready` pattern as Kargar — Godot's scene-instantiation order assigns @export defaults from the .tscn AFTER `_init`, clobbering _init's `unit_type` write; the `_ready` override fires BEFORE `super._ready()` reads unit_type to look up BalanceData. Path-string `extends "res://scripts/units/unit.gd"` to dodge the project-wide class_name registry race (see §6 v0.4.0). Stats from BalanceData: `attack_damage_x100 = 1000`, `attack_speed_per_sec = 1.0`, `attack_range = 1.5`, `max_hp 100`, `move_speed 2.5`. First read seam exercising non-zero combat fields through `Unit._apply_balance_data_defaults`.

- **TuranPiyade is the first Turan-team unit.** First `Constants.TEAM_TURAN` flow through `Unit._ready` into a `SpatialAgentComponent` for a runtime-spawned unit. Mirror-combat stats match balance-engineer wave 1C's intent.

- **`_spawn_starting_kargars` renamed to `_spawn_starting_units`.** Three position-array consts at 5 entries each = 15 starting units. Spawn order pinned: Kargar (1..5) → Iran Piyade (6..10) → Turan Piyade (11..15). Z-axis gap (Iran -8, Turan +20) means right-click engagements walk a meaningful distance.

- **First Farr drain (worker-killed-idle, -1 Farr) chosen strategy (c) — cause-string augmentation.** HealthComponent's death emit augments cause with `_idle_worker` suffix when the dying unit is a Kargar AND its FSM is in `&"idle"`. FarrSystem's `_on_unit_died` listener parses `String(cause).ends_with("_idle_worker")` and applies -1 Farr through `apply_farr_change(-1.0, "worker_killed_idle", null)` — the documented chokepoint per CLAUDE.md.

- **Why (c) over (a) and (b).** (a) extending the unit_died signature requires three cross-domain coordination points. (b) FarrSystem-side metadata Dictionary has lifetime concerns. (c) is purely additive — uses an existing signal field, no new state, forward-extensible (future drains: `_fleeing` for hero-killed-fleeing per §4, `_engaged` for hero-killed-in-battle, etc.). Cost: cause strings carry a "domain language" component documented at producer (HealthComponent) and consumer (FarrSystem._on_unit_died) sites.

- **Re-entrancy guard (cb95d09 lesson).** FarrSystem's listener confines itself to `apply_farr_change` ONLY — no SelectionManager mutation, no signal re-emit. Risk pattern that bit DoubleClickSelect → SelectionManager is structurally avoided.

- **CombatComponent passes `&"melee_attack"` as cause** (was `&"unspecified"` default). One-line change so HealthComponent's augmenter has a meaningful base cause to suffix.

- **Killer Node identity dropped at the listener boundary.** UnitRegistry autoload (now triple-LATER) would resolve `killer_unit_id: int` to a Node ref. Session 1 accepts `source_unit_id = -1` in F2 overlay log entries.

- **`reset()` re-arms the unit_died subscription idempotently.**

- **Pitfalls candidates this wave:**
  - **Pitfall #6 (candidate): Cause-string suffix conventions are domain language, not free-form telemetry.** New suffixes need explicit producer-side discipline AND consumer-side parser updates.
  - **Pitfall #7 (candidate, surfaced this session): Multi-agent shared-tree commit race.** Multiple Claude Code agents working in the same git working tree can `git add` AND `git commit` each other's working-tree changes. Mitigation: verify `git diff --staged --stat` immediately before `git commit`, AND `git log -1 --stat` after commit. Destructive resets get denied; cleanest path is to retro attribution in BUILD_LOG / ARCHITECTURE rather than rewrite history.

- **LATER items:**
  1. **`UnitRegistry` autoload.** Triple-LATER (CombatComponent target lookup, Attacking state target lookup, FarrSystem killer Node resolution).
  2. **`cause` taxonomy enum in Constants.** When ranged-attack causes land.
  3. **F2 overlay (Farr log).** Wave 2A produces F2 overlay's first non-test data point.
  4. **Friendly-fire policy.** DoD §10 open question; wave 2B's domain.
  5. **Killer Node passthrough via UnitRegistry.**

- **Coordination notes:**
  - `health_component.gd` extended ONLY in the augmenter block (wave 1A's death-emit logic unchanged).
  - `combat_component.gd` changed in exactly one line.
  - `farr_system.gd` got the `_on_unit_died` listener + `_ready` connect + `reset` re-arm.
  - `unit.gd`, `unit.tscn`, `event_bus.gd` UNTOUCHED per kickoff scope rules.

---

### v0.17.1 — Phase 2 Session 1 wave 2B (2026-05-04) — ai-engineer (UnitState_AttackMove + click-handler enemy-right-click + AttackMoveHandler)

- **Source code shipped in commit `aa429ef`** (bundled with ui-developer's wave 2C). The cross-agent contamination — ui-developer's `git add -A` swept up everyone's working-tree files — is the same Pitfall #7 (multi-agent shared-tree commit race) noted in the v0.17.0 entry. Code itself is correct; this §6 entry retros the wave-2B scope so future archaeologists can find it via `git log` even though the SHA name credits ui-developer's work. Files added by this wave: `attack_move_handler.gd`, `unit_state_attack_move.gd`, `test_attack_move_handler.gd`, `test_unit_state_attack_move.gd`. Files modified: `click_handler.gd` (team-aware right-click branch), `unit.gd` (register AttackMove state), `state_machine.gd` (one row in `_COMMAND_KIND_TO_STATE_ID`), `constants.gd` (`COMMAND_ATTACK_MOVE`, `STATE_ATTACK_MOVE`), `test_click_handler.gd` (+4 tests), `test_click_and_move.gd` (docstring update), `main.tscn` (AttackMoveHandler entry). The §6 v0.17.1 documentation entry + the §2 row updates land in this docs-only commit.

- **Three deliverables across two layers — input + state — but one unifying pattern: every command kind becomes a queue entry; the StateMachine's `_COMMAND_KIND_TO_STATE_ID` dispatcher maps it to the right state.** Wave 2B added two new command kinds (`COMMAND_ATTACK` was already in Constants from a parallel wave, but no input layer wrote it; `COMMAND_ATTACK_MOVE` is new) and the dispatcher now resolves both to their respective states. Adding a state entry to `_COMMAND_KIND_TO_STATE_ID` was the only edit to `state_machine.gd` — same shape as the existing `&"move" → &"moving"` row. Symmetric `Constants.STATE_ATTACK_MOVE = &"attack_move"` added for parity with other STATE_* constants.

- **The team-aware right-click branch reads `team` off the FIRST selected unit, not from a per-unit majority vote.** The MVP assumption is single-team selection (Iran selecting their own Iran units; box-select filters to TEAM_IRAN already). Reading `sel[0].team` is O(1), correct under the assumption, and falls back to `TEAM_NEUTRAL` if the field is missing (defensive, same duck-type pattern as `_resolve_unit_from_hit`). When spectator mode lands (Phase 6+) and cross-team selection becomes possible, this single-line read becomes a per-unit dispatch over each unit's own team.

- **`UnitState_AttackMove` solves resume-after-kill via the queue's FIFO ordering, not via state-local "remember target" plumbing.** When `_sim_tick` discovers an enemy: (1) `Unit.append_command(COMMAND_ATTACK_MOVE, {target: original_target})` pushes the resume to the BACK of the queue; (2) `CommandPool.rent` + `command_queue.push_front(attack_cmd)` puts the Attack command at the FRONT (the canonical AI-panic-insertion pattern per State Machine Contract §2.4 / §2.5); (3) `fsm.transition_to_next` pops the FRONT (Attack), stashes its payload on `ctx.current_command`, and dispatches into Attacking. When Attacking exits (target dead → transition_to_next), the queue's head is now the resume-AttackMove. Standard dispatch re-enters AttackMove with the original target. **No "remember target across states" code; no special "resuming" flag**. The mechanic falls out of FIFO discipline plus the kind→state_id map. The trickiest piece in the whole wave; documented exhaustively in the file header.

- **Why `UnitState_AttackMove` is a separate state, not a flag on `UnitState_Moving`.** Three reasons: (a) different `interrupt_level` — Moving is COMBAT-interruptible (worker walking past combat dives off-path on damage), AttackMove is NEVER (committed engagement movement). (b) The per-tick `SpatialIndex.query_radius_team` is non-trivial cost; gating it in a dedicated state means non-combat moves never pay for it. (c) Clean re-entry semantics — `_COMMAND_KIND_TO_STATE_ID[&"attack_move"] = &"attack_move"` makes the resume cycle a queue push, not a Moving-vs-AttackMove disambiguation. priority=15 sits between Moving's 10 and Attacking's 20: AttackMove preempts Moving (a worker shift-queueing an attack-move after a move inherits the more-committed behavior); Attacking preempts AttackMove.

- **Opposing-team derivation is a placeholder pending Phase 4 alliance-tables.** `_opposing_team(self_team)` is a binary Iran↔Turan switch with TEAM_ANY for neutral / future factions. When alliance-tables ship (Phase 4+), this becomes a lookup against a per-match alliance graph.

- **`AttackMoveHandler` is a sibling Node BEFORE `ClickHandler` in `main.tscn`.** Godot dispatches `_unhandled_input` to siblings in tree-document order; placing AttackMoveHandler first means it gets to consume the click via `set_input_as_handled` before ClickHandler sees the same press as a selection. Pending state machine: KEY_A flips `_attack_move_pending = true` (only with non-empty selection); next left-click consumes the input as the attack-move target; right-click or Escape cancels. Public `process_attack_move_hit(hit)` test seam mirrors ClickHandler's pattern. The pending flag clears regardless of dispatch success — A+click is a single-shot.

- **The integration test `test_right_click_on_unit_is_noop_integration` survived the team-aware branch unchanged.** Both kargars in `_spawn_kargar` default to `TEAM_NEUTRAL` (no explicit team override); same-team hits remain a no-op under the new logic. Updated only the docstring to reflect the new semantics ("right-click on a same-team unit must not issue any command"); the assertion (`fsm.current.id == &"idle"`) is preserved.

- **Live-game-broken-surface answers (Experiment 01 — refined):**
  1. *State/behavior that must work at runtime that no unit test exercises:* The Moving → AttackMove → Attacking → AttackMove resume cycle. Headless tests rebuild the SpatialIndex manually via `SpatialIndex._rebuild()`; the live game rebuilds during the `spatial_rebuild` phase each tick. **Click-ordering: AttackMoveHandler MUST consume the click before ClickHandler**; the BEFORE-ClickHandler placement in main.tscn is the only mechanism enforcing that. A future scene refactor that reorders the children would silently break attack-move (the click would propagate through to a regular selection click). Logged as Pitfall #5 candidate.
  2. *What headless tests cannot detect that the lead would notice in editor:* The `ENGAGE_RADIUS = 4.0` value's feel — too small and Piyade run past Turan that are 5m off-path; too large and they engage anyone vaguely on-screen. The "closest enemy" tie-breaking — when 5 Piyade attack-move and 3 Turan are in range, all 5 currently engage the closest one (pile-on); formation-engagement priority is a LATER item. Whether the resume-after-kill move feels "too eager" — the per-tick engage check fires the moment AttackMove re-enters.
  3. *Minimum interactive smoke test that catches it:* Lead box-selects 5 Iran Piyade, holds A, clicks across the map past 5 Turan Piyade. All 5 walk; encounter Turan in engage radius; engage and fight; kill; resume the original move. Lead also tests right-click on enemy (deliverable 2 wiring): selects Iran Piyade, right-clicks Turan Piyade, unit walks then attacks.

- **New Pitfalls candidates (for Experiment 01's Known Godot Pitfalls list):**
  - **Pitfall #5 (candidate): Sibling tree order is load-bearing for `_unhandled_input` consumption.** Two sibling nodes both implementing `_unhandled_input` and both potentially calling `set_input_as_handled` will have their effective priority determined by their position in the scene-tree document order. AttackMoveHandler MUST come before ClickHandler in main.tscn. **Mitigation idea**: a single "input dispatcher" node that fan-outs to handlers in an explicit priority order. Symmetric class to cb95d09's receiver-iteration-order bug — both are load-bearing dispatch orderings that look like "just a config detail" but break the feature silently when changed.

- **LATER items:**
  1. **Formation-aware engagement priority.** Phase 3+ should split fire among multiple AttackMove'd Piyade.
  2. **Stale-distance throttle for AttackMove engage queries.** Throttle to every-N-ticks once Phase 3 unit counts arrive.
  3. **A+click on enemy unit force-attack semantic.** Currently treated as ground-position attack-move.
  4. **InputDispatcher node.** Pitfall #5 mitigation.
  5. **Cursor change while attack-move pending (Phase 5 polish).**
  6. **`UnitRegistry` autoload (id → ref dict).** Inherited from waves 1A and 1B.
  7. **Cancel pending-attack-move on selection change.**

- **Coordination notes (cross-agent contamination guard):**
  - `unit.gd` was edited only to register `_UnitStateAttackMoveScript` alongside Idle/Moving/Attacking — wave 1A/1B's edits to other parts of the file are non-overlapping (committed in `81cf42a`).
  - `state_machine.gd` got one row added to `_COMMAND_KIND_TO_STATE_ID` (`&"attack_move" → &"attack_move"`) plus a brief comment.
  - `constants.gd` got `COMMAND_ATTACK_MOVE` and `STATE_ATTACK_MOVE` constants. `COMMAND_ATTACK` and `ENGAGE_RADIUS` were pre-existing.
  - `click_handler.gd` got a new branch in `process_right_click_hit`. Single block edit — the GroupMoveController route at the bottom of the function is unchanged.
  - `main.tscn` was the conflict surface — ui-developer's wave 2C and this wave both touched the file; final tscn has all entries cleanly merged: load_steps=13, AttackMoveHandler placed before ClickHandler.

---

### v0.17.4 — Phase 2 Session 1 (2026-05-01) — ai-engineer (BUG-02 fix: wire AttackMoveHandler in main.tscn)

- **BUG-02 fixed.** `main.tscn` now declares `AttackMoveHandler` as a sibling Node under `Main`, positioned IMMEDIATELY BEFORE `ClickHandler` in the .tscn so reverse-tree-order `_unhandled_input` delivery reaches AttackMoveHandler first when A+click is pending. Without the wiring, A+click was a silent no-op — the script existed and was preloaded by tests, but no instance hosted its `_unhandled_input` at runtime. Root cause: the Phase 2 wave 2B agent (`ai-eng-attack-input`) authored the script and the .tscn edit, but Deviation 02's parallel-agent commit-staging race left the .tscn change out of the wave's commit. Pitfall #5 (sibling tree-order is load-bearing) caught it.

- **Tree order check.** `AttackMoveHandler.get_index() < ClickHandler.get_index()` is now asserted in two integration tests (`test_main_tscn_attack_move_handler_before_click_handler` and `test_pitfall_5_attack_move_handler_before_click_handler_standalone`). Both were `pending(...)` waiting on this fix; both now run with full assertions.

- **Scene file mechanics.** Added one `[ext_resource]` declaration (`id="11_attackmove"`, the next free integer after wave 2C's 10_attackrange) and one `[node]` block. `load_steps` bumped 12 → 13 to match the new ext_resource count.

- **Test count: 716 → 718.** Two BUG-02 pending tests flipped to passing assertions. No new tests added — the existing regression locks were already in place from wave 3.

- **Files owned by this fix:** `game/scenes/main.tscn` (the wiring), `game/tests/integration/test_phase_2_session_1_combat.gd` (flipped 2 BUG-02 pending tests + updated the BUG-02 status comment block at the top of the file).

---

### v0.17.2 — Phase 2 Session 1 wave 3 (2026-05-01) — qa-engineer (Integration tests for combat flows)

- **36 new integration tests in `game/tests/integration/test_phase_2_session_1_combat.gd`.** Covers 9 flows: single-attack, range+cooldown, right-click-on-enemy, attack-move FSM, Farr drain worker-killed-idle, HealthBarsOverlay compute_bar_entries, AttackRangeOverlay+F4, cross-feature smoke (main.tscn structure), and pitfall regressions #1–#5. Test count: 675 → 711. Passing: 672 → 706 (34/36 new tests pass; 2 are intentional regression locks for BUG-02).

- **BUG-01 found and locked: `UnitState_Attacking._sim_tick` never calls `combat._sim_tick(dt)`.** `sim_phase(&"combat")` fires with no listeners; `UnitState_Attacking._sim_tick` calls `combat.set_target()` when in range but never drives the CombatComponent tick. Damage never fires through the production FSM chain. Test `test_bug01_combat_sim_tick_not_driven_by_fsm` asserts the current broken behavior (HP unchanged after 10 FSM ticks) and will turn green when the fix lands. Fix: `UnitState_Attacking._sim_tick` must call `combat._sim_tick(dt)` after `combat.set_target()` in the in-range branch. Owner: gameplay-systems.

- **BUG-02 locked: `AttackMoveHandler` absent from `main.tscn`.** Wave-2B shipped `attack_move_handler.gd` but Deviation 02 (parallel-agent commit-race) caused the ui-developer's `git add -A` to sweep up wave-2B code without the main.tscn node entry. Tests `test_main_tscn_attack_move_handler_before_click_handler` and `test_pitfall_5_attack_move_handler_before_click_handler_standalone` will fail until the node is added. Fix: Add `AttackMoveHandler` node BEFORE `ClickHandler` in main.tscn with `script = res://scripts/input/attack_move_handler.gd`. Owner: ai-engineer.

- **BUG-03 found and locked: No `dying` state registered for combat units.** `unit.gd._ready()` registers `idle/moving/attacking/attack_move` but not `dying`. When `unit_health_zero` fires, `StateMachine._on_unit_health_zero()` push_errors and returns without calling `queue_free`. Killed units stay valid in the scene tree — `UnitState_Attacking._sim_tick()`'s `is_instance_valid(_target)` check never triggers, so attackers never transition to idle after killing a target. Test `test_bug03_no_dying_state_unit_stays_valid_after_death` asserts units are still valid after lethal damage (current broken behavior) and will turn green when fixed. Fix: Add `UnitState_Dying` to `unit.gd` registration that calls `ctx.queue_free()`. Owner: gameplay-systems.

- **Integration test strategy: direct `CombatComponent._sim_tick` drive for damage tests (BUG-01 workaround).** Because the EventBus.sim_phase chain doesn't reach CombatComponent (BUG-01), Flow 1 (single-attack HP math) and Flow 2 (cooldown timing) tests drive combat directly using `SimClock._is_ticking = true; combat._sim_tick(dt); SimClock._is_ticking = false` — the same pattern as `test_combat_component.gd`. FSM-level tests (Flow 3, 4) use `_advance(n)` via `SimClock._test_run_tick()`. The two helpers must not be mixed in the same test function — `_test_run_tick()` fires `sim_phase(&"movement")` which triggers `UnitState_Attacking._sim_tick()` → `combat.set_target()`, resetting the cooldown to 0 and invalidating cooldown assertions.

- **GDScript lambda capture gotcha (Pitfall #6 candidate): primitive ints captured by value.** Test `test_pitfall_4_no_reentrant_unit_died_on_worker_death` originally used `var unit_died_count: int = 0` with an `EventBus.unit_died.connect(func(...) -> void: unit_died_count += 1)` lambda. The increment had no effect — GDScript lambdas capture primitive `int` by value; the outer `unit_died_count` never changed. Fixed by using `var unit_died_events: Array = []` and `unit_died_events.append(uid)`. Array is passed by reference; mutations inside the lambda are visible outside. **Rule**: signal-counting lambdas must use `Array.append`, not `int += 1`.

- **Files owned by this wave:** `game/tests/integration/test_phase_2_session_1_combat.gd` (new, 36 tests). No production code modified.

---

### v0.14.4 — Phase 1 Session 2 wave 2A (2026-05-04) — ui-developer (Control groups + double-click-select-of-type)

- **`ControlGroups` is an autoload, ordered AFTER `SelectionManager`.** Control groups outlive any single scene transition (UI overlay, pause menu, debug panel) and need a single global instance accessible to any future hotkey rebinder, replay player, or panel UI. Mirrors the SelectionManager pattern. The `[autoload]` order matters at parse time — at `ControlGroups._ready`, `SelectionManager` must already be parsed so `SelectionManager.selected_units` returns live data. The kickoff explicitly specified this ordering.

- **Double-tap detection uses `SimClock.tick`, NOT wall-clock.** L5 lint forbids `Time.get_*_msec` / `OS.get_unix_time` in gameplay code; the project's replay-determinism guarantee depends on every timer reading the sim clock. 350ms (kickoff guess) at 30Hz is 10.5 ticks; we round to `DOUBLE_TAP_TICKS = 10`. The same constraint applies to `DoubleClickSelect` — `DOUBLE_CLICK_TICKS = 9` (~300ms). Both constants are knobs on the public class for easy tuning by lead during interactive smoke testing.

- **Double-click detection is signal-driven (kickoff option b), not input-event sibling-ordered (option a).** Both options are in the kickoff doc. Option (a) — sibling-order a second left-click handler after BoxSelectHandler — is fragile because BoxSelectHandler claims the left-press first via `set_input_as_handled`, and a second listener would have to coordinate with the box-select's click-vs-drag arbitration. Option (b) subscribes to `EventBus.selection_changed`: whenever the payload is exactly one unit (single-target select), compare against the previously-armed unit + tick. The signal-based approach is INDEPENDENT of which input layer routed the click — ClickHandler, BoxSelectHandler, or any future single-click producer all funnel through SelectionManager and emit the same broadcast. This makes the double-click detector a pure observer; it never touches raw mouse events.

- **Multi-select payload disarms the double-click tracker.** Without this, a sequence like "box-select 5 → click one of them → click that one again" would false-fire as a double-click on the second single-click (because the prior `_armed_unit_id` was set during a multi-select-then-single-select pattern). The fix: any selection_changed payload with size != 1 (whether multi-select OR an empty deselect_all) clears the armed state. Single-target selections are the only ones that arm or fire.

- **`center_on(Vector3)` added to CameraController as a single-line public method.** The kickoff explicitly authorized this scoped addition; not a refactor. Implementation: clamp the input position via the existing `clamp_to_bounds` helper (Y forced to 0 — the camera target rides on the ground plane), set `target_position`, and call `_apply_transforms()` so the change takes effect immediately. The clamp prevents the camera from snapping outside the map — important when the player double-taps a control group that's near the map edge. Animated centering (lerp toward target over a few frames) is a future polish; snap is the MVP.

- **No InputMap entries for Ctrl+1..9 / 1..9.** The control groups autoload reads keycodes directly off `InputEventKey` (via `event.keycode` and `event.ctrl_pressed`). Same pattern the camera controller uses for WASD (`Input.is_key_pressed(KEY_W)`). Adding nine InputMap actions per digit, each with a Ctrl-modifier variant, would be 18 entries for a 1:1 mapping with the digit-keycode. Direct keycode reads are simpler and survive the absence of an action registration without breaking.

- **`recall(n)` is a no-op on unbound or empty groups (does NOT clear selection).** The kickoff smoke-test sequence implies recall is a one-way restore: "Lead deselects, hits 1, sees the same 3 selected." If recall on an unbound group cleared the selection, the player's behavior would surprise them — pressing 5 for a group they never bound shouldn't lose their current selection. The defensive no-op matches the convention in StarCraft 2 and AoE2.

- **Centroid computation skips freed group members and ignores Y.** `_centroid_of_group(n)` filters via `is_instance_valid` (lazy pattern, mirrors SelectionManager) and computes mean XZ of live members. Y is set to 0 because the camera target_position rides on the ground plane (per `_apply_transforms`'s pitch math). Empty groups (post-filter) yield `Vector3.ZERO` which the caller treats as "no center fires" — defensive, doesn't drag the camera to origin spuriously.

- **`select_visible_of_type` always includes the target unit.** Defensive against a 1-frame projection edge case where the target's `is_position_behind` flickers true between click-time and signal-handler-time. The player's intent is clear: "select all of this type starting from THIS one." Hardcoding the target's inclusion makes the contract robust to projection noise.

- **Lazy-resolved CameraController via duck-typed scene walk.** `ControlGroups` doesn't `preload` `camera_controller.gd` because that would couple the autoload to a specific class file path. Instead, on first centering call, it walks `get_tree().current_scene` looking for a node with the unique CameraController surface (`has_method(&"center_on")` AND `target_position in node`). Same duck-typing pattern `box_select_handler.gd::_resolve_click_handler` uses for `process_left_click_hit`. The `set_camera_target(stub)` test seam injects a RefCounted stub.

- **37 new tests** (+23 in `test_control_groups.gd`, +14 in `test_double_click_select.gd`). Coverage: bind/recall correctness, freed-member filtering, double-tap timing window (in/out, same-key/cross-key), centroid math (single, multi, with-frees, empty), reset semantics, synthetic InputEventKey dispatch (press/release, echo, non-digit, Ctrl-vs-bare). Double-click coverage: type-filter, on-screen filter, replace-prior-selection, null/freed targets, signal-driven arming/disarming, multi-select disarms, deselect_all resets, reset wipes state. Headless GUT: all pass.

- **Live-game-broken-surface answers (Experiment 01 — refined):**

  *Control groups:*
  1. *What state/behavior must work at runtime that no unit test exercises?* Autoload parse-time ordering — if `ControlGroups` were registered BEFORE `SelectionManager` in `[autoload]`, this autoload's `_ready` would fail to read `SelectionManager.selected_units` (autoload-during-autoload-init order is undefined in Godot 4). Tests register the script as a fresh `.new()` instance with a manual SelectionManager seed; live mode runs through the `project.godot` order. Camera resolution happens lazily on first double-tap — at boot, no camera resolves; if the user double-taps before the camera rig has finished `_ready`, the autoload silently no-ops (logged via `DEBUG_LOG_GROUPS`). InputEventKey.echo state — held keys auto-repeat at the OS level; without the `event.echo` filter, holding `1` would spam recall every frame. Tests synthesize echo=false events; live mode might not.
  2. *What can a headless test not detect that the lead would notice in the editor?* Double-tap timing FEEL — `DOUBLE_TAP_TICKS = 10` is the kickoff's 350ms guess minus integer rounding. Lead may want 7 (faster — punishes deliberate-but-slow players) or 13 (slower — accommodates trackpad users). Whether Ctrl+1+2 fires ambiguously when the player is sloppy (each key event is dispatched separately by the engine; Ctrl+1 binds, then Ctrl+2 binds — both succeed, but the player may not have intended both). Whether camera centering should be a snap (current MVP) or a smooth tween (animated, more polished). The visual cue when a group is bound vs. recalled (no UI feedback yet — out of scope per kickoff "Control group display in HUD — Phase 2+").
  3. *Minimum interactive smoke test:* Lead boxes 3 kargars, hits Ctrl+1, deselects via empty-space click → no rings. Lead hits 1 → same 3 select (gold rings). Lead hits 1 again within ~350ms → camera centers on them. Lead binds a different selection to Ctrl+2; toggles 1 ↔ 2 — both work, neither corrupts the other. Lead hits 5 (unbound) → no change to current selection. Lead binds Ctrl+1 to a 5-unit group, then box-select-replaces with 1 unit, then hits 1 → group restores to 5.

  *Double-click select-of-type:*
  1. *What state/behavior must work at runtime that no unit test exercises?* `Camera3D.unproject_position` runs against the live camera with the unit's actual `global_position`; headless tests inject a projector closure. The viewport-rect filter compares against `vp.get_visible_rect().size` which depends on window resize state. Subscribers see exactly one emission per `SelectionManager.select_only` call — if a future "select_many" fast-path lands and emits twice for one logical operation, the detector would false-fire (gated by the size==1 check, but worth knowing).
  2. *What can a headless test not detect that the lead would notice in the editor?* Double-click timing FEEL — `DOUBLE_CLICK_TICKS = 9` (~300ms). Lead may want 7 or 12. Whether the visible-on-screen filter does the right thing at extreme zoom (zoomed all the way out, all 5 kargars are tiny dots in viewport center; zoomed all the way in, only 1 may be visible). Whether the player's intent was "deliberate two clicks on the same unit" vs. "double-click for select-of-type" — UX call. The 1px viewport-edge tolerance: at 1280×720 vs. 1920×1080 vs. 4K, units exactly at the edge may flicker in/out of selection.
  3. *Minimum interactive smoke test:* Lead double-clicks one kargar with all 5 visible → all 5 selected. Lead pans so only 2 are on-screen, double-clicks one → only those 2 selected. Lead clicks one kargar, waits 1 second, clicks again → single-select both times (window expired). Lead box-selects 3, then double-clicks the 4th outside the box → all 5 same-type selected (multi-select prior disarmed; double-click-only fires after a fresh single-select pair).

- **LATER items surfaced in v0.14.4:**
  1. **Control group display in HUD (Phase 2+).** A row of numbered icons at screen-bottom showing which groups are bound, with a click-to-select binding. Out of scope per kickoff; pencil in for selection polish when the player has more than 5 units to manage.
  2. **Shift+N to ADD recalled group to current selection.** Out of scope per kickoff §2 (2). Easy add: a `recall_with_modifier(n, shift_held)` variant that calls `add_to_selection` instead of `deselect_all + add_to_selection`. Defer until lead-test confirms it's missed.
  3. **Animated camera centering (lerp instead of snap).** Current MVP snaps `target_position`. A tween over ~0.15s would feel less jarring at high zoom levels; the existing `_apply_transforms` already runs every frame so this is just an interpolated `target_position` setter. Defer to camera polish.
  4. **Triple-click → select-all-of-type-globally.** Out of scope per kickoff "Out of scope here: Triple-click (no special meaning)." Pencil in for Phase 3+ when there are enough units that on-screen-only feels limiting.

---

### v0.8.0 — Phase 0 Session 4 wave 2 (2026-05-01) — qa-engineer

- **`class_name MatchHarness` removed; callers use preloaded script ref.** The wave-2 kickoff specified `class_name MatchHarness extends RefCounted`. The Godot 4.6.2 class_name registry race (documented in §6 v0.4.0) bites RefCounted-based scripts the same as Node-based ones when GUT collects test files before the class_name registry populates. Removing the class_name declaration and switching to the established project pattern (preload the script, call `.new()`, then call `start_match()`) resolves the compile error. The Testing Contract §3.1 specified `static func new(seed, scenario) -> MatchHarness` as the factory shape; with class_name removed, a static factory can't reference its own type. Replaced with `func start_match(seed, scenario)` on a freshly `.new()`'d instance. Test callers define a `_make(seed, scenario)` helper to keep the line count low. No behavior change; contract behavior is preserved exactly.

- **`_test_set_farr` is off-tick, not on-tick as the Testing Contract worked example implied.** Testing Contract §3.3 showed `_test_set_farr` called inside `advance_ticks`. The initial linter-generated harness implemented this by connecting a one-shot lambda to `EventBus.sim_phase` and driving an internal `_test_run_tick()`. This caused "Cannot disconnect from 'sim_phase': callable is null" errors because GDScript closures and self-referential lambda disconnects interact badly in this context. Simplified to a direct off-tick write to `FarrSystem._farr_x100` (same pattern as `FarrSystem.reset()`) with a synthetic `EventBus.farr_changed` emit. The semantic is unchanged — test code sets Farr to a specific value without going through `apply_farr_change`'s on-tick assert. The side effect is `_test_set_farr` no longer advances `SimClock.tick` by 1; tests that need a specific tick count can be explicit about it. The Testing Contract §3.3 example note "must run inside a tick" is advisory context from the worked example, not a hard requirement for the method itself.

- **`FarrSystem.reset()` added cross-domain.** The wave-2 kickoff doc explicitly authorized this: "gameplay-systems' wave-1 retro flagged that reset() not added in this skeleton. You're MatchHarness — you need it. Add it (cross-domain, but small; document in §6)." Added `reset()` to `farr_system.gd` that reads the starting_value from BalanceData (same logic as `_load_starting_value_from_balance_data`), writes `_farr_x100`, and emits a synthetic `farr_changed` with reason `"harness_reset"`. The off-tick emit is intentional and documented in the source comment.

- **`MatchHarness.start_match` vs `_setup` naming.** The linter auto-generated `static func create(seed, scenario)` calling a private `_setup()`. After removing the static factory (class_name removal), renamed `_setup()` to be called from `start_match(seed, scenario)`. The public API name `start_match` better communicates intent than `create` — it's not constructing data; it's starting a match simulation.

- **`Scenarios.CATALOG` is a data-only dictionary, not a class with methods.** The initial Scenarios design had a static `apply(scenario_key, harness)` method. The linter simplified it to a `const CATALOG: Dictionary` with string keys → Dictionary of override values. MatchHarness._setup reads from the catalog dict and applies overrides directly. This is simpler and doesn't require Scenarios to know about the MatchHarness type. Adding a new scenario is a one-line Dict entry — no method needed.

- **`tests/integration/test_match_harness.gd` added alongside `tests/integration/test_determinism.gd`.** The linter created `test_match_harness.gd` in the integration directory in parallel. Both files exercise the harness: `test_match_harness.gd` covers the API surface (25 tests); `test_determinism.gd` covers the empty-match determinism guarantee (3 tests). The `tests/unit/test_match_harness.gd` tests overlap slightly — this is intentional redundancy for robustness. Test counts: 199 before wave 2 → 250 after (51 new tests; 1 intentional Pending remains from ui-developer's HUD defensive-fallback test).

- **What did not ship in wave 2 (intentionally out of scope per kickoff):** `spawn_resource_node` test helper (Phase 3). `MatchLogger` NDJSON writer (Phase 6). AI-vs-AI batch runner (Phase 6). `GameRNG` autoload wiring in harness (engine-architect; harness uses `seed()` on global RNG as fallback with TODO comment). Hot-reload of BalanceData mid-test (Phase 5+). Real unit/building spawn in `spawn_unit` / `spawn_building` (Phase 1+ when scenes exist — stubs return null with push_warning).

---

### v0.17.3 — Phase 2 Session 1 wave 3 bug fixes (2026-05-03) — gameplay-systems (BUG-01 + BUG-03)

Backfilled retroactively per arch-reviewer-phase-2-s1's F-1 finding.

- **BUG-01 fix — drive `combat._sim_tick(dt)` from `UnitState_Attacking._sim_tick`.** Mirrors `UnitState_Moving`'s `_movement._sim_tick(dt)` pattern: the state IS the per-tick driver for the component until a phase coordinator ships. The single-line addition (`unit_state_attacking.gd` in-range branch, after `combat.set_target(...)`) closes the gap where `EventBus.sim_phase(&"combat")` fired with no listeners. Without this fix, damage never fired through the production FSM chain — qa wave 3's integration tests (`test_bug01_combat_sim_tick_drives_damage_via_fsm`) caught the gap by writing a 10-tick advance through the real EventBus chain and asserting HP decreased; the original "broken-behavior assertion" was flipped to the fixed-behavior assertion when the fix landed.

- **BUG-01 ARCHITECTURAL CONSEQUENCE — combat damage now fires during the `&"movement"` phase, not the `&"combat"` phase.** Sim Contract §2 specifies a 7-phase canonical order: `input → ai → movement → spatial_rebuild → combat → farr → cleanup`. With combat reached via `Unit._on_sim_phase(&"movement")` → `fsm.tick()` → `UnitState_Attacking._sim_tick()` → `combat._sim_tick()`, damage application happens BEFORE `spatial_rebuild` runs each tick. Per Sim Contract §2 also: *"combat must see post-movement positions"* — combat now sees pre-movement positions because it runs IN movement. **At session-1's 15-unit cap with no ranged units, the impact is invisible.** When the CombatSystem phase coordinator lands (LATER), it subscribes to `&"combat"` and iterates registered CombatComponents, the drive call moves out of `UnitState_Attacking._sim_tick`, and §2 phase-order is restored. **Tagged as a known drift in `unit_state_attacking.gd:30-36` and `combat_component.gd:27-33`.** Phase-order audits at later phases will catch this if it slips out of the LATER queue.

- **BUG-03 fix — register `UnitState_Dying` in `unit.gd._ready`.** New file `game/scripts/units/states/unit_state_dying.gd`: `class_name UnitState_Dying`, id=`&"dying"`, priority=100 (top), interrupt_level=NEVER. `enter(prev, ctx)` calls `ctx.queue_free.call_deferred()` — the deferred path is load-bearing because `enter()` runs inside `StateMachine._apply_transition`, which is itself called inside `_on_unit_health_zero` (a signal handler firing inside another component's `_sim_tick`). A direct `queue_free()` would invalidate `current` while the StateMachine was still using it. Registered alongside Idle/Moving/Attacking/AttackMove via the existing idempotent register chain. Without registration, `StateMachine._on_unit_health_zero` push_errored on the missing `&"dying"` state and units stayed alive in the scene tree as zombie corpses for attackers to continue engaging. qa wave 3's integration test `test_bug03_dying_state_frees_unit_after_lethal_damage` asserts the unit is freed after lethal damage + 2 process_frames (see Pitfall #8 candidate below).

- **New Pitfall #8 candidate — `Node.queue_free.call_deferred()` is double-deferred.** Outer `call_deferred` queues `queue_free` for end-of-frame; `queue_free` itself queues actual deletion for end-of-next-frame. Tests verifying "unit freed after Dying.enter" need 2 `await get_tree().process_frame` calls (unit-test variant) or 3 (integration-test variant — runner has more pending deferreds). Surfaced during BUG-03 fix verification; godot-code-reviewer's wave-close audit confirmed this is real and load-bearing — promoted to Known Pitfalls list per Experiment 01's growing-list pattern.

- **Test strategy — direct `take_damage_x100(20000, ...)` for BUG-03 regression test.** The regression test originally drove damage through 20 `_combat_tick` iterations, but cooldown semantics meant that path landed only 1-2 attacks per 20 ticks (insufficient for kill at 100 HP). Lead's edit (committed as part of `47680cd`) switched to direct `health.take_damage_x100(20000, _iran, &"melee_attack")` — the death-preempt chain (HealthComponent → unit_health_zero → StateMachine → UnitState_Dying.enter → queue_free.call_deferred) does not depend on HOW damage was dealt. Same chain exercised, simpler test setup.

- **Process: lead-proxy commit (Deviation 01 second occurrence).** Wave-3 fix dispatch agents fell into the verification-loop pattern AGAIN despite explicit anti-loop guardrails in their brief. Lead committed BUG-01 + BUG-03 as `47680cd` on their behalf (gameplay-bug-fixes' authorship credited in commit body); the agent self-corrected and shipped the post-commit follow-ups (`6590a16` test_phase_2_session_1_combat.gd + test_unit_state_dying.gd refinements; `4ff8658` BUILD_LOG retros). Logged in `docs/PROCESS_EXPERIMENTS.md` as continuation of Deviation 01.

- **LATER items surfaced:**
  1. **CombatSystem phase coordinator** — closes the §2 phase-order drift documented above.
  2. **`set_target` idempotency** — wave-3 retro flagged that the BUG-01 fix's per-tick `set_target` call would reset cooldown every tick. Surfaced as **BUG-04**, fixed in v0.17.5.
  3. **`UnitRegistry` autoload** — closes target-lookup cost from O(N) tree-walk to O(1) when N>~100. Triple-LATER (CombatComponent, UnitState_Attacking, UnitState_AttackMove all surface this).

---

### v0.17.5 — Phase 2 Session 1 post-wave-3 (2026-05-03) — lead (BUG-04 fix: set_target idempotency)

Backfilled retroactively per arch-reviewer-phase-2-s1's F-2 finding.

- **BUG-04 — `CombatComponent.set_target` was not idempotent on same-target re-entry.** The BUG-01 fix (v0.17.3) wired `UnitState_Attacking._sim_tick` to call `combat.set_target(target.unit_id)` on EVERY in-range tick. `set_target` unconditionally reset `_attack_cooldown_ticks = 0`. So cooldown reset to 0 every tick → damage fired every tick (30 atk/sec at 30 Hz instead of the 1.0 atk/sec the cooldown formula intends).

- **Why the integration test missed it.** `test_bug01_combat_sim_tick_drives_damage_via_fsm` asserted on END behavior (HP decreases) without checking the rate. Combat resolved correctly in terms of DAMAGE FLOWING through the chain — just 30x faster than designed. The unit-level cooldown tests passed because they drove `combat._sim_tick` directly without the FSM, so per-tick `set_target` never fired in those test paths. Two test layers, neither caught the rate.

- **Fix.** `set_target` now early-returns when `_target_unit_id == unit_id_value`:

  ```gdscript
  func set_target(unit_id_value: int) -> void:
      if _target_unit_id == unit_id_value:
          return  # Idempotent — same target preserves cooldown.
      _target_unit_id = unit_id_value
      _attack_cooldown_ticks = 0
  ```

  Same-target re-call preserves cooldown. Genuine target change still resets to 0 — the "single-tick attack on engagement" RTS expectation is preserved. Setting `-1` while already `-1` is also no-op (no-target stable state).

- **Two regression tests** in `test_combat_component.gd`:
  - `test_set_target_idempotent_does_not_reset_cooldown` — first attack fires (cooldown→30), then `set_target(same_id)` must not change cooldown.
  - `test_set_target_new_target_resets_cooldown` — `set_target(new_id)` must reset to 0 and store the new id.

- **Future caller contract.** Anyone using `set_target` as a "force-fire-immediately" primitive must use `set_target(-1)` first to clear, then `set_target(target_id)`. The idempotency comment in source documents this. Otherwise the `same id` path skips the cooldown reset.

- **Process: lead-deviation (third small one this session).** Surfaced by `gameplay-bug-fixes`'s post-wave-3 retro report; lead made the 3-line fix + 2 regression tests directly rather than spawning yet another agent given the verification-loop and commit-race patterns recurring. Logged in `docs/PROCESS_EXPERIMENTS.md` Deviation 01 (second occurrence) and `47680cd`/`fd7b07b` commit bodies.

---

### v0.17.6 — Phase 2 Session 1 post-live-test (2026-05-04) — ai-engineer (BUG-05 fix: click-tolerance fallback)

- **BUG-05 — right-click on a Turan Piyade silhouette walked the units past the unit instead of attacking it.** Lead's live-test surfaced a UX bug: clicking on the rendered silhouette of a unit can hit terrain when the click lands on the visual mesh but outside the (smaller) collision pad. The Piyade visual is `0.5×0.7×0.5` while its collision shape is `0.4×0.55×0.4` — a click on the visual edge falls through to the ground beneath. `click_handler.gd::_resolve_unit_from_hit` returned null, so the click was treated as a Move command per the existing terrain-fallthrough behavior. Console proof from the live-test: `RIGHT: move command target=(-0.07, -0.000001, 19.96) selected=5` — the `Y=-0.000001` confirms terrain hit at the spawn position of a Turan unit.

- **Fix — robust click-tolerance fallback (lead's option (2)).** When `_resolve_unit_from_hit` returns null AND the hit dictionary carries a `position`, `click_handler` now follows up with `SpatialIndex.query_radius(hit_pos, Constants.CLICK_TOLERANCE_RADIUS)` and walks each agent's parent through the same `_is_unit_shaped` duck-type filter. The closest unit by XZ-distance is treated as if it had been the raycast collider. Routes through the existing team-check / selection logic — no new branching at the call sites in `process_left_click_hit` / `process_right_click_hit`, just a `if unit == null: unit = _resolve_unit_from_tolerance(hit)` line each. The previous lead-decision (option 1: enlarge collision pads) was rejected because changing physics shapes can cascade into pathfinding clearance, NavigationObstacle3D bake parameters, and stacking behavior — the input-side fallback is contained to one input file and one constant.

- **New constant `Constants.CLICK_TOLERANCE_RADIUS = 1.5`.** Sized as max-mesh-half-extent (~0.35 for Piyade) plus a forgiveness margin. Larger values risk "ghost target" clicks in dense engagements where a click on empty ground between units accidentally targets the closest one; smaller values shrink the rescue zone below useful. Lives in a new `# === INPUT ===` section of `constants.gd`. Structural — UX parameter, not a balance knob — so it lives in Constants, not BalanceData.

- **Why the fallback uses XZ-projection (not 3D distance).** Per `docs/SIMULATION_CONTRACT.md` §3.1, the SpatialIndex is a flat grid over the XZ plane; Y is ignored. The closest-unit selection in `_resolve_unit_from_tolerance` uses the same projection so distance comparisons are consistent with the index it queries. A unit elevated 100m on Y is still "near" a click on the ground beneath it for tolerance purposes, but in MVP all units sit at Y≈0.5 so this is a formal-correctness note, not a behavioral one.

- **Seven new tests** in `test_click_handler.gd` cover the rescue cases AND the regression guards:
  - `test_right_click_near_enemy_pushes_attack_command_via_tolerance` — primary fix verification.
  - `test_left_click_near_friendly_selects_via_tolerance` — same fallback, left-click branch.
  - `test_right_click_far_from_any_unit_still_issues_move` — far-click regression guard (move still works).
  - `test_left_click_far_from_any_unit_still_deselects` — far-click left-side regression (deselect still works).
  - `test_right_click_directly_on_unit_no_regression_from_tolerance` — direct-hit path unchanged.
  - `test_right_click_tolerance_picks_closest_when_two_units_in_radius` — XZ-distance tie-break correctness.
  - `test_click_tolerance_radius_is_configurable_via_constants` — placing a unit just outside the radius asserts the fallback declines (the radius constant is the gate, not a magic number).

- **Test count: 718 → 725 passing (+7).** All asserts pass; lint clean across L1-L5. No existing tests changed behavior.

- **Live-game-broken-surface answers (per Experiment 01 discipline):**
  1. **What state must work at runtime that no unit test exercises?** The fallback only fires when the SpatialIndex is populated AND the production raycast actually hits terrain. Tests substitute synthetic hit dicts; the live-test path requires a real Camera3D + PhysicsServer + populated SpatialIndex. Smoke test: spawn 1 Iran + 1 Turan, right-click within 1.5 units of the Turan but on terrain — should issue Attack.
  2. **What can a headless test not detect that the lead would notice in the editor?** The radius value (1.5) is a feel-tuning parameter — only live-clicking will tell whether 1.5 is too generous (ghost targets) or too tight (still feels broken). Tests assert behavior at the boundary; the boundary itself is a UX call.
  3. **Minimum interactive smoke test.** Boot main scene → right-click on the visual silhouette of an enemy from across the map — Attack issues, units engage. Right-click on empty ground 5m from any unit — Move issues, units walk to the click point. Both must work in the same session.

- **Known Godot Pitfalls list — no new entries.** This bug is an input-feel UX issue (mesh-vs-collision divergence), not a Godot engine pitfall. The existing list (mouse_filter, FSM tick, camera basis, signal re-entrancy, sibling tree-order, double-deferred queue_free) is unchanged.

- **LATER items surfaced (none new, one reinforced):**
  1. **Selectable-vs-collision parity in unit scenes** — once art ships, collision shapes should match visual silhouettes within a tight margin so the tolerance fallback isn't the load-bearing rescue path. The fallback then becomes a "small-error forgiveness" feature instead of a "click-misses-the-unit" workaround.

---

### v0.17.7 — Phase 2 Session 1 post-live-test (2026-05-01) — ai-engineer (BUG-06 fix: drive movement._sim_tick from Attacking out-of-range)

- **BUG-06 — right-clicking an out-of-range enemy issued the Attack command but units stayed put.** Live-test surfaced: an Attack command lands on the FSM (state transitions to Attacking) but if the target is outside `attack_range` (1.5), the unit never walked toward it. Console proof from the live-test session: `RIGHT: attack command target_unit_id=13 selected=5` repeated three times with no motion; only after the lead manually right-clicked terrain to move the units adjacent to the enemy did the next Attack command land damage.

- **Fix — drive `_movement._sim_tick(dt)` after `request_repath` in the out-of-range branch.** Same architectural shape as BUG-01: `UnitState_Attacking._sim_tick` was driving an *advisory* call (`request_repath` schedules a path) but not the *mechanical* one (`_movement._sim_tick` polls the result and steps `global_position`). The in-range branch already drove `combat._sim_tick` (BUG-01's fix); the out-of-range branch needed the parallel drive for movement. One-block change in `unit_state_attacking.gd::_sim_tick`, gated on the existing `_movement != null` check, with a `has_method(&"_sim_tick")` guard mirroring the in-range combat drive so test stubs don't crash.

- **Why prior tests missed it.** `test_bug01_combat_sim_tick_drives_damage_via_fsm` (the closest analog) spawns the target at `Vector3(1.0, 0, 0)` — within the 1.5 attack_range — so Attacking's first tick hits the in-range branch. The out-of-range walk-toward branch in `Attacking._sim_tick` is never exercised through the FSM chain by the prior wave-3 integration tests. Unit tests for MovementComponent stub or call directly, so the missing drive call doesn't surface there either.

- **One regression test added** in `test_phase_2_session_1_combat.gd`: `test_bug06_attacking_drives_movement_when_out_of_range`. Spawns Iran at origin and Turan at `Vector3(5, 0, 0)` (well outside attack_range), issues `replace_command(COMMAND_ATTACK)`, advances 80 ticks via the real EventBus chain, and asserts (a) the attacker's `global_position.distance_to(start)` exceeds 0.5, (b) distance-to-target decreased by ≥ 0.5, (c) HP on the target dropped after closing the gap (full walk-then-attack chain end-to-end).

- **The "instant scheduler" stub used for the regression test.** The default `MockPathScheduler` resolves on `requested_tick + 1`. Attacking's docstring explicitly chooses to re-issue `request_repath` every tick to track a moving target, which cancels the prior PENDING request before the mock's resolution boundary. The production `NavigationAgentPathScheduler` resolves SYNCHRONOUSLY (calls `NavigationServer3D.map_get_path` and returns READY same tick), so live-game motion works fine. To exercise the same shape in headless tests without a baked navmesh, the regression test instantiates a tiny in-file `_InstantPathScheduler` subclass of `IPathScheduler` that returns READY immediately with a straight-line two-waypoint path. This keeps the production code change minimal (no per-tick-throttle refactor) and matches the live-scheduler contract — the test isn't lying about what production does. LATER: the Phase 3+ scheduler-throttling refactor (per-N-ticks repath when target hasn't moved much) will obviate the need for the instant stub.

- **No changes to `UnitState_AttackMove` despite the parallel.** `unit_state_attack_move.gd::_sim_tick` (line 174) already drives `_movement._sim_tick(dt)` unconditionally on every tick — same drive call BUG-06 just added to Attacking — because attack-move's primary mode IS movement. AttackMove issues `request_repath` once in `enter()` (not per-tick like Attacking), so the mock-resolution semantics that bit Attacking don't apply there. Verified by reading the file; no fix needed.

- **Test count: 725 → 726 passing (+1).** All asserts pass; lint clean across L1-L5. No existing tests changed behavior. `test_bug06_attacking_drives_movement_when_out_of_range` end-state: attacker moved 3.58 units (start=(0,0,0) → ~(3.58, 0, 0)) closing 5.0 → 1.42 distance to target; target HP dropped 10000 → 8000 after entering range.

- **Live-game-broken-surface answers (per Experiment 01 discipline):**
  1. **What state must work at runtime that no unit test exercises?** The full out-of-range Attack chain in `main.tscn` with the production `NavigationAgentPathScheduler` and a baked navmesh — the smoke test is "select a Piyade, right-click a Turan halfway across the map, watch the Piyade walk over and engage." The instant stub in the regression test approximates this but doesn't exercise nav-poly snapping; live-test confirms the path-resolved waypoints fan out through real terrain.
  2. **What can a headless test not detect that the lead would notice in the editor?** Smoothness of the engage transition — does the unit's velocity feel right when it crosses the attack_range boundary? Does it stop dead, stutter, or smoothly transition? Test asserts only "got close" and "damage landed"; the perceptual quality is a Phase 5 polish concern.
  3. **Minimum interactive smoke test.** Boot main scene, select an Iran Piyade, right-click on a Turan Piyade across the map. Iran should walk toward Turan (no manual intermediate move-click). Once within ~1.5 units, motion should stop and damage should start landing.

- **Known Godot Pitfalls list — no new entries.** BUG-06 is the *exact same architectural drift as BUG-01* (Pitfall #2 — FSM driver wiring: code inside states only runs when something calls it). The existing Pitfall #2 entry already covers this category; reinforcing the lesson, not a new pitfall.

- **LATER items surfaced (one new, one reinforced):**
  1. **Per-tick repath throttle in Attacking** (NEW). `Attacking._sim_tick` currently re-issues `request_repath` every tick. At Phase 2's 15-unit cap this is fine; past ~50 simultaneous engaged units the every-tick repath cost will dominate. Switch to a stale-distance threshold (re-request only when target moves more than ε since last request) or a per-N-ticks throttle when unit count grows. Already noted in the Attacking docstring; surfacing here so the planning layer can budget for it.
  2. **CombatSystem / MovementSystem phase coordinators** (REINFORCED). Same item that BUG-01 surfaced. When the coordinators ship, both the in-range `combat._sim_tick` drive AND the out-of-range `movement._sim_tick` drive move out of `Attacking._sim_tick` and into the coordinators' phase iteration. Attacking then only calls `set_target` and `request_repath` (advisory writes). Tracked, not blocking.

---

*Read this doc at the start of any implementation session. Update it at every phase milestone. Keep the table in §2 honest — it's the project's mirror.*
